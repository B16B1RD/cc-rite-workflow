---
type: reviews
source_ref: "pr-659"
captured_at: "2026-04-25T07:44:16+00:00"
pr_number: 659
title: "PR #659 review results"
ingested: false
---

## Review Results

- **PR**: #659 — fix(projects-status): 3 経路の Status 更新を projects-status-update.sh delegate に統一 (#658)
- **Type**: review
- **Reviewed at**: 2026-04-25T16:40:50+09:00
- **Reviewers**: prompt-engineer, code-quality, error-handling

### Finding Patterns

本 PR は inline GraphQL+gh project 3 段 pipeline を共有 script delegate に置き換える refactor。3 reviewer が共通して検出したパターン:

1. **散在する delegate site の non-uniform implementation**: 同一 logic を 6 箇所で繰り返す Markdown command files で、1 箇所だけ stderr 抑止 / wildcard collapse / level-3→level-4 numbering の skip など micro-divergence が発生。high-confidence cross-validation で MEDIUM → HIGH 昇格。
2. **Cross-file documentation drift**: 削除済み Phase numbering への内部参照 (cleanup.md → archive-procedures.md, start.md → ready.md) が refactor 中に取り残される。MEDIUM 以上が複数。
3. **Markdown-only delegate instruction の LLM 実行揺らぎリスク**: 6 箇所のうち 5 箇所が markdown 散文のみで bash 実装例なし。LLM が `.warnings[]` surface を忘れる経路がある。Phase 4.6.3 のみ完全 bash 実装になっている asymmetry。
4. **`failed|*)` wildcard collapse**: 将来の `.result` 値追加時 silent miscategorization (defensive programming gap)。
5. **仕様への疑問**: Issue #658 の Error/Constraints `1 回 retry` 要件が `projects-status-update.sh` に未実装。delegate 化と retry が独立 scope なら別 Issue 化推奨。

### Severity Distribution
- CRITICAL: 0
- HIGH: 3 (cross-file drift × 2 + cross-validated stderr suppression × 1)
- MEDIUM: 4 (sub-numbering / narrative inaccuracy / dead state / wildcard collapse)
- LOW: 5 (whitelist comment / message grouping / 5 sites markdown-only / flag set instruction / idempotency note)

### Project-specific Insights

- **canonical pattern source**: `projects-status-update.sh` の delegate 契約は `commands/issue/start.md` Phase 2.4 / 5.5.1 / 5.7.2 の 3 invocations が canonical。本 PR の 6 invocations はこれを model にコピーしている。Phase 4.6.3 のみ state-machine を残しているが、6 箇所間 lexical unification の対象として review されるべき。
- **Markdown command files の literal substitution 依存**: rite plugin の commands/skills/agents は LLM が bash 生成時に `{plugin_root}` / `{issue_number}` / `{owner}` 等を literal substitute する前提で書かれている。delegate-only 経路 (5 箇所) は変数共通化を意図的に避けているが、bash 実装例の omit は `.warnings[]` surface 漏れの実 risk を生む。

