---
type: reviews
source_ref: "pr-2094"
captured_at: "2026-08-03T04:41:03+00:00"
pr_number: 2094
title: "PR #2094 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2094 — fix(hooks): 作業メモリの pr_number / loop_count を通常更新経路で carry-forward する
- **Type**: review
- **Reviewed at**: 2026-08-03T13:39:07+09:00
- **Reviewers**: code-quality, error-handling, test, tech-writer, prompt-engineer, security
- **Cycle**: 2 (前 cycle 3 件はすべて解消を実測確認、新規 5 件)

### Finding Patterns

本 cycle の 5 件は「前 cycle の修正が defect class より狭かった」2 件と「修正自体の副作用」3 件に分かれた。後者が 3/5 を占める点が本質的な教訓。

1. **修正が defect class より狭い (2 件)**: 前 cycle は「区切り文字 0x1f が値側でエスケープされない」列ずれを `gsub(""; "?")` で塞いだが、**同じ迂回が NUL バイトで残った** — bash の command substitution が NUL を*削除*するため、断片が連結されて `12<NUL>34` が `1234` という「もっともらしい別の数値」になり、非数値降格も WARNING も発火しない。同様に「別 Issue の値を転写しない」遮断は `issue_number_mismatch` を判定キーにしたため、`issue_number` 行を**削除**すれば迂回できた。**教訓**: 「特定の 1 バイト/1 条件」で書いた防御は、defect class 全体 (制御文字全域 / identity を確認できない全ケース) を覆っているかを、修正の直後に自問する。文字クラスなら `[[:cntrl:]]`、条件なら「検証できない」を包含する述語へ広げるのが定石。

2. **修正の副作用 — テストの変数再利用 (1 件)**: 新規テストをファイル末尾へ追加する際、「番号順では空きに見える」接尾辞 (`SBX18`) を再利用した。既存 T-14 が同じ変数を束縛しており、現状は評価順のおかげで実害がないが、T-14 側に追記した瞬間に別 sandbox を無言で読む。**教訓**: トップレベル変数を使う bash テストでは、宣言順と番号が乖離しているファイルほど「未使用番号」の判定を目視に頼れない。追加前に `grep -c` で衝突を確認する。

3. **修正の副作用 — 対称化が別種の非対称を残す (1 件)**: 「兄弟 WARNING と文言が片肺」という指摘に対しリテラルを揃えたが、**半角スペース 1 個が欠けて byte 一致にならなかった**。3 reviewer が独立に検出。**教訓**: 「2 箇所の文言を揃える」修正は、揃えた後に `grep -c` で一致数が期待どおりかを機械確認する。リテラル二重化を選んだ時点で目視 drift は不可避なので、検証を機械側に寄せる。

4. **修正の副作用 — 文言変更に kill power がない (1 件)**: WARNING の文言を条件付き表現へ直したが、**その文言が宣言する挙動を検証する走行が存在しない**。文言を旧断定形へ戻す変異でも全アサーションが生存する。しかも同じ一文が同一 commit 内で既に drift していたことが未検査の直接証拠になった。**教訓**: 診断文言の修正は「文言リテラルを pin する」のではなく「文言が宣言する振る舞いを走行で pin する」。前者は他コンポーネントのメッセージ形式への結合を増やすだけで脆い。

### プロセス上の観察

- **修正が計測そのものを変える場合、計測の記述も同 cycle で更新する必要がある**。前 cycle でテストスイートの自己切断を直した結果、PR 本文が主張していた baseline の Red 件数が偽になった。tech-writer が全 7 項目を独立再現して一致を確認しており、この更新を怠ると「その PR が別途是正した stale ドキュメント問題」を PR 本文自身が再生産する。
- **例外カテゴリの設計が機能している**: error-handling は 0x1f 列ずれ (cycle 1) と bare CR (cycle 2) の両方を検出しながら「Hypothetical Exception Category 外」として調査推奨に留め、閾値判断を security へ委ねた。security が例外カテゴリとして指摘化し修正に至った。同じ機構を 2 cycle 連続で正しく通しており、ゲートが空振りの blocking を抑止しつつ実質的な穴は拾えている。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 4
- LOW-MEDIUM: 1
- LOW: 0

