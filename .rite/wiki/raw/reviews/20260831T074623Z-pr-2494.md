---
type: reviews
source_ref: "pr-2494"
captured_at: "2026-08-31T07:46:23+00:00"
pr_number: 2494
title: "PR #2494 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2494 — fix(projects): Cancelled を終端 Status として認識させ reconcile 先を stateReason で分岐する
- **Type**: review
- **Reviewed at**: 2026-08-31T16:45:23+09:00 (cycle 2, 差分スコープ)
- **Reviewers**: application, error-handling, test, tech-writer, prompt-engineer, security

### Finding Patterns

- **語彙を変えたら同じ語彙の全 hit を sweep する**: SoT の `Cancelled` の意味から `duplicate` を外したが、同じ SoT の Consumers 表に載っている consumer script の user-facing WARNING に旧語彙が残り、新旧 2 版を同時に配布する状態になった。3 reviewer が独立到達。文言を直したら `git grep` で同一列挙の全コピーを当たる。
- **一般化を戻すときも全 hit を戻す**: 「A Done transition」→「A terminal transition」の過剰一般化を rationale 側だけ戻し、同文を持つ script header を取り残した。1 箇所を直した時点で「同じ文はほかにないか」を必ず引く。
- **件数を断定する列挙は、根拠が構造的なら必ず破れる**: 「Two documented paths が無条件 Done を書く」と数えたが、その根拠（委譲先 helper が現在値を読まない）は helper の性質であり全 caller が継承する。実数は 6。4 reviewer が独立到達。件数ではなく helper への帰属で書けば列挙は不要になる。
- **無条件主張の限定を求められた修正文が、別の無条件主張になる**: 「両終端値に到達可能」の無条件性を直した文の末尾が「誰かが手で option を足すまで報告され続ける」という別の無条件主張になっていた（実際の走査窓は `--limit` 既定 100 の有界窓）。限定句を足す修正では、置き換え後の文にも同じ検査をかける。
- **negative control には positive control を対にする**: 「WARNING が出ないこと」だけを見るテストは、対象経路に到達しなかった場合も PASS する。fixture パスの typo で緑のまま通ることを実測で確認した。rc と副作用ログの少なくとも一方を同じ run で assert する。
- **静的 grep のラベルに挙動を名乗らせない**: `assert_file_contains` のラベルが「無警告 Done」と名乗っていたが、検査するのは代入行の literal だけで stderr は見ていない。失敗時にラベルだけが道標になるため、ラベルは検査の実体に合わせる。
- **read-only モードの診断で書き込みを含意する現在形を使わない**: 既定 dry-run で「reconciling to Done」と出す WARNING が、script 自身が宣言する read-only 契約と矛盾していた。診断文言は実行モードに依存しない形（`reconcile target:` 等）で書く。

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 3
- LOW: 0

