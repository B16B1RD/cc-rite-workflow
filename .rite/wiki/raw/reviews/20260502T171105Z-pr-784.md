---
type: reviews
source_ref: "pr-784"
captured_at: "2026-05-02T17:11:05+00:00"
pr_number: 784
title: "PR #784 review results"
ingested: false
---

## Review Results

- **PR**: #784 — test(hooks): #771 4-site bash 引数対称化の grep test を追加 (P4-12)
- **Type**: review
- **Reviewed at**: 2026-05-03T02:10:20+09:00
- **Reviewers**: test, error-handling, code-quality

### Finding Patterns

- **`grep -c || echo 0` 罠の再導入**: bash test scripts で `count=$(grep -cE ... || echo 0)` パターンは grep の no-match (exit 1) 時に count を `"0\n0"` (double-printed) に汚染する。同リポジトリの `session-start.test.sh:760` に作者自身の警告コメントが既に存在 (`|| true` + `${var:-0}` パターンが正解) にも関わらず、新規 PR で同じ罠が再発した。プロジェクト内の慣用パターン参照不足が原因。

- **Self-reference filename mismatch**: bash test スクリプトの summary 行で実ファイル名と表示名が不一致 (`echo "─── test-foo.sh summary ───"` vs 実ファイル `foo.test.sh`)。同リポジトリの類似 metatest (`cross-session-guard-invocation-symmetry.test.sh`) では一致しており、`$(basename "$0")` 利用でドリフト防止可能。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 1
- LOW: 0

