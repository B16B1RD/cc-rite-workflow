---
type: reviews
source_ref: "pr-2070"
captured_at: "2026-07-31T06:44:14+00:00"
pr_number: 2070
title: "PR #2070 review results"
ingested: false
---

## Review Results

- **PR**: #2070 — fix(lint): 説明的番号参照の検出範囲を index.md へ広げ log.md / raw/ の除外を明文化する
- **Type**: review
- **Reviewed at**: 2026-07-31T15:42:10+09:00
- **Reviewers**: application, error-handling, prompt-engineer, test, tech-writer, security

### Finding Patterns

blocking 指摘 2 件はいずれも「実装は正しいがテストが新規分岐を pin していない」型で、6 reviewer のうち test-reviewer だけが検出した。他 5 名は helper を実際に走らせて挙動の正しさを確認し「可」と判定しており、実装の正しさとテストの網羅性が独立した軸であることが observable な形で現れた事例。

- **プロジェクト固有の癖 1: 「構成で保証する」設計判断は、その構成を pin するテストの網羅性が担保そのものになる。** D-06 は「AC-6 の非回帰は測定ではなく構成（ステップ 2.2 の pages_list を変更しないこと）で保証する」と主張し、TC-35 が SKILL.md 本文の regex を静的 pin していた。しかし pages_list 構築は separate_branch (grep -E) と same_branch (find) の 2 経路あり、TC-35 の抽出式は grep 由来の literal しか掴まないため後者が未検査だった。構成論を根拠に測定を省いた場合、構成 pin に穴があると担保がゼロになる。
- **プロジェクト固有の癖 2: 「形式移行に耐える」ための行単位分岐は、混在 fixture が無いとファイル単位判定への変異が生き残る。** D-07 は wiki index が表形式 → OKF 箇条書きへ移行する途中の混在に耐えるため行単位で形式判別する設計を採ったが、19 本の fixture はすべて単一形式で、混在を通す assert が 1 本も無かった。「移行期に必ず発生する形状」を守る分岐は、その形状の fixture が無ければ守られていることを検証できない。
- **レビュー手法: 6 reviewer 全員に helper の直接実行とテストスイート実行を明示的に指示したことで、全員が推測でなく実測を添えて判定した。** 結果として blocking 判定に必要な `Verification:` アンカーが自然に付き、非実測 non-blocking 降格が 0 件になった。実行可能なテストが差分に含まれる PR では、reviewer prompt で「実際に動かせ」と明示するのが有効。
- **仕様逸脱の裁定パターン: Issue 本文の指示 (§4.1) と AC (AC-6) が両立しない場合、PR 側が AC を優先して逸脱し Decision Log に記録するのが妥当と 4 名が独立に裁定した。** ただし裁定の根拠となった Decision Log の記述自体に誤り (孤児検出の集合差分の向きが逆) があり、reviewer が実コードで裏取りして訂正した。Decision Log の断定も実測で裏取りする対象である。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 2
- LOW: 0

