---
type: reviews
source_ref: "pr-636"
captured_at: "2026-04-21T02:49:47+00:00"
pr_number: 636
title: "PR #636 review results"
ingested: false
---

## Review Results

- **PR**: #636 — fix(issue-create): create-interview return 後の implicit stop を多層防御で防ぐ (#634)
- **Type**: review
- **Reviewed at**: 2026-04-21T11:46:39+09:00
- **Reviewers**: prompt-engineer, code-quality, error-handling, tech-writer, test

### Finding Patterns
8 回目の implicit stop regression 累積対策 PR (#634) のレビューで検出された典型的パターン:

1. **Step 数の記述 drift**: 新規 Step 追加時に preamble の範囲指定 ("Step 1-2" → "Step 0-3") を忘れる pattern。prompt engineering PR で頻出。同一 section 内で新旧記述が混在すると LLM は古い指示に従いやすい。

2. **"three-line" vs "four-line" spec drift**: sub-skill return block の行数仕様が同一ファイル内で矛盾。LLM が相反する指示を読んで sub-skill return 生成を失敗する経路。contract phrase の grep 検証では捕捉不能。

3. **emit-but-never-consumed marker**: 防御層強化で新 marker (`MANDATORY_AFTER_INTERVIEW_STEP_0=1` 等) を追加するが consumer (stop-guard grep / test assertion / regression spec) を同時追加し忘れる pattern。2 reviewer が同時指摘 (prompt-engineer + code-quality cross-validation 合意)。

4. **silent failure in defensive bash**: `flow-state-update.sh patch --if-exists` の exit code を未検査で後続処理に流す pattern。`--if-exists` が「file 不在 skip」と「patch 成功」を両方 exit 0 で返すため区別不能。Fail-Fast First 原則違反。

5. **cross-TC 独立性 convention drift**: 既存 test で統一されていた `fresh_ts="${fresh_ts:-$(date ...)}"` defensive fallback pattern が新規 test 1 箇所だけ欠落。test 順序依存で silent false-pass のリスク。

6. **state vs HINT 表示の乖離**: stop-guard.sh の jq write 失敗時に HINT は `error_count=2` を emit しても実 state file は前回値のまま残る silent 乖離。新規 HINT 強化で乖離が surface する副作用。

7. **stale script reference in doc**: regression spec が存在しない verify-634-structure.sh を参照。fixture 信頼性低下。

### Severity Distribution
- CRITICAL: 0
- HIGH: 4
- MEDIUM: 6
- LOW: 3

### 経験則として残すべき知見
- **Step 数変更時は preamble 範囲指定を必ず sync**: "Step X-Y を実行" 記述は Step 追加のたびに grep 対象とすべき。drift-check anchor + lint 自動化が望ましい。
- **新 marker 3 点セット規約**: marker を追加するなら (1) emit site、(2) consume site (grep 先)、(3) test assertion を同一 PR で揃える。欠けた場合は dead code として推奨事項に残す。
- **bash defensive flag は exit code 区別とセット**: `--if-exists` / `|| true` / `2>/dev/null` 等を使う際は「success / skip / failure」の 3 状態を区別する分岐を必ず設ける。
- **累積対策 PR では既存 convention の drift に注意**: 7 回目以降の同種 regression 対策では既存 test/doc pattern の drift が発生しやすい。cross-file impact check で catch する。

