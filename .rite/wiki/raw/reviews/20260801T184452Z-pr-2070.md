---
type: reviews
source_ref: "pr-2070"
captured_at: "2026-08-01T18:44:52+00:00"
pr_number: 2070
title: "PR #2070 review results (cycle 2)"
ingested: false
---

## Review Results

- **PR**: #2070 (cycle 2)
- **Type**: review
- **Reviewed at**: 2026-08-02T03:43:26+09:00
- **Reviewers**: application, error-handling, prompt-engineer, tech-writer, test, security

### Finding Patterns

cycle 2 の 4 件の blocking 指摘のうち **1 件は cycle 1 の fix が導入した regression**、残る 3 件は「片側だけ pin されている」同型だった。

1. **anti-pattern 回避のために書いたガードが dead assertion になった (application, MEDIUM)** — cycle 1 で「実在しない見出しへの参照を作らない」ことを pin するため `assert_not_grep "$FILE" 'ステップ 3-7\.4\|ステップ 7\.4'` を追加したが、helper 側の `assert_not_grep` は `grep -qE` (ERE) を使うため `\|` は選択子ではなく **literal のパイプ文字**になる。結果として「2 語をパイプで連結した 1 本の文字列」を探すパターンになり、対象ファイルには決して現れず常に緑。修正前の状態へ戻しても検出しないことが mutation で確認された。**教訓: `assert_not_grep` / `grep -q` 系にパターンを渡すときは BRE (`grep`) と ERE (`grep -E`) でメタ文字の意味が反転することを必ず確認する。特に選択子は BRE で `\|`、ERE で `|`。テストを足したら「そのテストが本当に落ちるか」を、修正前の状態を復元する mutation で必ず確かめる — 緑のまま通ることこそが検証すべき失敗モードである。**

2. **fixture が「素朴な形」しか持たず、相互作用が測れていない (test, MEDIUM ×2)** — 新設した OKF 箇条書き経路の fixture は、リンク・区切り・サマリーがマスク処理と一切干渉しない形しか持たなかった。テーブル経路は生パイプ入りタイトル (TC-27) とコードスパン入りサマリー (TC-50) で相互作用を pin しているのに、箇条書き側だけ空白。行頭 anchor を外す変異も、マスク前後の文字列を取り違える変異も、どちらも 172 PASS のまま通った。**教訓: 同じ処理を 2 経路で実装したら、fixture の「意地悪さ」も 2 経路で揃える。片方に生パイプ / コードスパン入りの fixture があるなら、もう片方にも同じ厄介さを持つ行を置く。素朴な fixture は経路の存在は測るが、経路の正しさは測らない。**

3. **同一ファイル内で前提を否定する記述が残った (tech-writer, MEDIUM)** — SPEC 側に「index のカタログ形式は既知の逸脱」という carve-out を足しながら、同じ話題を扱う reference の「**完全準拠した** bundle は visualizer で閲覧できます」を更新しなかった。しかも SPEC の carve-out が手順の参照先としてその reference を名指ししており、読者が送られる先が未更新の節という受け渡しになっていた。**教訓: 準拠性の carve-out を 1 箇所に足したら、「完全準拠」「フル対応」等の無条件肯定を同一トピックの全参照先で grep する。carve-out は追加より削除の同期漏れを生みやすい。**

### Process Observations

- **fix が新しい欠陥を持ち込む率が高い**: cycle 1 で 2 件修正し、cycle 2 で 4 件検出。うち 1 件は cycle 1 の fix 由来。レビュー→修正ループでは「修正した箇所自体を次 cycle の第一級のレビュー対象にする」ことが実効を持つ (application reviewer が最初に見つけた)。
- **mutation testing が指摘の質を決めた**: cycle 2 の blocking 4 件のうち 3 件は隔離 worktree での変異注入によって発見された。「テストが通る」ではなく「変異を注入しても通る」を測ることでしか見えない欠陥クラスがある。
- **前 cycle で 0 件だった reviewer にも同じ深さを要求する価値がある**: application reviewer は cycle 1 で 0 件だったが、cycle 2 で最も重要な指摘 (前 cycle の fix の regression) を出した。「前回 0 件だったから浅くてよい」は成立しない。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 4
- LOW: 2

