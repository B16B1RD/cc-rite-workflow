---
type: reviews
source_ref: "pr-2466"
captured_at: "2026-08-29T15:20:45+00:00"
pr_number: 2466
title: "PR #2466 review results"
ingested: true
---

## Review Results

- **PR**: #2466 — fix(open): 検出した親 Issue 番号を flow-state へ記録する
- **Type**: review
- **Reviewed at**: 2026-08-30T00:19:02+09:00
- **Reviewers**: security-reviewer, application-reviewer, test-reviewer

### Finding Patterns

散文駆動スキル（SKILL.md）の契約変更に対し、static-contract テスト（`assert_grep_in_section` による節限定 literal grep pin）を追加する PR で観測されたパターン。

1. **追記箇所と pin の 1 対 1 対応が崩れやすい**: 本 PR は SKILL.md の 3 箇所（2.4(B) の retain 指示 / 2.6 の条件付きフラグ付与 / Placeholder Legend と Note の substitution 契約）を追記したが、追加したアサーションは前 2 者のみを覆い、3 つ目（Legend + Note）が未 pin だった。test-reviewer が「その 1 箇所だけを base 版へ戻すとスイートは 17/17 green のまま通過する」ことを隔離 worktree の mutation で実測して検出。**契約を追記したら、追記した節ごとに pin があるかを数え合わせる**のが有効な検査手順。

2. **散文が新たに依存し始めた helper の挙動は helper 側にも pin が要る**: 2.6 に「flow-state.sh 側が未指定フィールドを merge-preserve するため書き込みは 1 箇所でよい」という設計上の依存を新設したが、`flow-state.sh:398` の merge-preserve を `parent_issue=0` へ変異させても既存スイートは全件 green だった。散文側の pin（「渡すこと」）と helper 側の pin（「保持し続けること」）は別物で、AC は両方に依存する。

3. **同一物理行を 2 つの assert が見る構成**: 2.6 の同一段落を対象にした 2 アサーションは、その行が削除されると常に同時に落ちる。片方だけを書き換える変異は各パターンが独立に検出できるため検証力の欠陥ではないが、将来段落が分割されたときの追随性のためコメントで明示しておくとよい。

4. **セクション境界パターンの前方一致**: `S26_END='^## ステップ 3'` は同ファイル内の `## ステップ 3.3 の計画` と前方一致する。awk range が最初の一致で閉じるため現状は正しく動くが、見出しが挿入されると extraction 範囲が変わる。`'^## ステップ 3: 実装計画'` のように見出し全体で閉じると意図が明示される。

5. **帰結クラス降格の実例**: 実測付き（mutation で failing_test を確認）でも、帰結が「検出網の穴」に留まる指摘は class B。ただし本 PR の diff が base 側の既存禁止文（Note の substitute 禁止リストから `{parent_issue_number}` を削除）を弱体化していたため、その削除に対する番人不在を指す指摘は exclusion 付き class B として blocking を維持した。「既存記述の削除/弱体化に対する pin 不在」と「新規追加分の pin 強化」は同じ検出網クラスでも扱いが分かれる。

6. **Complexity 宣言と実態の照合**: 3 reviewer とも `S` 宣言を妥当と判定。共有 helper のコード変更ゼロ・公開契約の拡張なし・+25/-2 行が根拠。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 1 (class B 降格により non-blocking)
- LOW: 0

