---
type: reviews
source_ref: "pr-2470"
captured_at: "2026-08-30T01:12:25+00:00"
pr_number: 2470
title: "PR #2470 review results"
ingested: false
---

## Review Results

- **PR**: #2470 — fix(open): Projects Status → In Progress の取りこぼしを検証ゲートと残留検知で塞ぐ
- **Type**: review
- **Reviewed at**: 2026-08-30T10:10:59+09:00
- **Reviewers**: prompt-engineer, code-quality, error-handling, test, tech-writer, security

### Finding Patterns

- **post-condition gate の「検証対象なし」分類が広すぎると、守るべき failure mode がその分岐で無音通過する**: gate が返す `skipped`（検証対象なし）に、sibling helper の on-board policy をそのまま持ち込んだ結果、「盤面未登録」= 実は「守るべき手順が未実行」と同値の状態まで clean bill に落ちた。第 2 の網（watchdog）も同じ状態を除外していたため両方をすり抜けた。同型の分類を別 helper から借りるときは、呼び出し文脈の前提（この gate は `auto_add: true` の直後に走る）が同じかを確認する必要がある。2 名の reviewer が独立に検出。

- **新設した marker の値域を散文で宣言しても、値を作る式が空を返しうると契約が破れる**: `jq -r '.result // "failed"'` は入力が空文字・非 JSON のとき無出力になり fallback が発火しない。marker が空になると「marker が出ていない」と機械的に区別できず、可視化のために足した marker が最も必要な失敗時にだけ消える。2 名の reviewer が独立に検出。

- **静的 pin だけのテストは実装のコアロジックを守らない（mutation で実証）**: 新規 helper 259 行に対し、テストは SKILL.md の表行を grep する静的 pin と、gh を無条件失敗させる 1 経路の behavioral assert のみだった。`emit missing` → `emit ok` の書き換えでも全テストが緑のまま通ることを mutation で実証。テストの網が「文書」を pin していて「挙動」を pin していない状態は、件数だけ見ると充実して見える。

- **走査条件を変える diff は、その条件を説明する定点コメントを同時に嘘にする**: draft skip を削除した結果、直上のセクション見出し `# --- Scan OPEN, non-draft PRs ---` が実装と正反対になった。ヘッダー本文は正しく更新されていたのに、走査ループ直前の見出しだけが取り残された。

- **`Verification:` アンカーの種別ラベルは `repro` / `failing_test` に限られる**: reviewer が `static_verification` を使った 12 件が実測必須ゲートの正規形 regex に一致せず、未判定（blocking のまま）として据え置かれた。降格側に倒れないため安全側だが、実測の有無が判定できない状態が残る。

### Severity Distribution

- CRITICAL: 1
- HIGH: 3
- MEDIUM: 4
- LOW-MEDIUM: 2
- LOW: 3

