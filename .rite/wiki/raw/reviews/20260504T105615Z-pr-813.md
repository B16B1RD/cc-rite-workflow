---
type: reviews
source_ref: "pr-813"
captured_at: "2026-05-04T10:56:15+00:00"
pr_number: 813
title: "PR #813 review results"
ingested: true
---

## Review Results

- **PR**: #813 — refactor(create-decompose): #806 create-decompose.md 本体スリム化 (#773 follow-up)
- **Type**: review
- **Reviewers**: prompt-engineer-reviewer, tech-writer-reviewer, code-quality-reviewer

### Finding Patterns
本 PR の review で実測された 10 finding は以下の pattern に分類される:

1. **Broken cross-reference (3 reviewer 一致 — high confidence)**: refactor で削除した heading (`#### Section ID Reference Guide (for Phase 0.8)`) を別箇所の prose が文字列で参照し続ける silent dangling reference。Markdown 大規模圧縮 refactor の典型的な silent regression。検出は `grep -F '"caption-text"' file.md` で機械化可能だが、reviewer 側の cross-validation で 3 人独立検出された

2. **Moved content の forward/backward pointer 残存 (2 reviewer 一致)**: Phase 0.9.1 bash inline comment の `(see "structure defined below")` が文書下方を指すが、structure を別ファイルに Moved した後も "below" 文言が残る。圧縮時にコメント文を Moved 通知と同期させる check が必要

3. **Asymmetric Fix Transcription (1 reviewer 単独だが Wiki 経験則 high confidence)**: parent-routing.md の DRIFT 警告と create-decompose.md の対称的 DRIFT 警告のうち、後者を圧縮で削除し対称性破壊。「Asymmetric Fix Transcription」(refactor pattern) は累積 20+ 回観測。peer reference 側の sync コメントを削除する際は対称位置への波及確認が必須

4. **位置づけ table の SoT 列 stale**: bulk-create-pattern.md に新節を追加した結果、既存「位置づけ」table の Phase 0.9.1 SoT 列を更新し忘れる drift。新 SoT 宣言時の pre-existing duplicate 顕在化 (PR #801 累積) と同型

5. **Design document の目標行数 stale**: Umbrella design doc の goal-setting (≤200/≤300/≤300) と実際の Sub-Issue PR 着地 (#809=331 / #813=489) の乖離。圧縮 refactor の AC 緩和を design doc 側に back-propagation する必要がある (PR #809 で確立した運用が design doc に shed されていない)

6. **Hardcoded 行番号 cross-reference の stale 化**: regression-history.md の `L578` 参照が、本 PR で対象ファイルが 489 行に縮んだことで stale 化。同時に参照先 inline comment 自体を削除したため二重 stale。「DRIFT-CHECK ANCHOR は semantic name 参照で記述する (line 番号禁止)」の延長で、references/regression-history への参照も同様の原則を適用すべき

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 4
- LOW: 4

### 経験則候補

- **圧縮 refactor の caption-string cross-reference は機械検証する**: refactor で heading を削除/統合する際、別 prose / table が削除済み caption を文字列参照していないかを `grep -F` で機械検証する pre-commit gate を持つ
- **Moved content の inline pointer 同期化**: Markdown blockquote `> **Moved**:` 通知と bash inline comment 内の "see below"/"see above"/"defined below" 文言を同期する。Moved 通知側を変更したら inline pointer も更新するルール
- **Design document への着地点 back-propagation**: Umbrella design doc を起点とする Sub-Issue series で AC が緩和された場合、最初の着地で design doc 側の goal-setting も更新する (#809 累積 → #813 で同パターン再発した実測ベース)

