---
type: reviews
source_ref: "pr-2120"
captured_at: "2026-08-06T01:33:18+00:00"
pr_number: 2120
title: "PR #2120 review results (cycle 4)"
ingested: true
---

## Review Results

- **PR**: #2120
- **Type**: review (cycle 4)
- **Reviewed at**: 2026-08-06T10:32:14+09:00
- **Reviewers**: application, error-handling, test, tech-writer, security（並列起動）

### Finding Patterns

**blocking が 4 → 3 → 4 → 1 へ落ちた cycle**。application と error-handling が指摘 0 件、残る 3 者も計 3 件（うち 2 件は非実測でコメント修正）。cycle 3 の「訂正ではなく削減」が効いた形で、コメント由来の finding は 4 件 → 2 件へ半減した。

唯一の blocking は **「fix で追加した防御に回帰テストが無い」** という構造的な穴で、test と security が独立に同一 mutation（中和補間 9 箇所を素の変数展開へ戻す）で実測した。hook 全 114 suite が両方で green のまま通る。**防御を足したら、その防御が消せないことを pin する** — これは cycle 1〜3 で繰り返し学んだはずのことが、cycle 3 の中和追加でだけ抜けていた。security 起因の修正は「脅威が塞がったか」の確認に意識が向き、「塞いだ状態が維持されるか」の pin を忘れやすい。

### 「同一箇所 4 cycle 連続」への 5 reviewer の回答

orchestrator が全 reviewer に「同一箇所への指摘が 4 cycle 連続なら機構ごとの単純化が必要というシグナル」と明示して投げた結果、**5 者中 4 者が同じ結論を返した**: 単純化すべきは本 PR のコードではなく、**リポジトリ内に 4 コピーある「ディレクトリに `*` だけの `.gitignore` を同梱する」idiom** の側である。4 コピーは guard（`-f` / `-s`）・失敗時の扱い（無音 / WARNING）・`LC_ALL=C` の有無・`[CONTEXT]` marker の有無がすべて分岐しており、新しいコピーを書く人がどれを写すかで品質が決まる。本 PR 単独では live defect が無いため blocking にはならず、共有 helper への抽出は follow-up Issue とするのが 4 者の一致した推奨だった。

**教訓**: 同一箇所への指摘が連続するとき、原因が「その箇所の品質」ではなく「その箇所が N 番目のコピーであること」にある場合がある。前者なら追加パッチ、後者なら抽出が答えで、両者の判別は **同じ idiom のコピー数を数える**ことでつく。

### error-handling が測った「この機構は何を買ったか」

cycle 3 で `$to` に追加した中和が、`$to` の唯一の供給元（`--phase`）に対しては **no-op** であることが実測された。0x9b を含む `--phase` は phase enum のどれとも一致しないため、同一実行の数行前に既存の `WARNING: unknown phase: $phase` が raw バイトをそのまま出す。つまり `$to` 分の中和は防御になっていない（`$from` / `$log_dir` / `$log_file` は他に raw 出力経路がないので有効）。

これを指摘ではなく計測として報告した判断が正しい — 既存行の中和追加は単一ユーザー開発機の宣言下で Guardrail #2 に該当し、要求できない。**cycle 4 で測るべきは「この機構が何を買ったか」であり、買えていない部分を正直に記録しておくと、後の cycle で同じ観点が再提起されたときの打ち切り根拠になる**。

### 非存在の consumer を現在形で語らない

「the only consumer of this log parses timestamps」という assert のコメントが、同一 PR が「consumer は将来の集計 Issue」「読む subcommand は存在しない」と明記している consumer を現在形で主張していた。読み手が grep しても 0 件で、陳腐化なのか将来形なのか判別できない。**write-only を謳う成果物について「consumer が〜する」と書くときは、その consumer が実在するかを確認する。将来形なら将来形と書く。**

### Severity Distribution
- CRITICAL: 0
- HIGH: 1（2 reviewer 独立検出による High Confidence boost）
- MEDIUM: 1（非実測で non-blocking）
- LOW-MEDIUM: 1（非実測で non-blocking）
- 収束トレンド: blocking 4 → 3 → 4 → 1

