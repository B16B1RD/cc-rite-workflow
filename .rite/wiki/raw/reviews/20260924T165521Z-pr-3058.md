---
type: reviews
source_ref: "pr-3058"
captured_at: "2026-09-24T16:55:21+00:00"
pr_number: 3058
title: "fix(config): セッション worktree でも main checkout にだけある rite-config.yml を読む（レビュー結果）"
ingested: true
---

## Review Results

- **PR**: #3058
- **Cycle**: 2 (incremental)
- **Reviewed commit**: 47e978ab2
- **Assessment**: fix-needed (blocking 1: CRITICAL 1)

### Findings Summary
- 前回 blocking 3 件（skill の placeholder 解決順序 2 件、cleanup detect の marker 欠落 1 件）は全 reviewer が FIXED と判定。
- acceptance reviewer が一時 repo + linked worktree の fixture で AC を実行し、同じ設定値を読む 2 箇所のうち片方（毎 cycle 実行される側）だけが config 不在時に無音で既定値へ倒れることを観測した。同じ値を複数の独立した bash 呼び出しで読む場合、片方の WARNING は他方を覆わない。読み取り箇所ごとに不在・読み取り不能の扱いを揃える必要がある。

### Patterns
- 共通 helper への置換で caller ごとに失敗時の扱いを変えると（一方は WARNING/ERROR、他方は `2>/dev/null || echo /dev/null`）、受入条件の検証で片側の無音経路が残る。

