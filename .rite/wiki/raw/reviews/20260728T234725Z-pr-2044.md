---
type: reviews
source_ref: "pr-2044"
captured_at: "2026-07-28T23:47:25+00:00"
pr_number: 2044
title: "PR #2044 review results"
ingested: true
---

## Review Results

- **PR**: #2044 — fix(iterate): サーキットブレーカー発火を batch/対話とも機械的停止に統一する
- **Type**: review
- **Reviewed at**: 2026-07-29T08:45:57+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

rite のような「散文が LLM の実行契約」なリポジトリで、分岐を 1 つ廃止する PR に固有の失敗パターンが 4 件中 3 件を占めた。

1. **同一ファイル内の新旧併存**: 波及箇所を grep で洗い出して直したつもりでも、同じ事実を別の言い回しで述べた行が同一ファイル内に取り残される（finding-cycling.md は 18 行目を直して 7 行目の「停止/ユーザー委譲」を残した）。grep のキーワードを新記述側の語彙で選ぶと旧語彙の行がヒットしないため、**廃止した概念の語彙**（「ユーザー委譲」「AskUserQuestion」「3 択」「判断を委ねる」）でも grep する必要がある。

2. **総論と各論の矛盾**: 節の冒頭に置かれた総論（docs/CONFIGURATION.md の safety 節「When a limit is exceeded, the workflow presents options: Continue / Abort / Manual intervention」）が、その節の一部だけを変更したことで各論と衝突する。総論は複数の設定キーを束ねているため、1 キーの挙動変更で総論が偽になる。変更行の前後だけでなく **その行が属する節の冒頭まで遡って読む** ことが必要。

3. **正典フィールド定義の未追従**: 挙動を説明する散文（SPEC.md:853）は直したが、同じファイル内のスキーマ表（SPEC.md:1373 の `cycle_count` フィールド定義）が旧規則のまま残った。散文とスキーマ表は別の読者（利用者 vs 実装者）を持つため、grep が散文側の語彙にしかヒットしない。**フィールド名そのもの**（`cycle_count`）で grep すると拾える。

4. **enum 値の意味の過負荷**: 新しい marker 値（`RESET=failed`）を導入するとき、その値が複数の異なる原因で立つ設計にすると、消費側が原因を区別できず誤った復旧手順を提示する。本 PR では `RESET=failed` が「発火後再実行の即時再発火」と「fresh entry での stale counter leak」の両方で立ち、後者で「review は 1 cycle も回っていません」という事実と逆の通知が出る。**marker 値は消費側の分岐数と 1:1 に対応させる**（`failed-refire` / `failed-stale` に分ける）。

補足: reviewer 3 名が独立に同一の欠陥（4番）を検出しており、cross-validation の severity boost が機能した。またレビュー品質面では、markdown 内の bash ブロックを `$TMPDIR` に切り出して stub 付きで実行する実測手法が全 finding に適用でき、`Verification:` アンカーの取得率が 100% になった（非実測 non-blocking 0 件）。

### Severity Distribution
- CRITICAL: 0
- HIGH: 4
- MEDIUM: 1
- LOW: 0

