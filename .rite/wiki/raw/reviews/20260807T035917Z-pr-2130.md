---
type: reviews
source_ref: "pr-2130"
captured_at: "2026-08-07T03:59:17+00:00"
pr_number: 2130
title: "PR #2130 review results (cycle 5)"
ingested: true
ingest_status: skipped
skip_reason: "推奨対応の列挙誤りが実装の誤りになる点は asymmetric-fix-transcription の追記で、散文の実測裏取りは既存ページで被覆済み"
---

## Review Results

- **PR**: #2130 — fix(pr-review): 8.0.4 positive 検査（本 cycle の結果 JSON 実在確認）を足す
- **Type**: review
- **Reviewed at**: 2026-08-07T12:58:25+09:00
- **Reviewers**: code-quality, error-handling, prompt-engineer, security, tech-writer, test

### Finding Patterns

cycle 5（差分スコープ、fix diff は +23 -12 の 5 ファイル）。cycle 4 の 9 件のうち 8 件が FIXED、
残る 1 件（F-01）が PARTIAL で、**6 reviewer 全員が同一の 1 行を独立に検出**した。

- **推奨対応が名指しした列挙をそのまま採ると、列挙の誤りがそのまま実装の誤りになる**:
  前 cycle の指摘が「trigger は `LOCAL_SAVE_FAILED` の reason（`mktemp_failure` /
  `mkdir_failure` / `write_failure`）で名指しせよ」と推奨し、fix はそれに忠実に従った。
  しかし実測すると `${TMPDIR}` 起因は `mktemp_failure` だけで、`mkdir_failure` は
  **探索先そのもの**の作成失敗、`write_failure` は caller 起因の経路を含む。結果、
  「復旧するのは探索先ではなく `${TMPDIR}`」が 2/3 の reason で**逆**を指し、
  同じ会話で save helper 自身が出す「対処: 親ディレクトリの permission…」と正面から矛盾した。
  **reviewer 側も「誤りは推奨側にある」と自認している** — 推奨に列挙を書くときは、
  列挙の各要素が主張を満たすことを個別に実測してから書く必要がある。

- **同一箇所が 3 cycle 連続で欠陥を出した**（cycle 3 で新設 → cycle 4 で全面書換 →
  cycle 5 で trigger 縮小）。いずれも「診断文が名指しする artifact / reason が
  実装のどれを指すか」の取り違えで、**散文の 1 行でも grep と実測で裏を取る対象**である
  ことを示す。なお reviewer は「散文の字面を assert で固定するのはプロジェクトの
  『assert は増やさない』に反する」として**テスト追加は推奨しない**と一致した — 担保は
  レビュー手順（注記が名指しする reason ごとに収束性を実測する）の側に置く。

- **修正が「どちらか一方でよい」を両方適用すると、片方の根拠が陳腐化する**:
  前 cycle の F-09 が 2 択で提示した対応を両方入れた結果、新しい終端錨が単独で空一覧を
  排除するようになり、同時に足した assert のラベル「(T-11f の前提)」が偽になった。

- **else 枝へ assert を足したら skip ラベルも追随させる**: `skip "T-12d-e"` のまま
  T-12f を足したため、skip 時に不実行となる 3 本のうち 1 本が記録に残らなくなっていた。

### Severity Distribution

- CRITICAL: 0
- HIGH: 1
- MEDIUM: 0
- LOW-MEDIUM: 0
- LOW: 2（いずれも scope=nit-noted）
- non-blocking (実測なし): 0

