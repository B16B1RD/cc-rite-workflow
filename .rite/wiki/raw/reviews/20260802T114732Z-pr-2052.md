---
type: reviews
source_ref: "pr-2052"
captured_at: "2026-08-02T11:47:32+00:00"
pr_number: 2052
title: "PR #2052 review results (cycle 3, loop exit)"
ingested: true
---

## Review Results

- **PR**: #2052 — docs(wiki-ingest): ステップ 6 の index.md 更新指示を実体の 5 列テーブル形式へ是正する
- **Type**: review
- **Reviewed at**: 2026-08-02T20:38:38+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling, test, security (cycle 3。この cycle でユーザー判断によりループを離脱)

### Finding Patterns

3 cycle 回して blocking が 2 → 3 → 6 と**増えた**。収束しなかった構造をここに残す。

1. **「pin が無い」系の指摘は、直すたびに新しい pin 対象を作る**: cycle 3 の blocking 6 件のうち 4 件が「テスト / drift pin を足せ」だった。内訳は (a) cycle 2 で変更した bash ブロックにテストが無い、(b) cycle 1 で追加した fixture がテンプレートの末尾改行 1 バイトに依存する、(c)(d) 配布テンプレートと SKILL.md の二重定義（ヘッダー列順 / 統計 3 行 literal）に drift pin が無い。**(b) は cycle 1 の修正そのものが生んだ指摘**で、修正 → その修正への指摘 → さらに修正、という増殖形になっている。docs 是正 PR にテスト基盤を後付けし続けると、PR の主題（散文の是正）から離れた作業が主になり cycle が終わらない。テスト基盤の不足は**一連の作業として別 Issue に括る**方が、PR 単位でも作業単位でも健全。

2. **形式変更 PR では「変更していないファイル」に欠陥が現れ、Issue のファイル単位スコープと衝突する**: producer（テンプレート + ingest 指示）の形式を変えると、consumer（`wiki-query-inject.sh`）の未対応が顕在化する。Issue はファイル単位で Target / Non-Target を切るため、「Non-Target のファイルに blocking 欠陥がある」状態が生じ、reviewer の scope 判定が `current-pr` と `follow-up` に割れた。**scope が割れたらユーザー判断**であり、rite の `follow-up` は「別 Issue へ deferred」で `current-pr` と同義ではない。

3. **cycle 2 の処置（Issue への AC 追記）を cycle 3 が「encode されていない」と再指摘した**: リリース順序 gate を Issue #2047 の AC-5 として追記したが、#2047 は `/rite:cleanup` でマージ直後に閉じる一方、gate が必要になるのはその後のリリース時点。**閉じる側の Issue に置いた制約は、必要になる時点には存在しない**。制約は「その時点でまだ open な側」（この場合 #2053）か、リリース手順が実際に読む場所へ置く。

### 有効だった検証手法

- **fix の主張を別 reviewer に mutation で追試させる**: cycle 1 の fix がコメントに書いた実測値を cycle 2 の test reviewer が 10 変異で再現し、テンプレートを develop へ全面 revert した変異で「既存 assertion は 1 本も落ちず追加した 2 本だけが red」を示した。**穴が実在したことと塞がったことを同一の実測が同時に証明する**形。
- **収束の traction を cycle 間で数える**: blocking 件数の推移（2 → 3 → 6）と、その内訳の性質（実装欠陥 → テスト基盤要求）を並べると、ループを続けても収束しないことが早期に判る。件数だけでなく**指摘の性質が「実装の誤り」から「検証資産の不足」へ移ったら、それは別 Issue のシグナル**。
- **非実測指摘を捨てずに記録経路へ流す**: 3 cycle で計 8 件が `Verification:` アンカー無しとして non-blocking へ降格したが、すべて PR 記録コメントと永続 JSON に残った。散文が実行契約であるリポジトリでは、この記録経路の実効性が「merge を止めない」ことの前提になる。

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 4
- LOW: 0

