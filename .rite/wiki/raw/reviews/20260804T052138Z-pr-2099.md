---
type: reviews
source_ref: "pr-2099"
captured_at: "2026-08-04T05:21:38+00:00"
pr_number: 2099
title: "PR #2099 review (cycle 4)"
ingested: true
ingest_status: skipped
skip_reason: "PR #2099 の cycle 1-4 のサイクル別スナップショット。同一 PR の cycle 5 raw (統合済み) が同じ経験則をより完全な形で含み、既存ページ asymmetric-fix-transcription / fix-induced-drift-in-cumulative-defense への追記で網羅済みのため個別ページ化しない"
---

## Review Results
- **PR**: #2099 (cycle 4) / **Type**: review

### Finding Patterns
blocking 推移 6 → 8 → 7 → 5（初めて明確な下降）。cycle 4 の指摘は cycle 3 の fix が生んだものが
ほぼ消え、代わりに **cycle 1-3 の伝播作業が構造的に届かなかった領域**が surface した。

**伝播確認の grep が「語彙軸」だと必ず取りこぼす**（本 PR で 3 回連続で発生）:
cycle 2 は日本語側だけ、cycle 3 は日英両方を見たが、cycle 4 で `cycle 上限` / `上限到達` という
**別語彙で同じ invariant を述べるサイト**が見つかった。同一概念が「遅い非収束」「slow non-convergent」
「cycle 上限」「上限到達」と 4 通りに表現されるコードベースでは、語彙を列挙する grep は原理的に
網羅できない。**伝播確認は「条件を指す語」（`cycle-count limit` / `max_review_cycles` そのもの）で
走らせ、ヒットした全行を目視する**のが唯一機能する方法。

**「fix の rationale を pin する」を適用した結果**（cycle 3 で導入）: cycle 4 の test 指摘は
1 件のみで、しかも「signal 経路は pin したが正常終了経路が未 pin」という**対の片側**だった。
新しい観測点を pin するとき、その観測点が**複数の経路から到達する**なら全経路を pin する
（signal / 正常終了、成功 / 失敗）。

**設定値を既定以外にしたときに断定が偽になる型**: 「発散検出は上限に関係なく発火する」という
無限定の断定が、`max_review_cycles <= 3` では偽（判定に 3 件必要なため発火可能な loop head が 0 個）。
**閾値パラメータを持つ機構の文書は、既定値だけでなく境界値でも真かを確認する。**

### Severity Distribution
- CRITICAL: 0 / HIGH: 2 / MEDIUM: 3

