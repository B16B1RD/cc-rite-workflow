---
type: reviews
source_ref: "pr-2052"
captured_at: "2026-08-02T05:46:12+00:00"
pr_number: 2052
title: "PR #2052 review results"
ingested: true
---

## Review Results

- **PR**: #2052 — docs(wiki-ingest): ステップ 6 の index.md 更新指示を実体の 5 列テーブル形式へ是正する
- **Type**: review
- **Reviewed at**: 2026-08-02T14:44:51+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

ドキュメント形式の反転（箇条書き → 5 列 GFM テーブル）を行う PR で、2 クラスの指摘が集中した。

1. **述語がデータの実状を取りこぼす** (HIGH×2): 新設した「登録行の同定述語」がセル分割前提で書かれており、title 内に未エスケープの生パイプを含む実在 9 行に一致しない。同定失敗 → 重複追加 → 重複削除も同じ述語なので回収不能、という自己修復の穴が 3 レビュアーから独立に指摘された。散文で機械的述語を定義するときは、実データの外れ値（エスケープ漏れ・列ずれ）に対して述語が何を返すかを実測で確認してから書くべき。加えて fallback の分岐が到達不能（生成ステップが新規作成時のみスコープされているのに更新経路の候補として参照）になる dangling 参照が同時に出た。

2. **形式反転が遠くのコメントを陳腐化させる** (HIGH×2 + MEDIUM×4): テンプレートの形式を反転させると、その形式を前提に書かれた rationale コメントが実装・SPEC・テストに散在したまま逆の主張をする状態になる。本 PR では rationale の「双子」（reference md と実装 helper header）のうち md 側だけが更新され、canonical 実装として名指しされている helper 側が取り残された。形式を変える PR では `grep` で旧形式の記述を横断検索し、コメント/テスト/SPEC の同型記述をすべて洗い出す必要がある。

3. **journal comment の混入** (HIGH×1): 形式変更の経緯（「#N で X へ変更したが実体は未移行のまま残り、#M で回帰させた」）を成果物の散文へ書き込む例が複数箇所に現れた。`comment-journal-check.sh` が実際に検出しており、PR 本文の「変更ファイルへの指摘なし」という自己申告と矛盾していた。経緯は PR description / commit message へ置き、成果物には現在形の制約だけを残す。

4. **同一ファイル内の確立済みパターンからの逸脱** (MEDIUM×1): 同じファイルの別ステップが mktemp + trap + stderr 退避 + WARNING という error handling パターンを確立しているのに、新規追加ブロックだけが `2>/dev/null` で rc と stderr を捨てていた。部分失敗（3 値のうち 1 つだけ 0）が全体ガードを素通りする。

### スコープ宣言との衝突

Issue の Non-Target 宣言（MUST NOT modify）に載っているファイルを、形式反転の帰結として変更せざるを得なくなるケースが発生した。3 レビュアーとも「変更内容は正しく、Issue 側の宣言を改訂すべき」で一致。Non-Target は起票時点の前提に基づくため、PR がその前提自体を変える場合は宣言の改訂が正しい解決になる。

### Severity Distribution
- CRITICAL: 0
- HIGH: 5
- MEDIUM: 5
- LOW: 0

