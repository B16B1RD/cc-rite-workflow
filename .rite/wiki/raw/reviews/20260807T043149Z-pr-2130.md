---
type: reviews
source_ref: "pr-2130"
captured_at: "2026-08-07T04:31:49+00:00"
pr_number: 2130
title: "PR #2130 review results (full pass)"
ingested: false
---

## Review Results

- **PR**: #2130 — fix(pr-review): 8.0.4 positive 検査（本 cycle の結果 JSON 実在確認）を足す
- **Type**: review
- **Reviewed at**: 2026-08-07T13:30:43+09:00
- **Reviewers**: code-quality, error-handling, prompt-engineer, security, tech-writer, test

### Finding Patterns

前の run で 5 cycle の差分スコープを消化し blocking を 7→5→4→5→1 まで落とした PR に対し、
サーキットブレーカーの上限到達後に人間が `/rite:iterate` を再実行して**新しい run の cycle 1 =
PR 全体のフルレビュー**を回した。結果、**差分スコープが 5 cycle 通して一度も見なかった面から
blocking 5 件（うち HIGH 3 件）が出た**。

- **【最重要】gate の anchor を gate される側が選ぶ構造**: 8.0.4 positive 層の判定 anchor
  `--commit-sha` は LLM が会話コンテキストの 1.2.5 出力から選ぶ値で、helper は形状検査しか
  行わず実 HEAD と突き合わせない。cycle 2+ で「区間ごと skip」と「前 cycle の SHA を拾う」が
  同時に起きると、**2 層の機械強制が同一の誤り（現 cycle 分が無いとき前 cycle の値へ手を伸ばす）で
  同時に無効化される**。security / prompt-engineer が独立に検出。教訓: **機械強制を設計するとき、
  その anchor が「強制される側が選べる値」になっていないかを問う**。marker 層には選択規則が
  4 文がかりで書かれているのに、同じ段落で新規導入した SHA 側には無かった。

- **AC の列挙が 5 群でも、テストが守るのは 4 群**: AC-6 が degraded に倒すと明示列挙する 5 群の
  うち「run pin を読めない」だけが未被覆。しかも **degraded と pass は rc が同値（どちらも 0）**
  なので rc だけの assert では原理的に検出できない — marker の**不在**を pin する必要がある。

- **fixture の並べ方が境界条件を消す**: run 境界の判定は「pin 自身の除外」と「pin より古いものの
  除外」の 2 段だが、fixture が pin を常に最古のファイルに置いていたため後段が一度も走らず、
  no-op 化しても全 assert が緑。**境界を測るテストは、境界の両側にデータを置く。**

- **assert 名と fixture の乖離**: 「別 PR の JSON が**同 SHA を持っても** pass しない」という名前の
  assert の fixture が実は別 SHA だったため、PR 絞り込みを外す変異を捕まえていたのは
  無関係な件数 assert だった。

- **差分スコープの構造的限界**: 上記はすべて、各 cycle の diff の**外側**にある。差分スコープは
  「範囲を絞るが基準は緩めない」と規定されているが、**範囲の外は基準以前に見られない**。
  cycle 上限到達後にフルレビューを 1 回挟む運用は、この限界に対する実効的な対処になっている。

### Severity Distribution

- CRITICAL: 0
- HIGH: 3
- MEDIUM: 2
- LOW-MEDIUM: 0
- LOW: 0
- non-blocking (実測なし): 6
- nit-noted: 3

