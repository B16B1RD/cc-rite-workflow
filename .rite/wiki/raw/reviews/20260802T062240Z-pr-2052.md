---
type: reviews
source_ref: "pr-2052"
captured_at: "2026-08-02T06:22:40+00:00"
pr_number: 2052
title: "PR #2052 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2052 (cycle 2 / re-review after fix)
- **Type**: review
- **Reviewed at**: 2026-08-02T15:21:21+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling, application, test

### Finding Patterns

cycle 1 の 10 件を修正した結果、その修正自体が 11 件の新規指摘を生んだ。fix-introduced regression の類型が明瞭に出た。

1. **一般化した機械的述語は境界条件で破綻する** (HIGH×1、2 レビュアー独立): cycle 1 で「セル分割」から「位置ベース」へ一般化した抽出規則が、行頭・行末の区切り文字が生む空要素を規定しておらず、全 342 行で列が 1 つずれる。散文で機械的規則を書くときは、正常系のデータで検証するだけでなく**分割結果の両端**を明示的に規定する必要がある。「決定的に働く」と書いた規則ほど、字義どおり適用したときの結果を実測すべき。

2. **是正のために導入した限定句が史実と食い違う** (CRITICAL×1): 「テーブル形式化より前に初期化された bundle は箇条書きのまま残る」という限定句を 7 箇所に複製したが、テンプレートは過去にもテーブル形式だった期間があり (箇条書きは後から導入され今回戻した)、その窓の bundle はテーブルを持つ。**「A より前」という時間限定は、A が初出のときにしか成立しない**。復元・回帰の場合は「B が配布されていた期間」のように、対象が実在した区間で限定する。史実は Issue 本文に正しく書かれていたのに、修正時に参照されなかった。

3. **伝播修正は同一ファイル内でも漏れる** (HIGH×2 + MEDIUM×2): 形式反転の伝播漏れを 5 ファイルで直したが、(a) 同じファイルの別の節 (`wiki-lint/SKILL.md:624`)、(b) 同じファイルの冒頭 Design notes (`wiki-query-inject.sh:42`)、(c) 同じファイルの別 TC のコメント (`test.sh:543`) が残った。**触ったファイルの中でも、触った箇所から離れた場所は別ファイルと同じ確率で漏れる**。伝播スキャンは「旧形式の語」でファイル横断 grep するだけでなく、変更したファイル自体を全文再読する。

4. **枠組み語の割れ**: 「未達 (達成すべき目標に未到達)」と「意図的な逸脱 (declared deviation)」は正反対の含意を持つ。参照元だけ書き換えると、参照先が逆のことを言う状態になる。

5. **番号なし表現へ書き換えた側と番号を残した側が同一 PR 内で割れる** (HIGH×1、2 レビュアー独立): journal comment の除去を 4 箇所で行った一方、別ファイルには `#N` を新規に 8 箇所追加していた。しかも lint (`comment-journal-check.sh`) は裸の `#N` を意図的に非検出とするため緑のまま通る。**lint が緑であることは規約遵守の証明にならない**。

6. **テストの pin は fixture 側の変更で無言で死ぬ** (HIGH×1、2 レビュアー独立実測): 実ファイルを `cp` する fixture は「テンプレートが変わっても追随する」利点と引き換えに、**テンプレートから検証対象の構造が消えると変異を kill できなくなる**。変異注入で実測したところ、対象規則を削除してもスイートは 180 passed で通った。この失敗は「テスト総数が変わらない」ため差分レビューでは見えない。literal fixture との併用が要る。

7. **中止経路を増やすときは報告先も同時に用意する** (MEDIUM×1): 是正で 4 つの中止経路を新設したが、いずれも警告カウンタにも完了レポートにも載らず、下流の lint 検出器も存在しない。中止しても「未完了事項: なし」と報告される。同一ファイルの別ステップには受け皿が既にあり、新規ブロックだけが非対称だった。

8. **単一 find への集約は「同一基点」を保証しない** (MEDIUM×1): 総数と内訳を同じ出力から数えても、内訳側が部分文字列一致だと基点が揃わない。「構造的に一致するので検査不要」というコメントを書く前に、その invariant がコードから反証できないかを確かめる。

### Severity Distribution
- CRITICAL: 1
- HIGH: 5
- MEDIUM: 5
- LOW: 0

