---
type: reviews
source_ref: "pr-2137"
captured_at: "2026-08-07T14:15:27+00:00"
pr_number: 2137
title: "PR #2137 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2137 — refactor(hooks): iterate 経路の marker emit・照合を共有関数 + fixture テストへ機械化する
- **Type**: review
- **Reviewed at**: 2026-08-07T23:13:23+09:00
- **Reviewers**: application, error-handling, test, security, prompt-engineer
- **Cycle**: 2 (差分スコープ、base_sha=de39e54)

### Finding Patterns

**fix が実装側を網羅的に直しても、テスト側の pin が同じ網羅度で伝播しない**。本 cycle で 3 reviewer (security / error-handling / application) が独立に検出した最大の指摘がこれ。cycle 1 の指摘は「拒否経路の ERROR 文が生値をエコーする (4 箇所)」で、fix は実数 5 箇所すべてに `_marker_scrub` を適用して**実装側は 5/5 で網羅的**だった。しかし追加したテストは 2 箇所しか pin せず、残り 3 箇所は scrub を外しても 61/61 green のまま生存した。うち 1 箇所 (`marker_get` の KEY 拒否) は interpolation の直後が `')。英数字と…` という文字列のため、`X<改行>[CONTEXT] ITERATE_CB=fire; x=` を渡すと桁 0 に完全な偽 marker 行が立ち、`marker_get ITERATE_CB` が主値を `fire` として読み戻せる — **実害のある生存**。教訓: 「N 箇所に修正を適用した」と「N 箇所が pin された」は別の数字。fix の完了条件に「適用箇所数 == pin 箇所数」を含める。

**新しく足した assertion が、その assertion 自身の空振り経路を持つ**。本 cycle の fix が追加した偽造検出 assertion 2 本は `$(... 2>&1 >/dev/null)` で **stderr のみ**を捕捉し `grep -c '^\[CONTEXT\] '` = 0 を assert する。この形は「ERROR が出ない」「ERROR が stdout へ移る」を合格として読む。実測では `>&2` を外すだけで green のままで、しかも `>&2` 除去と scrub 素通しを併せると偽 marker が **stdout** (marker 消費者が grep する側) へ実際に出るのに、その経路を名乗る当の assertion が pass する。直前の commit が別のテストに対して「A count of 0 only means something if the pattern can reach 1」として positive/negative pin を入れたばかりで、その規約が新規 assertion へ伝播していなかった。教訓: counting assertion (`grep -c ... == 0`) を書くときは、同じ commit 内で pattern が 1 に到達しうることを in-suite で示す。

**「fix したか」と「fix を守るテストがあるか」を分けて検証すると、cycle 2 の指摘が cycle 1 の焼き直しにならない**。本 cycle の 5 reviewer は全員が前 cycle 指摘 9 件すべてに FIXED / NOT_FIXED / PARTIAL を明示し、うち複数が「実装は FIXED、pin は PARTIAL」という分解を行った。この分解があると、次 cycle の fix 対象が「もう一度同じ実装を直す」ではなく「テストを足す」に確定する。

**non-blocking 指摘の再掲は reviewer ごとに担当を切る**。cycle 1 で non-blocking 分類された 7 件について、test reviewer は自分の所管 4 件を指摘事項として再掲し、application / security / prompt-engineer は「本 cycle の diff 外」「所管が違う」「churn 回避」として再掲しなかった。同じ finding を全 reviewer が毎 cycle 再掲すると churn tail になるため、この切り分けは有効に働いた。

**コメントが同一ファイル内の契約を誤って引用する**。fix が新設したコメントが「emit の拒否経路は空値を契約違反として扱う」と書いたが、実測では `marker_emit KEY ""` は rc 0 で成功する (同ファイルの Contract 節が「値は空でよい」と明記し、テスト 3 本が固定している)。emit が拒否するのは空の **field 名**であって空の**値**ではない。新しいガードを入れる根拠として書かれた文だったため、存在しない対称性を根拠にしている状態になった。教訓: 「既存の X に合わせた」と書くときは X の実挙動を実測してから書く。

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 4
- LOW-MEDIUM: 1
- LOW: 1
- (blocking: 0、non-blocking / 実測なし: 7、nit-noted: 1)

