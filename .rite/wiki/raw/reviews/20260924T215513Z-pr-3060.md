---
type: reviews
source_ref: "pr-3060"
captured_at: "2026-09-24T21:55:13+00:00"
pr_number: 3060
title: "fix(hooks): レビュー後の base 取り込みを検証済みの 1 経路に揃える（レビュー結果）"
ingested: true
---

## Review Results

- **PR**: #3060
- **Cycle**: 3 (incremental)
- **Reviewed commit**: f36952c0f
- **Assessment**: fix-needed (blocking 5、non-blocking 6)、停滞診断 replan（作業時間）

### Findings Summary
- 引用内のコマンド置換を検出するため中身を再帰的に分割した結果、heredoc 本文（データ）までコマンドとして読み、正当な commit を拒否・作業先を誤認する後退が入った。エージェント標準の `git commit -m "$(cat <<'EOF' ... EOF)"` が本文のアポストロフィだけで wiki gate に止められる。
- 敵対的な形を追う修正の各 cycle で、実運用の commit 形を固定するテストが無かったため、実運用側の後退に気付けなかった。
- 手順書に頼まれていない分岐（未レビュー修正 commit への戻し）を足し、実装が受け付けない手順になった。

### Patterns
- 検査用パーサを変更するときは、実運用の入力（commit メッセージの heredoc 形、skill の fenced bash）をコーパスとして固定し、変更前後の分類を突き合わせる。
- コマンド置換の中身は解析して分類するより、実行されるものは direct でないとして fail-closed にし、heredoc 本文はデータとして読み飛ばす方が面が小さい。

