---
type: reviews
source_ref: "pr-2094"
captured_at: "2026-08-03T00:49:41+00:00"
pr_number: 2094
title: "PR #2094 review results (cycle 2)"
ingested: false
---

## Review Results

- **PR**: #2094 — fix(hooks): 作業メモリの pr_number / loop_count を通常更新経路で carry-forward する
- **Type**: review
- **Reviewed at**: 2026-08-03T09:48:11+09:00
- **Reviewers**: application, error-handling, test, tech-writer, prompt-engineer, security

### Finding Patterns

cycle 2 は blocking 5 件で cycle 1 の 3 件より増えたが、振動ではなく深掘りの結果だった。mutation testing (13 mutant) と injection testing (約 20 vector) を実施した cycle で、前 cycle が到達していなかった層の欠陥が出た。

**1. 列挙を完成させる修正は、列挙という機構ごと撤去しない限り指摘を再生産する**。cycle 1 で docs の「値の出所」列挙に欠けていた 1 ケースを補完したところ、次 cycle で**別の欠けたケース**が実測で出た（parse は通るが非数値 → carry-forward が発火したうえで `null` へ降格、という第 3 の帰結）。列挙の完成とポインタ化を比較して前者を選んだ判断が 1 サイクルで falsify された。**優先順位表の散文化で列挙形を採ると、実装の帰結集合との差分が毎回残る**。SoT へのポインタに差し替えれば失敗クラスごと消える。

**2. 静的 pin テストは「消失」より「drift」を見落とす**。skill markdown の 1 行を静的に照合するテストが、照合 literal に行末の行継続文字を含めていなかった。継続が落ちると env 代入列が非 export のシェル変数に退化し、次行のコマンドが env を 1 つも受け取らずに実行される — しかも `2>/dev/null || true` が握り潰す。**行削除の mutant は捕捉できるが、1 文字削除の mutant は素通りする**。静的 pin は「その行があるか」ではなく「その行が意図した構造で機能するか」まで含めて照合する。

**3. stderr を両方捨てた pipeline では、唯一の診断文が誤った component を名指しできる**。python3 と jq を繋ぐ経路で両方の stderr を `2>/dev/null` に捨てた結果、WARNING が載せる rc は前段のものだけになり、後段が失敗した経路で「前段は成功した」と能動的に否定する表示になった。同じ pipeline を回す姉妹 hook は同じ罠を避けるため stderr capture を実装しており、コメントに理由まで書いてあった。**リポジトリ内に確立済みの doctrine がある場合、新規コードがそこから外れているかを機械的に確認する価値がある**。

**4. 「値なし」sentinel を carry-forward が実値として固定する**。flow-state は PR 未作成を `0` で表すが、carry-forward の `-z` ガードは空 / 非空しか見ないため `0` を実値として採用し恒久化させた。変更前は次の更新で `null` に戻っていた。**sentinel を持つ上流から値を引き継ぐ機構を足すときは、sentinel が「値あり」と判定されないかを確認する**。

**Observed Likelihood ゲートの適用基準（cycle 1・2 で同じ判断を 2 回）**: 「repro があるから blocking」ではない。repro が**入力を手で与えている**場合、その入力を production が生成する経路を grep で示せなければゲート 3 は通らない。cycle 1 で撤回された指摘と cycle 2 で推奨事項へ降格した指摘は同型で、どちらも「消費側の call site」を Likelihood-Evidence に挙げていた。

**同一根本の指摘は Target ファイル側のアンカーに統合する**。2 人のレビュアーが同じ根本原因を別ファイルのアンカーで報告した。片方は Non-Target ファイルで、そちらを blocking で残すと fix に合法な作業が無くなり 1 サイクル空転する。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 3
- LOW-MEDIUM: 2 (blocking) + 1 (non-blocking)
- LOW: 0

