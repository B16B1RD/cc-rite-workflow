---
type: reviews
source_ref: "pr-2111"
captured_at: "2026-08-04T23:54:30+00:00"
pr_number: 2111
title: "PR #2111 review results (cycle 3)"
ingested: true
---

## Review Results

- **PR**: #2111 — refactor(wiki-ingest): ステップ 6 の index 操作をテスト付き helper へ降ろす
- **Type**: review
- **Reviewed at**: 2026-08-05T08:53:11+09:00
- **Reviewers**: application, error-handling, test, code-quality, security, prompt-engineer

### Finding Patterns

cycle 3。blocking 8 件 / non-blocking 3 件 + nit 1 件。cycle 1→2 の修正はいずれも意図どおり機能（application / code-quality が 0 blocking、test が mutation 4 種を独立再現）した一方、**同じ防御の「次の層」が cycle ごとに露出し続けている**。

- **quoted heredoc の残る脱出口は終端子行**: cycle 2 で `"` 経路を塞いだが、値に `WIU_EOF` だけの行が含まれると heredoc が早期終了し残りがコマンド実行される。6 heredoc が同一終端子を共有するためクォート均衡が不要で、脱出後も helper 呼び出しは正常に走るため **rc=0 + 3 marker 揃いの silent success** になる（cycle 2 で追加した「marker が出ない」行では捕捉できない）。シェル層で閉じられないため LLM 側の事前ゲート（値が複数行 or 終端子行と一致するなら実行しない）が要る。
- **ガード固有文言 assert の横展開が 1 cycle 遅れる**: cycle 2 で TC-13/13b に施した「総称 `grep -q 'ERROR'` → ガード固有文言」の是正が、同一クラスの兄弟 TC（TC-14 / TC-22 / TC-22b）へ転記されなかった。TC-22b は自ら「この shape が当該ガード単独の識別力を担う」と宣言しながら総称 assert のため、2 つの ERROR 文言を相互置換する変異が 40/40 green で生存する。**rationale をコメントに書いた時点で同型 TC への転記が必要**という cycle 2 の教訓がそのまま再発した。
- **必須引数ガードの片肺**: TC-15 はヘッダで「必須引数欠落 → exit 2」を掲げながら 4 本中 1 本（`--title`）しか流していない。`--updated` ガード削除が 40/40 green で生存し、削除下では空の更新日セルが rc=0 + `stats_sync=synced` で書かれる silent corruption になる。
- **カウンタの生産者・消費者の非対称**: cycle 2 で完了レポートに `dedup_removed` 合計の消費者を足したが、生産者（ステップ 2.1 のカウンタ表）を足していない。**受け皿を作る修正は producer 側の同時追加を要求する**。
- **無条件性の根拠が縮退で落ちる**: helper docstring は Procedure 3a を「runs every invocation」と invariant 宣言しているのに、呼び出し側の無条件性の根拠（旧散文の「冪等な手順のため実行タイミングの分岐は持たない」）が削除され、skip 決定 Raw Source での扱いが未定義になった。**docstring の invariant は caller 側の呼び出し条件が満たして初めて成立する**。
- **診断出力の中和規約も対称位置を持つ**: exit 2 系は `neutralize_ctrl` 適用済みなのに、awk 内 exit 1 系の malformed-row ERROR は index.md の行内容を制御バイト未中和で stderr へ出す。corrupt-input 診断こそ中和規約が閉じる対象。

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 9
- LOW: 1

