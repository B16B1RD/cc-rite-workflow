---
type: reviews
source_ref: "pr-2044"
captured_at: "2026-07-29T04:23:19+00:00"
pr_number: 2044
title: "PR #2044 review results (cycle 2)"
ingested: false
---

## Review Results

- **PR**: #2044 — fix(iterate): サーキットブレーカー発火を batch/対話とも機械的停止に統一する
- **Type**: review (cycle 2)
- **Reviewed at**: 2026-07-29T13:21:25+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

cycle 1 の修正が新たな欠陥を生んだ形で、パターンは「**状態を早く消しすぎる／invariant を一部の分岐にしか適用しない**」に集約された。

1. **リセットを「記録」より前に置くと、記録前の中断で事実が消える（HIGH）**: 発火時点で cycle counter を 0 に戻す設計は「再実行でループを再開できる」ためだが、リセットが sentinel emit（＝発火の唯一の記録）より前にあるため、その間にターンが終わると発火が完全に無記録になる。同じ set が handoff も消しているので Stop hook は停止を許可し、次回 recover は満額 budget でループを再開する。**修正前は counter が上限のまま残ることでこの窓が自己修復していた** — つまりリセットの導入が、意図せず安全網の自己修復性を外していた。**教訓: 「失敗を記録する」処理と「失敗状態をクリアする」処理が別ステップにあるとき、クリアは記録の後に置く。前に置くと、その隙間での中断がすべて無記録の成功に化ける。**

2. **同型の分岐に invariant が転記されない（Asymmetric Fix Transcription）**: cycle 1 で「marker に載せる counter は永続 counter と一致させる」を reset の成功・失敗の両分岐に入れたが、構造的に同型の increment 分岐には転記しなかった。increment set が失敗すると marker だけが前進し、観測者は counter 停滞と marker ずれを切り分けられない。**教訓: invariant を分岐に足したら、同じ変数を同じ目的で書く他の分岐を grep して全部に適用する。**

3. **人間向け出力ブロック内の placeholder は置換指示の射程外に落ちやすい**: 「以下はリテラル置換する」という指示は直後のテンプレートにしか係らず、条件付きで追加される注意行ブロック内の `{plugin_root}` は射程外に残った。その行は「state 書き込みが既に失敗している状況で提示される唯一の復旧手順」であり、未置換だと実行不能になる。**教訓: LLM 向けテンプレートに条件付きブロックを足したら、そのブロックが既存の「置換する」指示の係り先に含まれるかを確認する。**

### Process Observations

- 実測必須ゲートが 2 cycle 連続で機能した。cycle 2 は 6 件中 2 件が `Verification:` アンカー付き（うち HIGH 1 件は base 版との対比実測）、4 件が非実測で non-blocking。**散文 PR でも stub 環境で埋め込み bash を実走させれば実測は成立する**ことが両 cycle で再現された。
- cycle 1 の non-blocking 指摘（docs/CONFIGURATION.md:646）が cycle 2 で精緻化されて再出現した。ゲートは同じく non-blocking に分類し、記録コメントは update-in-place で 1 件→4 件に更新された。**非実測指摘は消えず、cycle をまたいで精度を上げながら記録に残る**という設計どおりの挙動。
- reviewer の主張の前提を base 側で検証する運用が 2 cycle 連続で誤りを 1 件ずつ捕捉した（cycle 1: 「本 PR が skills 側に最初の該当 site を作った」→ develop に 63 site 既存。cycle 2: tech-writer が自ら 149 件の prevalence を数えて blocking から降格）。**「本 PR が初」「これは新規」という新規性の主張は、base 側の同一 grep で件数を突き合わせてから受け入れる。**

### Severity Distribution
- CRITICAL: 0
- HIGH: 1 (blocking)
- MEDIUM: 1 (blocking) + 3 (non-blocking)
- LOW-MEDIUM: 1 (non-blocking)
- LOW: 0

