---
type: reviews
source_ref: "pr-2094"
captured_at: "2026-08-03T01:26:46+00:00"
pr_number: 2094
title: "PR #2094 review results (cycle 3)"
ingested: false
---

## Review Results

- **PR**: #2094
- **Type**: review
- **Reviewed at**: 2026-08-03T10:25:31+09:00
- **Reviewers**: application, error-handling, test, tech-writer, prompt-engineer, security

### Finding Patterns

blocking 6 件。**うち 3 件が前 cycle の fix 自体の欠陥**で、修正が修正対象面を作る局面に入っている。

**1. ポインタ化は指し先の完全性を先に検証する**。前 cycle で docs の列挙を撤去し実装 header への SoT ポインタへ差し替えたが、header は 2 規則（sentinel 除外・数値バリデーション降格）を述べていなかった。docs 側で「複製するな」と宣言した以上、**header に無い情報は読者にとって存在しない**。さらに header の既存記述が、同 PR が追加したガードによって**事実として偽**になっていた。列挙撤去自体は正しいが、SoT 指定は「指し先が単独で規則を述べ切っているか」を確認してから行う。

**2. 診断の分離は逆向きの誤誘導を作りうる**。前 cycle で「前段の rc だけを載せて後段の失敗を否定する」誤誘導を直したが、後段の rc を `0` で初期化したため、**後段が一度も起動しない経路で「起動して成功した」と断定する**鏡像の誤りが生まれた。rc 変数は「未起動」を表す非数値 sentinel で初期化する。

**3. 既定値と一致する値を assert するテストは kill power を持たない**。`loop_count: 0` の照合は、既定値が `0` である以上 carry-forward の有無を区別できない。mutation で確認したところ carry-forward を丸ごと削除しても Green のまま。**アサーション名が「直接確認」と称していると、読み手は守られていると誤認する**。出力から原理的に判別できない性質は、強化ではなく relabel が正しい対処。

**4. seed 値と同じ値を assert するブロックは、検証対象の実行が no-op でも通る**。seed が env override で書いた値と、更新後に照合する値が同一だと、2 回目の更新を丸ごと削除しても全アサーションが Green。**各ブロックに前提確認か body 差し替えの照合を 1 本置く**（本ファイルの他 7 ブロックはすべて持っていた）。

**5. 新しい idiom を導入したら、その idiom が伝播しうる先を pin する**。本 PR は「env override の有無で発火を止める」ガードを新規導入し、その 17 行下に同じ 2 変数を無条件上書きするブロックが残った。**「2 つのブロックを揃える」方向の後続編集がそのまま契約違反を作る形**で、それを捕捉するテストが無い。前 cycle は revert test fail として推奨に留めたが、**ドリフトの形を持ち込んだのが当該 PR 自身**であれば diff 内の根拠として成立する。

**Verification アンカーに raw pipe を書くとゲートが未判定に倒す**。`grep -n '|| true'` のような repro をアンカーに書くと検出 regex に match せず、`measured` が確定しないまま blocking に据え置かれる（降格ではない）。パイプは `¦` で代替表記する。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 4
- LOW-MEDIUM: 2
- LOW: 0

