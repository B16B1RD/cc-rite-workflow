---
type: reviews
source_ref: "pr-2094"
captured_at: "2026-08-02T18:04:59+00:00"
pr_number: 2094
title: "PR #2094 review results (cycle 4)"
ingested: true
---

## Review Results

- **PR**: #2094
- **Type**: review
- **Reviewed at**: 2026-08-03 (cycle 4)
- **Reviewers**: code-quality, error-handling, test, security, tech-writer, prompt-engineer

### Finding Patterns

- **「列挙をやめる」だけでは足りず、基準語そのものが実コードの集合を選べるかを検証する必要がある**: cycle 3 で「call site が 2>/dev/null で捨てるから届かない」という形式ベースの説明を撤去し「直接 source する caller」を基準にしたが、その基準で選んだ集合が実際の grep 結果 (3 件) と食い違った (2 件しか挙げていない)。列挙を持つ記述は、列挙を消しても**基準側へ様式が移動する**。基準を書くときは「その基準で grep したら何件出るか」を実際に走らせて確認する。
- **規則とその反例を同じ diff で導入しない**: 「caller を列挙して説明しない」という規則をコメントに書きながら、同一ファイル内の 2 箇所で caller を列挙していた。規則を書いたら、その規則をファイル全体へ適用しきってから commit する。適用しきれないなら規則の scope を明示する (「本コメントでは」等)。
- **観測性の主張は発火条件の射程と一致させる**: 「WARNING で観測性を確保する」と書いたが、発火条件が `.data` の要素数だったため、個別 key だけが欠落する系統は無警告のまま値が巻き戻った。主張の範囲は発火条件の範囲で書く。
- **診断値を捕捉したなら、それが最も意味を持つ分岐で出す**: parse の corrupt verdict を変数に捕捉しながら、印字しているのは parse が成功している側の分岐だけだった。結果として「rc=0」と書かれた縮退 WARNING が出る一方、本当に corrupt なファイルからの値伝播は無警告になっていた。
- **新機能が「片方だけ渡す」構成を作ったら、その構成を直接テストする**: 2 つの独立ガードを持つ実装で、テストが「両方渡す」と「両方渡さない」しか通っていなかった。本 PR 自身が追加した seed 行が「片方だけ渡す」構成であり、ガードの取り違えが無検知で通る状態だった。

### Severity Distribution
- CRITICAL: 1
- HIGH: 2
- MEDIUM: 3
- LOW: 0

