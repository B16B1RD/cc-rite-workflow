---
type: reviews
source_ref: "pr-529"
captured_at: "2026-04-15T09:40:07+00:00"
pr_number: 529
title: "PR #529 review results"
ingested: true
---

## Review Results

- **PR**: #529 — fix(wiki): raw commit 経路を shell script 化して orchestrator 依存を断つ
- **Type**: review
- **Reviewed at**: 2026-04-15T18:39+09:00
- **Reviewers**: tech-writer, prompt-engineer, devops, security, error-handling, code-quality

### Finding Patterns

- **cleanup trap rollback-safety**: signal handler での exit code loss (rc=0 による rollback skip) と checkout-back 失敗時の無条件 stash pop が 2 件検出。単一プロセス完結を謳う shell script の rollback 契約は signal 経路 + 順序依存の両方で明示的な guard が必要。
- **対称性違反 (Phase X.X.W.2 vs trigger 経路)**: 同一ファイル内で trigger 経路には fallback_sentinel と emit_err trap があるのに、本 PR で追加した commit 経路にはいずれも欠落。既存パターンからの非対称は silent failure の兆候。
- **Exit code semantic mismatch**: wiki-ingest-commit.sh は exit 2 を legitimate skip として定義しているのに、caller 3 ファイルは exit != 0 を一律 failure として sentinel emit。caller 側で exit code の意味論を保持する case 文が必要。
- **YAML パースロジック 3 箇所目の再実装**: trigger.sh の同期警告コメントがペアスクリプト commit.sh を認識していない drift リスク。
- **`$wiki_branch` option injection**: YAML から読み込んだ branch name に ref-format validation も -- separator もなく、ダッシュ始まりの値を与えられた場合の git option injection 経路が本 PR で新設される。

### Severity Distribution
- CRITICAL: 0
- HIGH: 4
- MEDIUM: 4
- LOW: 4

### Key patterns (project-specific experiential knowledge)
- **単一プロセス完結を謳う shell script でも signal 経路の exit code 損失で rollback が silent skip される**: `trap 'trap - EXIT; cleanup' INT TERM HUP` だけでは不十分。signal 専用 handler で明示的な POSIX exit code (130/143/129) を渡す canonical pattern が必須。
- **ペアスクリプト化する際は同期警告コメントを必ず更新**: 片方の script に「keep in sync with X」コメントがある場合、新たに追加する 3 番目の script は sync list に自身を追加する責務がある。
- **Phase X.X.W.2 の trigger 経路と commit 経路は同一ファイル内で対称に書く**: trigger にある fallback_sentinel / emit_err trap が commit 経路に欠落していたら silent failure の兆候として lint 対象化できる。

