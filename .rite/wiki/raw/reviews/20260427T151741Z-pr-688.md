---
type: reviews
source_ref: "pr-688"
captured_at: "2026-04-27T15:17:41+00:00"
pr_number: 688
title: "PR #688 review results (cycle 35)"
ingested: true
---

## Review Results

- **PR**: #688 — fix(workflow): #687 multi-state-aware flow-state read helper
- **Type**: review
- **Reviewed at**: 2026-04-28T00:12:17+09:00
- **Reviewers**: code-quality, error-handling, test, prompt-engineer, tech-writer, security

### Finding Patterns

**Dominant pattern**: `2>&1` stderr merge による silent sentinel suppression (CRITICAL × 2)。Issue #687 の core deliverable である `legacy_state_corrupt` / `cross_session_takeover_refused` workflow incident sentinel が、reader (state-read.sh:119) / writer (flow-state-update.sh:149) 両方で `2>&1` により helper の stderr 出力が classification 文字列に混入し、`case "$classification" in corrupt:*) ...` arm が defensive `*)` 経路に落ちることで silent suppression される対称バグ。同 PR が「silent failure を sentinel で観測可能化」を目的としながら、その deliverable 自体が silent failure する self-defeating 構造。

**Secondary pattern**: bash の `if ! cmd; then rc=$?` anti-pattern (HIGH)。本 PR の同 file 内 `pr/review.md:3387` で「rc=0 を常に取得する既知の落とし穴」として明示警告されているにもかかわらず、caller migration 全 6 site (start.md 4 + implement.md 1 + review.md 1) で当該 anti-pattern が採用された。同一 PR 内で正しい pattern (`work-memory-update.sh:97-104` の `if cmd; then :; else rc=$?; fi`) が共存しているため、Asymmetric Fix Transcription (Wiki 経験則) の典型例。

**Tertiary pattern**: helper の output contract 曖昧性が writer/reader 双方の同時バグを誘発。`_resolve-cross-session-guard.sh` が「stdout: classification token / stderr: 診断ログ」の混合 output を採用したため、caller が `2>&1` か `2>/dev/null` のどちらを選ぶかという contract 曖昧性が生じ、両 caller が同じ誤った選択 (`2>&1`) をした。

**Coverage gap**: `corrupt:*` 分類経路の TC が新規 4 test file (1470 行) のいずれにも不在。F-01/F-02/F-03 を捕捉する gate がなく、cycle 28-34 の review-fix loop でこれらのバグが残存した構造的盲点。

**Pre-existing arithmetic injection (調査推奨)**: `flow-state-update.sh:379, 453` の `[[ "$PARENT_ISSUE" -eq 0 ]]` arithmetic context で command substitution が発火可能 (security reviewer 実機検証済)。本 PR では未変更 pre-existing code のため findings 除外。

### Severity Distribution
- CRITICAL: 2
- HIGH: 2
- MEDIUM: 4
- LOW: 5

