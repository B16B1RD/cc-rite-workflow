---
type: reviews
source_ref: "pr-2030"
captured_at: "2026-07-26T16:03:31+00:00"
pr_number: 2030
title: "PR #2030 review results"
ingested: true
---

## Review Results

- **PR**: #2030 — feat(pr-review): blocking 判定に実測必須 CONFIRMED ゲートを追加し非実測指摘を PR コメント記録に落とす
- **Type**: review
- **Reviewed at**: 2026-07-27T01:02:34+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling, security, test

### Finding Patterns

本サイクルで観測された、rite 自身のワークフロー定義を変更する PR に固有の指摘パターン:

1. **SoT 同期漏れ (authoring 側 / detection 側)**: 制約を detection 側の reference にだけ書き、reviewer が実際に読む authoring 側 SoT (`_reviewer-base.md`) に伝播しないと、機構が silent に空振りする。今回は `Verification:` アンカーの regex がセル境界束縛のため raw パイプを含む repro を no-match にするという制約が detection 側にしか書かれておらず、canonical な再現イディオム (`printf ... | jq`) がそのまま降格される経路が開いていた。同期規約を自分で書いた PR がその規約に違反していた。

2. **marker 検索の部分一致 + destructive write**: PR コメントを marker 文字列の `contains` で検索し `last` を PATCH する設計は、marker を引用しただけのコメント (自分のレビュー結果コメント / 人間の Quote reply) を誤選択して上書き破壊する。write 側で「marker は 1 行目」を invariant として明文化していても、検索側が `startswith` にしなければその保証は使われない。本 repo は Raw JSON 抽出について同型の自己参照捕捉を既に scope 限定 awk で対策済みで、新規経路だけが非対称に残った。

3. **`2>/dev/null` による placeholder residue の握り潰し**: `[ "{placeholder}" -gt 0 ] 2>/dev/null` は置換漏れ時の rc=2 を偽に畳み、正常系と同一の marker を emit して silent に skip する。既存の helper 群が numeric gate を machine-enforced で持つのに、SKILL.md 直書きの新規 block だけが prose 依存になりやすい。

4. **降格ゲートの WARNING が実行不能**: bash を実行しない推論ステップ (5.3.0.M) に「WARNING を stderr に出す」とだけ書いても構造的に達成できない。明示 `echo >&2` step と `[CONTEXT]` marker、reason 語彙の登録がセットでないと silent auto-correct になる。

5. **静的 parity は drift を pin するが semantics を pin しない**: 述語文字列を anchor にした 3-site parity テストは、3 site を一貫改修する mutation では素通りする (実測: 112 pass のまま正準形状が fallback に落ちた)。機能の主経路が実際に書き出す正準 JSON 形状に受理側 fixture を置くことでしか semantics は守れない。

6. **新規 emit marker の reason 表未登録**: `Pattern-2 documented-union input` を自称する reason 表に新規 marker を追加し忘れると、失敗時の reason を SoT から逆引きできず provenance が失われる。姉妹ファイル (fix/SKILL.md) では登録済みで片側だけ漏れる非対称が起きやすい。

### Severity Distribution
- CRITICAL: 0
- HIGH: 3
- MEDIUM: 9
- LOW: 1 (scope=nit-noted、blocking countdown 対象外)

