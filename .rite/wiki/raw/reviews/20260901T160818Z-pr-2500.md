---
type: reviews
source_ref: "pr-2500"
captured_at: "2026-09-01T16:08:18+00:00"
pr_number: 2500
title: "PR #2500 review results"
ingested: false
---

## Review Results

- **PR**: #2500 — feat(issue-cancel): /rite:issue-cancel を追加し Issue の中止経路を通す
- **Type**: review
- **Reviewed at**: 2026-09-02T01:05:57+09:00
- **Reviewers**: prompt-engineer, tech-writer, test, error-handling, code-quality, application

### Finding Patterns

新規スキル定義 (SKILL.md) の fenced bash block に集中した 2 つの CRITICAL が、いずれも「ガードが書かれているのに構造的に発火しない」形だった。

1. `pr_candidates=$(gh api ... | sort -un) || _tl_rc=$?` — pipeline の rc は最終段 `sort` のもので、埋め込み bash block は Bash tool 呼び出しごとに fresh shell (`pipefail` OFF) で起動するため上流 `gh api` の失敗が伝播しない。直後の fail-loud `exit 1` が dead code 化し、「取得失敗」が「0 件 = 該当なし」へ畳まれた。rc を消費するコマンドはパイプから外して capture-first にするか、block 冒頭で `set -o pipefail` を宣言する必要がある。同 block の 1 行下に「0 件と取得失敗を同じ値へ畳まない」と自己宣言があったのに実装が破っていた点が、散文の意図と bash の実挙動の乖離として典型的。

2. `select(.pull_request != null)` — プラグイン自身の PreToolUse ガード (`pre-tool-bash-guard.sh` Pattern 3 `jq-not-equal-null`) が `!= null` を含む Bash 呼び出しを無条件 deny するため、記述どおりには一度も実行できない。`references/gh-cli-patterns.md` が同構文を PROHIBITED と明記済みで、truthiness (`select(.pull_request)`) への置換が canonical。**プラグイン自身の hook が自プラグインの skill 定義をブロックする**というドッグフーディング固有の失敗クラスで、契約テストが「行の形」だけを grep していたため 68 PASS のまま素通りした。

3. 契約テスト側では「assertion のラベルが名乗る挙動を pattern が検証していない」欠陥が 3 件 (HIGH)。素の語 (`停止する` / `github.projects.enabled` / コメント冒頭リテラル) を pattern にすると、同セクション内の無関係な文や第 1 文だけで充足してしまい、AC の `Then` を反転させても緑のまま通る。mutation 実験 (隔離 worktree で SKILL.md 側を書き換えてスイート実行) で 31 本中 7 本が生存し、うち 3 本が AC 直結だった。判定表の行そのもの (行頭テーブル区切り + 帰結) へアンカーし直すのが有効な対策。

4. helper 委譲パターンでは「helper が非ブロッキング契約で rc=0 を返すのに、呼び出し側が rc だけで失敗を判定する」クラスが 2 件。marker family (`REVIEW_CLEANUP_PARTIAL_FAILURE` / `CLEANUP_WT=unknown`) を受けない新規 caller は、hardened sibling (`skills/cleanup/SKILL.md`) が既に持っている判定行を継承し損ねる。新規 caller を書くときは sibling の marker 判定表を必ず突き合わせること。

5. `unknown` (分類不能) を `none` (該当なし) へ畳む routing が 4 レビュアーから独立に指摘された。marker が特定フィールドを持たないケースを「空 = 無し」と読むと、判定表の `unknown` 行が到達不能なデッド行になり、未検証の残置が「なし」として報告される。

### Severity Distribution
- CRITICAL: 2
- HIGH: 3
- MEDIUM: 1
- LOW: 0

