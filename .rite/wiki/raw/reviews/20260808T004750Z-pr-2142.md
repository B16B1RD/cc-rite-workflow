## Review Results

- **PR**: #2142 — feat(pr-review,issue-implement): 儀式コストを Issue の Complexity に比例させる XS/S レーンを導入する
- **Type**: review
- **Reviewed at**: 2026-08-08T09:47:05+09:00 (cycle 2, 差分スコープ)
- **Reviewers**: application, code-quality, error-handling, prompt-engineer, test, security

### Finding Patterns

cycle 1 の指摘 6 件を修正した diff (65 行) に対する cycle 2 レビュー。**新規指摘 16 件のうち 5 件が、cycle 1 の修正で新設したたった 1 行に集中した**。以下はその構造から得られた経験則。

1. **「診断を足す」修正は、足した診断自体が新しいレビュー面になる — しかも元の指摘より高い密度で**: cycle 1 で「fail-safe の reason が原因を誤帰属する」と指摘され、その是正として「宣言らしき行を名指しする WARNING」を 1 行足した。結果、その 1 行に対して cycle 2 で (a) 述語が両方向で誤る、(b) 外部入力を無中和で出力する、(c) `||` が SIGPIPE で捕捉済みの値を破棄する、(d) その分岐に assert がゼロ、の 4 件が付いた。**6 reviewer 全員が (a) を独立検出**した。教訓は「診断の追加は最小の変更に見えて、外部入力・エラー経路・テスト網羅の 3 領域を同時に開く」こと。診断を足すときは最初から (述語の形 / 中和 / rc の扱い / 双方向 assert) の 4 点をセットで設計する。

2. **キーワード grep で「〜らしき行」を判定すると、両方向で外す**: `grep -E 'Complexity|複雑度'` のような裸の語検索は、(i) case-sensitive のため lowercase key を落とし、(ii) 行の形を問わないため散文・表セルの単なる言及を拾う。しかも**この 2 つは同じ根 (形状判定ではなくキーワード判定) から出る**ので、片方だけ直すともう片方が悪化する (`-i` だけ足すと誤検出が英小文字の散文まで広がる)。「宣言行らしさ」を判定するなら**行頭 anchor + キー + 区切り記号**の形にする。実測: 行頭 anchor + `[:：]` 要求 + `-i` で、lowercase key / 全角コロン / リスト項目化 / 太字なし / 見出しの 5 形で発火し散文で沈黙する。

3. **`var=$(pipeline) || var=""` は `set -e` 非設定下では純粋なリスク**: 利得ゼロ (どのみち止まらない) で、`pipefail` × 早期終了フィルタ (`grep -m1` / `head -1` 相当) × 上流 `printf` の SIGPIPE(141) を「不一致(1)」と融合し、**捕捉に成功した値を破棄する**。閾値は Linux 既定の pipe buffer 64KB。同一ファイル内の sibling が `|| var=""` を付けずに書いているなら、その 1 行だけが house pattern から外れている合図。

4. **静的 denylist probe は「守る対象」ではなく「探し方」で壊れる**: GNU 拡張の再混入を止めるために `grep -n 'sed -n' file` を haystack にしたところ、(i) 式を継続行へ折り返すだけで走査対象から外れ、(ii) 探し方が変わって 0 件になると haystack が空になり assert が**無言で vacuous pass** する。probe を書くときは haystack の非空を precondition として assert し、走査範囲は「実装の形」ではなく「コメント以外の本文全体」のように**壊れにくい軸**で切る。denylist の語彙も、宣言した規範 (POSIX BRE のみ) に対して網羅する — `\b` `\|` だけ挙げて `\+` `\?` を漏らすと、最も自然な書き換え形が素通りする。

5. **正規表現を緩めると、同じ欠陥が入力形式によって別の reason に分裂する**: 抽出式を「トークン全体を貪欲に取る」形へ単純化したことで、記法 1 の未展開 placeholder は `absent` のまま、記法 2 の同じ placeholder は先頭記号が読み飛ばされて `invalid` になった。reason が効果計測の分母を数える観測値である場合、**同一の記入漏れが 2 つの reason へ分裂すると集計の意味が壊れる**。正規表現の緩和は「受理集合が広がる」だけでなく「分類の境界が動く」ことを疑う。

6. **前提の訂正は、その前提が書かれた全 site を数えてから行う**: 「この値は必ずある」という偽の前提を実測 (60 件中 23 件が非該当) で訂正したが、訂正できたのは 5 コピー中 2 コピーだった。残る 3 つ (実行手順書 / 英語 docs / 同一ファイルの 3 行下) がそのまま残り、3 reviewer が別々に検出した。**訂正の前に `grep` で全 site を数え、修正 diff にその数を明記する**と漏れが可視化される。

7. **CRITICAL を直しても「再発検出 assert」を足さないと、同じ欠陥に戻せる**: cycle 1 の CRITICAL (GFM の表が blockquote で分断され AND 条件が 1 本消える) を本文側で直したが、再発を止める assert を足さなかったため、**欠陥状態そのものを再現しても contract suite は green のまま**だった。CRITICAL 修正には「その欠陥状態で落ちる assert」を同一 commit で足す。

### Severity Distribution
- CRITICAL: 1
- HIGH: 4
- MEDIUM: 10
- LOW-MEDIUM: 1
- LOW: 0

(blocking 6 件 / 実測なし non-blocking 10 件。実測必須ゲート適用後。cycle 1 は blocking 6 / non-blocking 9 で、per-cycle blocking の推移は 6 → 6)
