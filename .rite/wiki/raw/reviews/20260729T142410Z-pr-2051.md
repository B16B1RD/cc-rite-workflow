---
type: reviews
source_ref: "pr-2051"
captured_at: "2026-07-29T14:24:10+00:00"
pr_number: 2051
title: "PR #2051 review results"
ingested: false
---

## Review Results

- **PR**: #2051 — fix(skills): fenced bash の位置パラメータ展開で YAML パーサが全キー空を返す経路を塞ぐ
- **Type**: review
- **Reviewed at**: 2026-07-29
- **Reviewers**: prompt-engineer, code-quality, error-handling, test, tech-writer, security

### Finding Patterns

本 PR は「skill 本文の位置パラメータが起動引数へ展開されて awk が壊れ、空値が opt-out default に吸収されて実失敗が正常スキップとして報告される」defect を塞ぐ修正だったが、6 reviewer が独立に **同型の欠陥が修正コード側に再出現している** ことを検出した。

1. **検出器自身が「検出できなかったこと」を検出できない**（2 reviewer が独立指摘）: 新規静的チェック `dollar-zero-check.sh` は fence 不整合 / awk fatal / target 不在をすべて「findings 0 件 + rc=0」に畳む。唯一の呼び出し経路 `/rite:lint` Phase 3.5 は rc=0 を success と扱い、WARNING を表示する経路（appendix は warning/error 時のみ、summary は E2E で skip）が無いため、ガードが黙って無検査になる。塞いだはずの defect class と構造が同一。加えて awk の fatal exit code (2) を unbalanced sentinel に流用したため両者が区別できない。
2. **DoD 検証スクリプトの空集合 silent pass**: `fix-reason-coverage-check.sh` は emit 側の抽出が 0 件でも `comm` の差分が空になり rc=0 を返す。「全部文書化されている」と「1 件も抽出できなかった」が同じ成功として返る。
3. **恒真アサーション**: 新規テストの AC-5（実スクリプト除外）検証は、fixture の参照が fence の外にあるため実装がどう壊れても pass する。除外契約は事実上未検証だった。
4. **参照関係の列挙が同期されない欠陥クラスが 6 箇所**（3 reviewer が独立指摘）: シンボルを削除したのに、それを列挙する側（helper header の caller リスト、hook の同期インベントリ comment、SPEC.md の構造ツリー、reason 表、skill 内の相互参照、設計差異ブロック）が未更新。「唯一の同期一覧」を自称するファイルですら箇条書きだけ更新し設計差異ブロックが取り残された。
5. **委譲で失われた診断粒度**: helper 化により最も起こりやすい失敗が「helper 不在 = rc=127」に変わったのに、WARNING の原因ラベルと reason 表の説明は inline awk 時代のままの箇所が残った（同一 PR 内で更新した箇所と非対称）。
6. **一時ファイルの atomic 保証**: mktemp が生成した名前に固定 suffix を足した派生パスへ書き込む形は、mktemp の O_CREAT|O_EXCL 保証を失う。security reviewer が隔離環境で symlink 経由の truncate を実測した。兄弟スクリプト 2 本は派生パスを作らない書き方をしている。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 14
- LOW-MEDIUM: 3
- LOW: 3

