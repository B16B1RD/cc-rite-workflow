---
type: reviews
source_ref: "pr-586-cycle5"
captured_at: "2026-04-19T03:42:37+00:00"
pr_number: 586
title: "PR #586 review results (cycle 5)"
ingested: false
---

## Review Results

- **PR**: #586 — feat(commands): /rite:wiki:init で same_branch 戦略向け .gitignore negation を自動注入
- **Type**: review
- **Reviewed at**: 2026-04-19T12:41:28+09:00
- **Reviewers**: code-quality-reviewer, prompt-engineer-reviewer, error-handling-reviewer
- **Cycle**: 5 (re-review after cycle 4 fix)

### Finding Patterns

**パターン: cycle 4 fix で「canonical 一字一句揃える」と謳ったコメントと実装の整合性検証**

cycle 4 で「canonical reference (gitignore-health-check.sh L270-277) と一字一句揃える (canonical drift 防止)」とコメントに明記したが、実コードは canonical の if-wrapper 構造 (`if cmd; then rc=0; else rc=$?; fi`) を採用せず、簡略な `var=$(cmd); rc=$?` パターンに留まった。この乖離は cycle 5 で MEDIUM finding として検出された (F-01)。

教訓: コメントに「canonical 一字一句同期」を宣言する場合、実コードと canonical の **構造全体** (rc capture スタイル / 制御フロー) を含めて完全一致させること。stderr 退避部分のみ揃えて「一字一句」を主張するのは silent over-claim となり、後続レビューで drift として検出される。

**パターン: 既存ファイルへ大量行を挿入した際のコメント内行番号参照 drift**

cycle 4 fix で Phase 1.3 ブロック (約 235 行) を Phase 1.2 と Phase 2 の間に挿入した結果、cycle 4 fix の F-02 修正コメント自身が「同一ファイル内 L555」を参照していたが、実際の対象行は L563 にずれた (8 行 drift)。

教訓: 同一ファイル内の他箇所を参照するコメントを書く場合、(a) 行番号ではなく Phase 番号 / heading anchor / function 名を参照する (drift 耐性が高い)、(b) PR 内で大量行挿入を伴う場合は最終 commit 後に行番号参照を見直す lint を組み込む。

**パターン: 同一 state を 2 箇所で記述する際の動作矛盾検出**

Phase 1.3.1 の `already_negated` state は Skip 条件記述 (L119) で「silent skip」と明記される一方、LLM 分岐テーブル (L173) では「✅ メッセージを表示して Phase 2 へ」と指定。同一 state の動作が文書内で矛盾しており、LLM 実行時の動作が不定 (F-03)。

教訓: state machine を 2 箇所 (実装ロジック / LLM 分岐テーブル) で記述する場合、同一 state の動作 (silent / verbose / 表示メッセージ) を文字単位で同期させる。drift 防止 lint で 2 箇所の整合性を機械検証する仕組みが望ましい。

**パターン: 検出 grep と Edit old_string の strictness 非対称**

Phase 1.3.1 anchor 存在 check (L142 `grep -qF prefix-only`) と Phase 1.3.3 Edit ツール `old_string` (L219 `prefix + suffix exact match`) で文字列 strictness が異なる。consumer が anchor の suffix 部分を独自編集している場合、anchor 存在 check は通過するが Edit が `old_string not found` で hard fail する (F-04)。

教訓: 「存在 check → exact 操作」の 2 段階処理を書く場合、両者で**同一の文字列 strictness** を採用する。検出側を loose に書くと「検出 OK → 操作 fail」の中途半端な状態に陥り、cycle 4 fix の `anchor_absent` 救済経路すら通らない silent failure を生む。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 3
- LOW: 1

