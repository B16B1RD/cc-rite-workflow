---
type: reviews
source_ref: "pr-2124"
captured_at: "2026-08-06T10:31:16+00:00"
pr_number: 2124
title: "PR #2124 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2124 — refactor(hooks): tempfile ライフサイクルを lib 化し残余 2 種に lint 検出器を足す (cycle 2)
- **Type**: review
- **Reviewed at**: 2026-08-06T19:29:50+09:00
- **Reviewers**: code-quality, error-handling, test, security, prompt-engineer, tech-writer

### Finding Patterns

cycle 2 で唯一の blocking 指摘は「guard 自身が持つ、guard が戒めている欠陥」だった。
検出器のヘッダは「走査しなかったものを『見つからなかった』へ畳むのは guard 内で同じ
欠陥クラスを再現する」と宣言していたが、ハンドル登録側が最初の 1 個で return して
おり、1 論理行が複数の tempfile を作る形では 2 個目以降が未登録 = その派生パスが
恒久的に silent clean になっていた。しかも走査対象に実在の該当行があった
(`lib/git-status-filtered.sh:45` の `a=$(mktemp) && b=$(mktemp)`)。**宣言した契約を
自分自身の実装で破っていないかは、宣言文の grep ではなく実データでの反証で確かめる**
— security reviewer は走査対象ツリーから同形の行を探して existing_call_site として
提示し、fixture で再現した。

もう 1 つの観測: cycle 1 で non-blocking だった指摘 (SoT 宣言が派生先を 1 箇所しか
名指ししない) が cycle 2 で同一 file:line のまま再出現した。実測必須ゲートは
「修正しないと再指摘され続ける」ことを許容する設計なので、これは異常ではなく想定
どおりの挙動。ただし cycle を跨いで同じ指摘が積み上がるため、non-blocking のまま
放置するか accept するかの判断は人間側に残る。

reviewer の手法として有効だったのは mutation testing (test reviewer が 73 変異中 57
kill を実測) と、走査対象ツリー内での existing_call_site 探索 (security reviewer)。
どちらも「テストが green である」ことと「テストが守っている」ことを分離して測る。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0

