---
type: reviews
source_ref: "pr-2142"
captured_at: "2026-08-08T03:17:04+00:00"
pr_number: 2142
title: "PR #2142 review results (cycle 5)"
ingested: true
---

## Review Results

- **PR**: #2142 — feat(pr-review,issue-implement): 儀式コストを Issue の Complexity に比例させる XS/S レーンを導入する
- **Type**: review
- **Reviewed at**: 2026-08-08T12:14:47+09:00 (cycle 5)
- **Reviewers**: application, code-quality, error-handling, prompt-engineer, security, test

### Finding Patterns

前 cycle の 9 件が全件解消し、blocking が 3 → 2 へ落ちた cycle。残った指摘の質が変わっている —
「実装の欠陥」ではなく「**新しく足した規則を守る番人の穴**」と「**説明文の帰属誤り**」に集中した。

1. **新しい述語を足したら、その述語が守っている入力クラスの fixture が既存にあるか数える**: 抽出側へ
   節境界 `f && /^#/{exit}` を足したが、テストの記法 2 fixture 8 本はすべて `## 複雑度` が 1 行目か
   直前が散文行のみで、**見出しに先行する別の見出しを持つ body が 1 本も無かった**。そのため
   `f &&` 腕を落とす変異が両スイート green で生存し、`## 1. Goal` が先行する**実運用形状のほぼ全域**で
   軽量レーンが発動しなくなる欠陥を素通しする。新規則の pin は「規則を消す」変異だけでなく
   「**連言の片腕を消す**」変異でも検証する。

2. **退避先を足したら、その退避が合流する既存メッセージの原因列挙も見る**: 空セクションで見出しへ
   退避する分岐を足した結果、「lowercase key / 全角コロン / リスト項目化 / 未展開 placeholder 等の
   崩れが疑われます」という既存の列挙へ、**列挙のどれにも当たらない原因**（節が空）が合流した。
   診断を読んだ運用者は存在しない記法崩れを探す。分岐の追加は文言の追加とセットで考える。

3. **指示語（前者 / 後者）で対比を圧縮した説明文は、4 cycle 連続で誤帰属を生んだ**: 「退避しないと
   … 後者は無関係な次節見出しを提示する」は、`後者` がどの語を指しても実測と逆になっていた。
   同クラス（コメントが実測を騙る）は cycle 3 → 4 → 5 と連続再発しており、しかも**同種を潰した
   commit の中で新しい個体が生まれている**。対比構造は指示語で圧縮せず、両方を明示的に書く。

4. **レビュアーの提案パッチを却下したら、却下根拠を当の提案者に検証させる**: cycle 4 で却下した
   error-handling の案について、同 reviewer 自身が却下根拠を独立実測し「却下は事実、採用形が正しい」
   と確認した（awk の `exit` が END を実行するため二重 print になり、正常系の WARNING まで無音化する
   ことを gawk / mawk 両実装で再現）。**却下は一方的な宣言で終わらせず、次 cycle の検証項目にする。**

5. **reviewer 間で判定が割れたら、統合側が実測して裁定する**: `--keep-newline` の 1 語削除 mutant に
   ついて security は「既定モードへの格上げは中和が**強まる**方向だから pin 不要」、code-quality と
   prompt-engineer は「行構造が壊れる」と対立した。実測すると 3 行の stderr が 1 行へ潰れ、直後の
   `[CONTEXT]` marker が snippet 末尾へ連結して**行頭を失う**。security の反論はバイト衛生については
   正しく、別軸の帰結を扱っていなかった。**どちらかを採るのではなく、両者が見ていた軸を明示する。**

6. **`grep -qE` に渡すパターンの裸の `|` は交替演算子 — `^` 単独腕が全行に一致して常に PASS する**:
   前 cycle の fix が自ら mutation で検出・是正したが、その sweep 主張を 3 reviewer が独立に再走査した。
   `assert_grep` だけでなく **`assert_not_grep` も同じ `grep -qE` を使う**ため同一ハザードを持つ
   （申し送りの sweep は 2 関数のみを対象にしていた）。3 関数 949 call site を走査して該当 0 件は
   確認できたが、**sweep の網羅性は「対象関数の列挙」で決まる**。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 3
- LOW-MEDIUM: 3
- LOW: 0

(blocking 2 件 / 実測なし non-blocking 5 件。実測必須ゲート適用後。推移 6 → 6 → 6 → 3 → 2)

