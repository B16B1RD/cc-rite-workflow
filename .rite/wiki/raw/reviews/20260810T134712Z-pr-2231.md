---
type: reviews
source_ref: "pr-2231"
captured_at: "2026-08-10T13:47:12+00:00"
pr_number: 2231
title: "PR #2231 review results"
ingested: false
---

## Review Results

- **PR**: #2231 — fix(pr-review): reviewer 並列起動の直列化を spawn 時刻の機械記録で検出する
- **Type**: review
- **Reviewed at**: 2026-08-10T22:46:10+09:00
- **Reviewers**: prompt-engineer, application, tech-writer

### Finding Patterns

本 cycle で最も強い信号は「**silent failure を検出する道具自身が silent failure を持つ**」パターン。新設した spawn spread 検出 helper が、jq のパイプ先で `2>/dev/null` + rc 未検査 + `pipefail` 不在という 3 点セットにより、element 単位の parse 失敗を捨てて切り詰めた stream で判定し、`serialized=false` を完全無音で書き込む経路を持っていた。helper docstring は「判定不能の 3 経路。いずれも silent skip せず理由付きで表面化する」と宣言していたが、その母集団の外側に第 4 の経路が成立していた。**宣言的な網羅性の主張（「N 経路すべてを表面化する」）は、その N を数え上げた根拠が実装内に無いと破れる**。

第 2 のパターンは「**排他分岐が仕様の併記要求を落とす**」。「直列化検出」と「起動時刻の欠落あり」を `if / elif` で書いたため、両方が同時に成立する cycle で後者の WARNING が消えた。仕様側は「判定は実施し、欠落を併記」と明記していたが、実装は 2 つの状態を排他として読んだ。**仕様が「A かつ B のとき両方報告」を要求する箇所は、実装で `elif` を使った時点で違反する**。

第 3 のパターンは「**escape clause が構造的に発火しない**」。「ファイル不在なら省略する」という条件が、そのファイルのパスに cycle 識別子を含まないため cycle 2 以降で永久に偽になる。同じスキル内の別ステップは既に「パスに cycle 識別子を含めるのは必須」を house rule として明文化していたが、新設ステップはそれに従っていなかった。**セッション内不変の TMPDIR 配下に cycle スコープの中間ファイルを置くときは、パスに cycle 識別子を含めないと「不在」を検出条件にできない**。

加えて、本 cycle は**レビュー実行そのものが直列化した**（reviewer を 1 名ずつ別メッセージで発行し spawn spread 856s）。皮肉にも本 PR が追加した検出器がそれを正しく捕捉し、機能の実地検証になった。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 7
- LOW: 0

