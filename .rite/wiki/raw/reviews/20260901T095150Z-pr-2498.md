---
type: reviews
source_ref: "pr-2498"
captured_at: "2026-09-01T09:51:50+00:00"
pr_number: 2498
title: "PR #2498 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2498 — refactor(cleanup): worktree 退出・ブランチ削除・state 削除を helper へ抽出する
- **Type**: review
- **Reviewed at**: 2026-09-01T18:50:32+09:00
- **Reviewers**: code-quality, error-handling, test, prompt-engineer, tech-writer, security (cycle 2 / 差分スコープ)

### Finding Patterns

cycle 2 は差分スコープ（21 行）で、前 cycle の blocking 3 件はすべて FIXED と検証された。
新規 blocking は 0 件で、残ったのは実測アンカーを構造的に持てない字面整合クラスのみ。

1. **契約と実装が衝突する経路は「契約側の更新」で閉じられる**: 純粋抽出 Issue の「振る舞い不変」MUST と
   fail-loud 原則が衝突した経路で、実装を戻さず Decision Log に例外エントリを足し AC の Then に例外を
   明記したところ、前 cycle に CRITICAL を出した reviewer 自身が FIXED と判定した。往復を止めるのは
   revert ではなく契約の明文化だった。ただし**契約文を足すときは適用範囲を全箇所で揃える**必要がある —
   §3.3 の例外括弧を 2 軸、AC-8 を 3 軸で書いたところ、次の cycle で「契約文書内で例外の適用範囲が
   食い違う」と別 reviewer に指摘された。例外は複数箇所に書くほど不一致の面が増える。

2. **非回帰 pin は「変異を当てて赤くなるか」まで確認して初めて完成する**: prefix 一致から値 pin へ
   強めた assert について、3 名の reviewer がそれぞれ独立に変異（helper を修正前の形へ戻す / marker 値を
   反転させる）を一時コピーへ当て、当該 assert だけが赤くなることを確認した。pin を強めた側が
   自分で変異を 1 回当てておくと、この確認が reviewer 間で重複しない。

3. **同一箇所を 4 名が独立検出したが、finding にするか推奨事項にするかで判定が割れた**: 診断文言の
   ラベル誤りについて 3 名が finding、1 名が「cycle 2 差分外なのでユーザー判断」として推奨事項に置いた。
   scope 判定の基準は **cycle の diff ではなく PR の diff** であり、その行が base に存在しないなら
   current-pr が正しい。cycle 単位で scope を判定すると、前 cycle で入った行が永久に「差分外」になる。

4. **アンカー適格性の判定は reviewer 間でぶれる**: 同一の「判定表に marker 不在行が無い」指摘に対し、
   一方は「字面整合クラスなのでアンカー不適格」として実測なしで報告し、他方は対象文書自身を grep した
   結果をアンカーとして添付した。**レビュー対象文書自身のテキストを grep して pin 不在を示す形は
   アンカー適格ではない**（Finding Gate の明文）。統合側はこの規則で揃える必要があり、揃えないと
   同じ指摘が cycle ごとに blocking / non-blocking を行き来する。

5. **診断文言のラベルは呼び先ごとに実測して書き分ける**: 「原因候補: … / 引数不正 (rc=2)」のような
   案内は、呼び先が実際にその終了コードを返すかを測ってから書く。今回の呼び先は `exit` 文を 1 つも持たず
   未知オプションを握り潰して rc=0 を返すため、rc=2 は helper 自身の構文エラーでしか出なかった。
   さらに呼び出し側が全オプションを値付きでハードコードしているため、引数エラー自体が到達不能だった。
   sibling から文言をコピーするときは、呼び先の終了コード規約まで一緒に確認する。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW: 0

