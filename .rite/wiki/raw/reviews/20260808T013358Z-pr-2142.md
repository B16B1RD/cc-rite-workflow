---
type: reviews
source_ref: "pr-2142"
captured_at: "2026-08-08T01:33:58+00:00"
pr_number: 2142
title: "PR #2142 review results (cycle 3)"
ingested: true
---

## Review Results

- **PR**: #2142
- **Type**: review
- **Reviewed at**: 2026-08-08T10:32:43+09:00 (cycle 3, 差分スコープ)
- **Reviewers**: application, code-quality, error-handling, prompt-engineer, security, test

### Finding Patterns

cycle 2 の指摘 11 件を修正した diff (104 行) に対する cycle 3 レビュー。**新規 blocking 6 件のうち 4 件が、前 cycle で「番人を立てる」ために書いたコメントと assert 自身の欠陥**だった。以下はその構造から得られた経験則。

1. **「機械検査を足す」修正は、足した検査が名乗る範囲と実際に見る範囲の一致まで含めて 1 セットである**: cycle 2 の指摘に応えて (a) 隣接性 assert、(b) 双方向 pin、(c) 静的 probe を新設したところ、cycle 3 で (a) が「all three」と名乗りながら 3 条件のうち 1 対しか見ていない、(b) が「中和後も可読部分が残る」と名乗りながら定型接頭辞しか needle にしていない、と 2 件とも指摘された。**assert のラベルは契約の宣言であり、述語がそれを満たさなければ「虚偽主張」クラスの欠陥になる**。新しい assert を書いたら、ラベルが約束する全ての壊れ方について mutant を当てる。1 つでも生存したら、ラベルを狭めるか述語を広げるかの二択で、放置は選べない。

2. **負のコントロールが定型文だけを見ていると、対になる正のコントロールごと空振りになる**: 「制御文字が出ない」を assert する TC の隣に「可読部分は残る」を置いたが、後者の needle が固定接頭辞だったため、**出力から payload を丸ごと削除しても両方 green** になった。`assert_not_contains` を書いたら、その裏返しの `assert_contains` は必ず**可変部分**（payload そのもの）を needle にする。固定部分を needle にした counterweight は counterweight として機能しない。

3. **共有ヘルパーの欠陥を主張するときは、影響範囲を数えてから書く**: 「`assert_grep_in_section` は escape が剥がれて **assert が常に PASS する**」「**全 caller** に同じ縮退がある」と書いたが、実測では (i) start が一致しなければ empty section を検出して FAIL する（常時 PASS ではない）、(ii) 縮退するのは**終端**パターンに意味を変える escape を含む caller だけで既存 28 caller 中 0 件だった。誤った一般化は「28 箇所の一斉修正」か「正常な assert 全体への不信」のどちらかを誘発する。**主張の範囲は grep で数え、数えた結果を主張に書く**。

4. **中和モードの選択は入力の信頼境界で決める — 既存 call site の多数派に合わせない**: `neutralize_ctrl --c0-only` を既存 caller と同形だからと採用したが、既存の `--c0-only` 利用箇所はいずれも gh/jq の stderr・ローカルパス・セッション内生成文で、**第三者が書ける public リポジトリの Issue body が端末向け診断へ入る経路は本 site が初**だった。`--c0-only` は C1 (CSI U+009B 等) を素通しする設計で、helper の header 自身がそれを「jq プライマリ経路依存」と限定している。**helper のモードを選ぶときは header が書いた前提条件が自分の経路で成立するかを確認する**。

5. **正規表現を「宣言形」に絞ると、絞った側の分岐が別の情報を落とすことがある**: 述語を宣言形へ anchor した結果、`## 複雑度` セクション記法では**見出し行**が先に一致し、実際に読めなかった値行が診断に一度も現れなくなった。前 cycle の別修正（placeholder を absent 側へ合流）がこの経路を新たに開いたため、2 つの修正の交差で生まれた欠陥である。**同一 cycle で複数箇所を直したら、修正どうしの交差経路を 1 つずつ確認する**。

6. **行番号による自己参照コメントは、書いた時点で既にずれていることがある**: 同一ファイル内へ `:157 / :170 の house pattern と揃える` と書いたが、同じ diff が上流に 8 行を挿入していたため参照先は実際には `:160 / :177` だった。**同一 diff 内の行番号参照は、自分自身の挿入で必ずずれる**。意味的アンカー（「記法 1 / 記法 2 の抽出式」等）で書けば腐らない。

### Severity Distribution
- CRITICAL: 4
- HIGH: 2
- MEDIUM: 4
- LOW-MEDIUM: 0
- LOW: 0

(blocking 6 件 / 実測なし non-blocking 4 件。実測必須ゲート適用後。per-cycle blocking の推移は 6 → 6 → 6)

