---
type: reviews
source_ref: "pr-2137"
captured_at: "2026-08-07T13:33:23+00:00"
pr_number: 2137
title: "PR #2137 review results"
ingested: false
---

## Review Results

- **PR**: #2137 — refactor(hooks): iterate 経路の marker emit・照合を共有関数 + fixture テストへ機械化する
- **Type**: review
- **Reviewed at**: 2026-08-07T22:30:57+09:00
- **Reviewers**: application, error-handling, test, security, prompt-engineer, tech-writer

### Finding Patterns

**新規 lib を既存 lib 群へ追加するとき、同ディレクトリの兄弟が既に採用している防御パターンが引き継がれない**。本 PR の `hooks/scripts/lib/context-marker.sh` は引数パースで `shift 2` を使い、値なしフラグ末尾で無限ループする。同ディレクトリの `worktree-git.sh:584-591` は同じ失敗を名指しするコメント付きで既に `shift 2 || shift` を採っており、リポジトリには専用テスト `hooks/tests/shift2-loop-hardening.test.sh` まで存在する。にもかかわらず新規ファイルだけが後退した。原因は静的検査リスト (TC-7) がファイル名の明示列挙で、`lib/` 配下を 1 本も含んでいなかったこと — 新規ファイルは既存ガードの死角に入る。教訓: 既存の hardening test がファイル名列挙型なら、新規ファイル追加時にリストへの登録が必要か必ず確認する。列挙ではなく sweep 型にできないかも検討する。

**「検査を通らなかった値」を診断メッセージへ無加工で埋めると、拒否経路が当の攻撃を成立させる**。`marker_emit` は改行入り KEY を rc 1 で拒否するが、ERROR 文が KEY を無加工でエコーするため、拒否した出力の中に桁 0 の `[CONTEXT]` 行が現れる。同リポジトリの `review-save-json-verify.sh:117-119` は同一の失敗を明記した上で `_scrub()` を検査ブロックより前に定義済み。教訓: 入力検証を持つ helper では、拒否経路の診断出力も同じ検証の対象にする。`_scrub` の定義位置は検証ブロックより前でなければ拒否経路を覆えない。

**emit 側と照合側で pin が非対称になる**。本 PR は emit 側を双方向に pin (直接 echo 0 件 + 共有関数呼び出し 6 件以上) した一方、照合側は 1 つも pin していない。実測: 照合 4 箇所を旧 sed idiom へ全戻ししてもスイートは 57/57 green のまま。本 PR が実測で修正した既存バグ 2 件が silent に再発しうる。教訓: 委譲リファクタで「emit と照合の両方を関数化する」場合、pin も両側に置く。片側だけの pin は Asymmetric Fix Transcription の一形態。

**規約を「関数と test へ移す」refactor で、散文の規約リストが逆に増殖する**。本 PR は「読み取り側の規約を散文で書き直さないこと」という MUST NOT を新設しながら、同じ diff がその 5 項目リストを 5 箇所に複製し、生成時点で既に 2 箇所が drift していた (片方は「トークン完全一致」を落とし、もう片方は「branch スコープ」を落とす)。教訓: 「散文から機械へ移す」目的の PR では、移動元の散文が残っていないかを grep で数える。所在ポインタ (1 行) と規約の再定義 (列挙) を明確に区別する。

**規約の一部が委譲先を持たないまま消える**。削除された規約のうち「値側の部分一致禁止」は `marker_get` の担当外 (関数は値を返すだけで比較しない) で、移送先の fixture に pin する余地が構造的に無かった。教訓: 散文規約を関数へ委譲するとき、規約の各項目が実際に関数の責務範囲内かを 1 項目ずつ確認する。範囲外の項目は呼び出し側に残す。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 1
- LOW: 1
- (non-blocking / 実測なし: 7)

