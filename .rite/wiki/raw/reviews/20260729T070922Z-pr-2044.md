---
type: reviews
source_ref: "pr-2044"
captured_at: "2026-07-29T07:09:22+00:00"
pr_number: 2044
title: "PR #2044 review results (cycle 3)"
ingested: false
---

## Review Results

- **PR**: #2044 (cycle 3)
- **Type**: review
- **Reviewed at**: 2026-07-29T16:08:26+09:00
- **Reviewers**: prompt-engineer, tech-writer, error-handling

### Finding Patterns

同一 PR の 3 巡目。cycle 2 の修正が cycle 3 で新たな blocking 2 件を生んだ (fix-introduced)。得られた再発パターン:

1. **「非対称の是正」が別の軸に新しい非対称を作る**: cycle 2 で「sid 軸を `--session` で塞いだのに state root 軸が無防備」を是正して `RITE_STATE_ROOT={state_root}` を足したところ、今度は「`state_root` が空のとき何も起きない」という第 3 の劣化軸が生まれた。しかも空値は `[ -n "$VAR" ]` 判定により「未設定」と**完全に同義**へ縮退するため、明示的に渡しているのに省略時と同じ空振りが無言で起きる。さらに悪いのは分岐で、`state_root` が空でも `flow-state.sh path` は rc=0 を返すので `session_id` は非空のままとなり、劣化を想定した (b') ではなく健全前提の (b) へ **mis-route** する。教訓: 値を marker で運ぶときは「空だったら」を必ず設計する。空値が「未設定」と同義になる env var では、渡すこと自体が防御にならない。

2. **リテラル置換される値は quote する**: `RITE_STATE_ROOT={state_root}` のように placeholder をリテラル置換する新規サイトでは、既存の `RITE_STATE_ROOT="$VAR"` (変数展開・assignment context では word splitting が抑止され quote は装飾的) と違い、**word splitting が実際に効く**。空白を含むチェックアウトパスで assignment prefix が途中で切れ rc=127 になる。「既存パターンに合わせた」は変数展開とリテラル置換の差を無視した弁護になる。

3. **場合分けを増やしたら「各ケースで何が観測できるか」も同時に更新する**: cycle 2 で内訳を 3 ケースに補った際、末尾に「観測できる signal は WARNING のみ」と一括で書いたが、そのうち 1 ケースは WARNING を出すブロック自体が未実行のため **signal ゼロ**だった。「WARNING を探して見つからない」ことを「この失敗モードではない」と誤読させる。ケースを足すときは、そのケースで何が出て何が出ないかをケース単位で書く。

4. **修正が新しい指摘を生むのは異常ではない**: cycle 1→2→3 と blocking が 2→3→3 で減っていない。ただし内容は毎回具体的で、同じ指摘の再出現ではなく「前の修正が触れた領域の隣接欠陥」。fix-introduced finding を attribution できると、収束していないのか掘り進んでいるのかを区別できる。

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 2
- LOW: 2

