---
type: reviews
source_ref: "pr-2126"
captured_at: "2026-08-06T15:16:43+00:00"
pr_number: 2126
title: "PR #2126 review results"
ingested: false
---

## Review Results

- **PR**: #2126 — feat(pr-review): cycle 2+ を差分スコープ化し reviewer を動的選抜する
- **Type**: review
- **Reviewed at**: 2026-08-07T00:14:45+09:00
- **Reviewers**: prompt-engineer, security, application, error-handling, tech-writer, test

### Finding Patterns

cycle 1 で blocking 8 件 / 非実測 non-blocking 9 件。本 PR は「新規 bash helper + それを消費する散文契約 + テスト」という 3 層構成で、指摘は層をまたいだ**契約の不一致**に集中した。

1. **helper の出力語彙と散文側の定義がずれる（4 reviewer が独立検出、最頻パターン）**: helper が `findings[]` から抽出した値を「blocking を出した reviewer」と 5 箇所（helper docstring / SKILL.md 2 箇所 / cycle-scope.md 2 箇所 / docs 2 箇所）で呼んでいたが、実測必須ゲート適用後の `findings[]` は blocking 集合と nit-noted 集合の**和**だった。新規 helper が既存の永続 JSON を読むとき、その配列の意味論を書込側（`review-measured-gate.sh`）のコメントで確認せず名前から推測すると、この種のずれが記述側 N 箇所へ同時に伝播する。教訓: **既存永続データを新規 consumer が読むときは、書込側が定義した集合の意味を SoT として引用し、名前から推測しない**。

2. **house convention の未適用が専用 lint スイートの死角に落ちる**: 引数パーサの `shift 2` は既存 8 script へ横展開 hardening 済みで専用テスト（`shift2-loop-hardening.test.sh`）が機械検査しているが、**新規 script はそのテストの対象リストへ自動登録されない**ため suite は green のまま素通りし、値なしフラグ末尾で無限 hang する欠陥が残った。教訓: **横展開 hardening のテストが「対象リスト方式」のとき、新規ファイル追加時にリスト登録を同時に行わないと保護が効かない**。リスト方式の lint を持つリポジトリでは新規ファイルの登録漏れが構造的な死角になる。

3. **fail-safe fallback が「安全側に倒す」と「失敗理由を可視化する」を混同する**: `|| var=""` 形の fallback が jq 失敗を握り潰し、正常系（0 件）と失敗系がバイト単位で同一出力になった。同一ファイル内に selective surface を実装した経路と診断を捨てる経路が同居する非対称も検出された（既存 Wiki 経験則 `stderr-selective-surface-over-truncate` の再演）。

4. **static contract test の pin が母集合を絞らず mutation を素通りさせる**: 注入本文から mandate 1 行を削除しても、同じ語が同一ファイルの別 2 箇所（SoT 宣言 / 合成理由）に出現するため pin が満たされ green のままだった。テスト自身の header が「単語 1 個の pin は素通りする」と警告していたにもかかわらず、4 つの mandate のうち 1 つだけがその規約に違反していた。教訓: **rationale を reference へ退避する設計では同じ語が SoT 宣言・理由説明・注入本文の 3 箇所に現れるため、注入本文だけを行形アンカーで pin しないと検証力が失われる**。

5. **production が使う呼び出し形にテストが 1 件も無い**: 全 14 テストが `--results-dir` を明示するのに、実呼び出しは同オプションを渡さない。既定解決経路（`state-path-resolve.sh`）を壊しても全件 green だった。教訓: **helper のテストは production の呼び出し形を最低 1 本含めること**。オプションを常に明示するテストは、既定値解決という最も壊れやすい経路を丸ごと素通りさせる。

6. **前文の identity 宣言が後続ステップの新機能を無効化しうる**: skill 冒頭の「一切関係なく常にフルレビュー・スコープ縮退禁止」が未変更のまま残り、新設した差分スコープと正面衝突した。さらに別 skill（fix）の handoff **実行時文字列**にも同じ禁止句があり、差分スコープが発動すべき瞬間に orchestrator の context へ注入される。教訓: **スキルに「例外なし」の語調で書かれた前文がある場合、その例外を作る変更は前文と、他 skill の handoff 文字列まで含めて同時に更新する**。

### Severity Distribution
- CRITICAL: 0
- HIGH: 5
- MEDIUM: 3
- LOW: 0

