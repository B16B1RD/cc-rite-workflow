---
type: reviews
source_ref: "pr-688"
captured_at: "2026-04-28T10:58:54+00:00"
pr_number: 688
title: "PR #688 review results (cycle 12)"
ingested: false
---

## Review Results

- **PR**: #688 — fix(workflow): #687 multi-state-aware flow-state read helper
- **Type**: review
- **Reviewed at**: 2026-04-28T19:58:00+09:00
- **Reviewers**: prompt-engineer / code-quality / error-handling / test / tech-writer / security (6 reviewers)
- **Cycle**: 12 (累積 14 回目 38+ cycle 中)

### Finding Patterns

本 PR (Issue #687 multi-state-aware flow-state read helper) cycle 12 の review-fix loop で検出された 7 finding (HIGH 1 + MEDIUM 3 + LOW 3) の主要パターン:

1. **本 PR 自身の DRIFT-CHECK ANCHOR doctrine 違反 (MEDIUM, code-quality)**: 本 PR cycle 43 F-02 で確立された「semantic name 参照」原則 (hardcoded 行番号 → 関数名 anchor 置換) が、同一ファイル `flow-state-update.sh` の他 2 箇所 (L181 / L220) で未適用のまま残存。同 PR 内で確立された規範が同 PR 内の他箇所に未浸透という self-meta drift fractal の典型。

2. **Migration 取り残し (MEDIUM, code-quality)**: 新規 helper `_validate-helpers.sh` を 3 caller のうち 2 つだけが使用し、`resume-active-flag-restore.sh` 1 つは旧 inline pattern を残存。3 caller 中 2/1 の不均一更新 = DRY 化導入の核心理由 (drift 防止) が部分的にしか達成されない。

3. **集約効果のコメント overstate (MEDIUM, code-quality)**: `_validate-helpers.sh` で集約したのは validation logic のみだが、コメントは「helper 追加時の 2 箇所更新が不要になり」と書いており、実際には helper 名 list そのもの (両ファイルにハードコード重複 7 entry × 2 箇所) が依然 2 箇所同期更新を要する。Issue #687 root cause と同型の drift 再発許容経路。

4. **Documentation grep evidence 検査範囲漏れ (HIGH, tech-writer)**: workflow-incident-emit-protocol.md cycle 43 F-05 で `commands/wiki/lint.md` のみ grep して `commands/lint.md` (rite lint) を見落とし、「lint.md は out-of-scope」と誤記。実際には commands/lint.md:1108 で reverse-link + 3 sentinel emit declarative path を持つ。Wiki 経験則「Design doc は現 HEAD の SoT を verify してから書く」の同型違反 (ファイル名衝突パスの取り違え)。

5. **Security defense-in-depth で発見された factual comment error (LOW, security)**: GNU coreutils mktemp の実装 (mode 600 デフォルト) と矛盾する「mktemp は umask 022 環境で 644 で作成」コメント。chmod 600 自体は防御として正しいが誤った仕様引用は将来の cognitive burden + 冗長 defensive code 誘発の経路。実機検証 (umask 022; mktemp /tmp/test → -rw-------) で確認。

6. **Hypothetical security findings (LOW × 2, security)**: STATE_ROOT validation の metachar reject 不足 / sentinel sanitize() の制御文字素通し。いずれも Hypothetical (現 caller chain では到達不能だが将来の direct caller / details 流入経路でリスク)。security 例外カテゴリにより blocking 維持。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 3
- LOW: 3

### 教訓 (経験則候補)
- 「semantic name 参照原則」を確立したら同 PR 内の **同型ファイル他箇所**にも適用漏れが無いか cross-check する (self-meta drift fractal pattern 12 回目実例)
- 集約 helper 導入時の効果記述は「何が集約され、何が依然分散しているか」を明示する (overstate は drift 再発を許容)
- ファイル名衝突 (lint.md vs wiki/lint.md) 検査時は `find -name` で全候補列挙してから対象選択する手順を caller doctrine に組み込む
- DRY 化助手の API は「caller が 1 箇所のみ更新で済む」設計にしないと真の drift 防止にならない (helper-list の DEFAULT_* 配列内蔵 / 2 callsite の sed audit 等)
- review.md spec の jq query (`[.findings[].id] | unique | length == (.findings | length)`) は context リセットで `.findings` 参照不能になる pre-existing bug; 別 Issue で修正を検討

