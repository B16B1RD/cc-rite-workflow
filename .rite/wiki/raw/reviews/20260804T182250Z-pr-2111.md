---
type: reviews
source_ref: "pr-2111"
captured_at: "2026-08-04T18:22:50+00:00"
pr_number: 2111
title: "PR #2111 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2111 — refactor(wiki-ingest): ステップ 6 の index 操作をテスト付き helper へ降ろす
- **Type**: review
- **Reviewed at**: 2026-08-05T03:20:20+09:00
- **Reviewers**: application, error-handling, test, code-quality, security, prompt-engineer

### Finding Patterns

散文 → helper 委譲リファクタ (Issue #2089) の cycle 2。blocking 7 件 / non-blocking 9 件。cycle 1 の修正 5 件はいずれも意図どおり機能し (test reviewer が mutation 4 種を独立再現)、cycle 2 の指摘は「cycle 1 の fix 自身が導入した欠陥」と「cycle 1 では届かなかった深度の検証」に二分された。

- **fix が過剰に広い中和を導入して実データを壊す (cycle 内再帰)**: cycle 1 の F-05 対策 `esc_title` が title 内の**すべての** `]` を `&#93;` へ中和したため、詐称ベクタ `](pages/…)` と無関係な `[CONTEXT]` / `.errors[]` / コードスパン内 `[[:cntrl:]]` まで潰し、本 repo の Wiki に実在する 3 ページで GFM のリンク範囲が壊れることを reviewer が `gh api /markdown` で実測。攻撃ベクタが literal 連なり (`](`) でしか成立しないなら中和も同じ粒度に絞る — 「防御は攻撃面と同じ幅で」。
- **委譲で shell 語へ literal 置換する値が増えると injection 面が開く**: ステップ 6 の新 bash block が `{title}` / `{description}` だけ quoted heredoc、`{updated}` / `{confidence}` / `{slug}` / `{domain}` は double-quote 内へ直接置換という非対称になり、`"` 混入で任意コマンド実行が成立することを security reviewer が実測 (PWNED ファイル生成)。helper 側の enum / charset 検証はシェル parse 後に走るため無力。さらに注入されたコマンド列の rc が複合コマンドの rc になるため **rc=0 + marker 0 行**という marker 表のどの行にも当たらない状態が silent success として素通りする。
- **散文を畳む過程で入力正規化ルールが 1 つ落ちる**: 旧手順 2 の「title / updated / confidence は YAML の引用符を外す」から `title` が脱落し、かつ `description` の出典が LLM 散文から frontmatter へ移ったため、契約どおり substitute すると引用符が登録行に固着する (サマリー列は保持経路で以後も運ばれ続ける)。**縮退時に失われる規則は「削除文の文単位突合」でしか検出できない**。
- **ガードの識別力は「そのガード単独で発火する形状」でしか担保されない**: cycle 1 で TC-13b (読み取り不能) にガード固有文言 assert の rationale をコメントで書きながら、同型の TC-13 (不在) へ転記せず `grep -q 'ERROR'` のままにしたため `-f` ガード削除が 39/39 green で素通り。TC-16c の総称 WARNING assert も同型。**rationale をコメントに書いた時点で同型 TC への転記が必要**。
- **共有テスト runner の会計契約はローカル summary 再定義で壊れる**: `_test-helpers.sh` の `skip()` を使いながら summary に `, N skipped` 成分を持たないと run-tests.sh の skip 会計 cross-check が全 run を exit 1 にする。3 reviewer が独立検出 (cross-validation)。

### Severity Distribution
- CRITICAL: 0
- HIGH: 5
- MEDIUM: 9
- LOW: 2

