---
type: reviews
source_ref: "pr-2036"
captured_at: "2026-07-27T04:14:03+00:00"
pr_number: 2036
title: "PR #2036 review results"
ingested: true
---

## Review Results

- **PR**: #2036 — feat(pr-review): blocking/mergeable 判定を実測必須ゲート (measured=true) に再定義
- **Type**: review
- **Reviewed at**: 2026-07-27T13:13:13+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling, application

### Finding Patterns

判定入力の「格上げ」が生む SoT 非同期パターンが 5 reviewer 中 3-4 名から独立に検出された。既存の記録専用フィールド (findings[].verification.measured) を blocking 判定の入力に格上げする変更で、read 側だけを配線し write 側 (JSON 生成) を後続 PR に委ねたため、以下が同時発生した:

1. **同一 invariant の複数コピー間の非対称** — cross-field invariant #2 の述語を read 経路 3 箇所 (P0/P2/P3) のうち P3 のみ変更し、SoT ドキュメントも未更新。同一 JSON が経路により受理/拒否に割れる。
2. **SoT 内での新旧規則の同居** — 「欠落 = measured=false」(旧 1 層) を禁止する注記を追加したが、同じファイルの canonical コードブロック・prospective 段落・サブフィールド表前置きが旧規則のまま残り、禁止対象の式を SoT 自身が「正準」として先に提示する導線が残った。
3. **判定時点に存在しないフィールドでの定義** — total_findings を verification.measured == true で定義したが、この判定が走る時点では JSON が未生成。実際の検出媒体 (内容列の Verification: アンカー regex) と語彙がずれ、literal 解釈で判定が壊れる。
4. **prompt 同時注入テキストの正面衝突** — reviewer prompt に同時注入される 2 文書 (_reviewer-base.md と pr-review/SKILL.md) が、同じアンカーについて「記録専用」と「blocking を決める」という逆の説明を持つ状態になった。
5. **相対リンクの深さ誤り** — 同一リストの近接行が ../../references/ なのに新規行だけ ../../../references/ で解決不能。機械検証 (realpath -m) で確定。

**経験則**: フィールドのセマンティクスを「記録専用 → 判定入力」に格上げする変更では、(a) そのフィールドを評価する全コピーの grep 同期、(b) SoT 内の旧規則記述の棚卸し、(c) 判定が走る時点でそのフィールドが実在するかの確認、(d) 同一 prompt に注入される他文書との整合、の 4 点を必ずチェックリスト化する。read 側と write 側を別 PR に分割する場合、read 側単独マージ時の挙動 (fail-safe 方向か、経路間で判定が割れないか) を明示的に文書化しないと、分割の境界そのものが欠陥になる。

### Severity Distribution
- CRITICAL: 1
- HIGH: 6
- MEDIUM: 4
- LOW: 0

