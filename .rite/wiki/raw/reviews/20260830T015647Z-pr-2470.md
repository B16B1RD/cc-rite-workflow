---
type: reviews
source_ref: "pr-2470"
captured_at: "2026-08-30T01:56:47+00:00"
pr_number: 2470
title: "PR #2470 review results (cycle 3)"
ingested: false
---

## Review Results

- **PR**: #2470 (cycle 3, incremental scope)
- **Type**: review
- **Reviewed at**: 2026-08-30T10:55:34+09:00
- **Reviewers**: code-quality, error-handling, prompt-engineer, security, tech-writer, test

### Finding Patterns

- **`Verification:` アンカーの正規形はラベルと `=>` の間に LHS を要求する**: 実測必須ゲートの検出 regex は `Verification: <repro|failing_test> <実施内容> => <結果>` の形で、`Verification: repro => 結果` のようにラベル直後に `=>` を置くと LHS が 0 文字になり **未判定（blocking 据え置き）** に落ちる。cycle 1 と cycle 2 で連続して全 finding が未判定になったのはこれが原因で、警告文の「種別ラベル誤記」「境界欠落」を疑って 2 cycle 分の時間を使った。**警告文の候補列挙に自分のケースが無いときは、helper の regex を直接読んで最小ケースで probe する**。

- **削除が正しいとは限らない — 削除後にその入力がどこへ流れるかを見る**: 到達不能に見える `case` arm の削除を 2 名が推奨したが、削除するとその result は消えるのではなく `failed|*)` に流れ込み、失敗ではない値に対して「更新に失敗しました」という誤診断と手動復旧コマンドを出す。つまり「デッドコード除去」ではなく「診断の劣化」だった。**分岐の削除を検討するときは、その分岐が受けていた入力の行き先を必ず確認する**。

- **ガードの除外リストは「移設」には足せても「削除」には足さない**: rail の subsequence 保存ガードには既存の除外前例があるが、それは 1 行が別の書き方へ**移設**されたケース。恒久的に pin から外す除外を「削除を通すため」に足すと、散文 88 バイトと引き換えに機械レール pin を 1 本失う取引になる。**除外の前例があっても、前例の性質と自分のケースの性質が同じかを確かめる**。

- **`grep -c` を `set -e` 下で裸の代入に使わない**: マッチ 0 件で rc=1 を返すため、0 件が検出対象そのものであるテストでは、失敗を報告する前に suite が abort して summary も failure 一覧も出ない。CI は赤くなるので見逃しは生じないが、何が壊れたかが読めなくなる。repo 内の既存 19 箇所はすべて `|| true` を伴っており、慣習としては確立している。

### Severity Distribution

- CRITICAL: 0
- HIGH: 0
- MEDIUM: 0
- LOW-MEDIUM: 2 (いずれも class B 降格で non-blocking)
- LOW: 0

