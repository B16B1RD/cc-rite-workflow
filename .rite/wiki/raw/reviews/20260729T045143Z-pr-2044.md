---
type: reviews
source_ref: "pr-2044"
captured_at: "2026-07-29T04:51:43+00:00"
pr_number: 2044
title: "PR #2044 review results (cycle 3)"
ingested: false
---

## Review Results

- **PR**: #2044
- **Type**: review (cycle 3)
- **Reviewed at**: 2026-07-29T13:50:06+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

cycle 3 の 3 件はすべて **cycle 2 の修正に付随した散文**の欠陥で、コード側の分岐ロジックには指摘ゼロ。パターンは「**修正の効果を、実際より強く書いた**」に収束した。

1. **「窓を閉じた」と書いたが、実際は「窓を狭めた」だった（2 レビュアーが独立検出）**: counter reset を fire 分岐から sentinel 直前へ移す修正は、中断窓を「review invoke + ステップ 6 全体」から「共有前段 → sentinel 出力」へ縮めたが消してはいない。にもかかわらず散文は 5 箇所で「発火が無記録のまま予算だけ復活する経路を持たない」と無条件に断定した。**教訓: 窓を縮める修正をしたら、残った窓の大きさを書く。「閉じた」と書けるのは、残存窓がゼロであることを示せるときだけ。** 縮小と消滅を書き分けないと、後続の実装者は「ここはもう安全」と読んで別の変更で窓を広げる。

2. **同一 API の default 挙動を、片方の呼び出しでは思い出し、もう片方では忘れた（3 レビュアーが同一 file:line を検出）**: `flow-state.sh set` は `--handoff` を伴わないと handoff を default-clear する。fire 分岐についてはこれを正しくモデル化していたのに、同じ PR で新設した共有前段の set については「再セットしない」（受動）としか書かず、「クリアする」（能動）に踏み込まなかった。その結果、停止通知が「counter reset に失敗＝handoff クリアにも失敗」と誤った連言を人間に断定する。**教訓: default 挙動を持つ API を複数箇所で呼ぶなら、各呼び出しについて default が何をするかを個別に書き下す。1 箇所で正しく書けたことは、他の箇所で正しく書けている保証にならない。**

3. **marker を移設したのに、marker 源を列挙する散文が追随しなかった**: `FIRE_RESET` の emit 位置をステップ 1 からステップ 6 共有前段へ移したが、注意行の前提条件は「ステップ 0.6 / ステップ 1 の marker を観測している場合」のまま。字義どおり適用する LLM は `FIRE_RESET` を観測対象から外し、注意行が無言で発火しなくなる。cycle 1・cycle 2 でも同型（条件を移管したのに条件を名指しする散文が残る）が出ており、**本 PR で 3 回目の再発**。

### Cross-Validation

`docs/CONFIGURATION.md:646` の「one cycle past the limit」について 3 レビュアーの見解が割れ、討論フェーズで解決した。tech-writer は「pr-review → fix → pr-review の handoff 連鎖が自己再武装し iterate ステップ 1 の counter 判定を経由しないため無制限」、prompt-engineer は「ステップ 1 に戻れば即再発火するので +1 cycle は妥当」、error-handling は「LLM が手順書に戻るか次第で確定できない」。合意点は「確定的に量化しない」で一致。**教訓: 「N cycle で収まる」のような blast radius の量化は、その N を決める制御フローが LLM の裁量を経由するなら書けない。**

### Process Observations

- 3 cycle 連続で「cycle N の修正が cycle N+1 の指摘を生む」構造になったが、指摘の性質は毎回 1 段ずつ浅くなっている（cycle 1: marker の自己矛盾 → cycle 2: 安全網の実挙動退行 → cycle 3: 散文の過大主張のみ、コード指摘ゼロ）。**収束の兆候は「指摘件数」より「指摘がコード層から散文層へ移ったか」で読むほうが早い。**
- 実測必須ゲートは 3 cycle で一貫して機能した。cycle 3 は 6 件中 3 件が `Verification:` アンカー付きで blocking、3 件が非実測で non-blocking。non-blocking 側には HIGH 相当の事実誤り（"one cycle"）も含まれるが、ゲートは severity ではなく実測の有無で判定するため降格された。**severity と実測は直交であり、HIGH かつ non-blocking は正常な組み合わせ。**
- cross-validation の boost 規則（同一 file:line を 2+ レビュアーが検出 → severity boost）が初めて発火し、MEDIUM×2 + HIGH×1 の指摘が CRITICAL へ昇格した。

### Severity Distribution
- CRITICAL: 1 (blocking)
- HIGH: 2 (blocking) + 1 (non-blocking)
- MEDIUM: 2 (non-blocking)
- LOW: 0

