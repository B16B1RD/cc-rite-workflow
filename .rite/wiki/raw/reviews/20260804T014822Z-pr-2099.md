---
type: reviews
source_ref: "pr-2099"
captured_at: "2026-08-04T01:48:22+00:00"
pr_number: 2099
title: "PR #2099 review (cycle 4)"
ingested: false
---

## Review Results

- **PR**: #2099 / cycle 4 — blocking 13 件（推移 10 → 9 → 10 → 13）

### Finding Patterns

1. **本 PR が実装した発散判定を、この PR 自身の review-fix ループへ適用すると発火する**（`trend=10,9,10,13; fire_at=4`）。
   同じ設計領域（run 境界のガード）を 3 cycle 往復し、各 cycle の fix が次 cycle の blocking を生み続けた。
   発散判定の妥当性は「作った当人のループを正しく発散と判定した」ことで実証されたが、同時に
   **修正主体が同じ領域で連続して誤る場合、cycle を重ねても収束しない**ことの実測記録でもある。

2. **「構造的に起きない」と書いた不変条件は、その保証が非ブロッキング処理に依存していれば偽**:
   「pin があれば混入は構造的に起きない」と header に書いたが、pin を更新する処理自体が
   非ブロッキング（失敗しても続行）だったため保証にならなかった。不変条件を主張するときは
   「それを保証する処理は失敗しうるか」を先に確認する。

3. **state を書き換える新ステップは、そのフィールドを根拠にしている全機構を洗ってから挿入する**:
   完了通知の前に counter をリセットするステップを足したところ、同じ set が handoff も
   default-clear し、完了通知を強制する Stop hook の保証を通知の前に外した（3 reviewer が独立検出）。
   `flow-state.sh set` は指定しなかったフィールドを消す契約なので、「何を書くか」だけでなく
   「何を消すか」を書く前に列挙する。

4. **同一ファイル内に canonical idiom がある処理を新規追加するときは、その idiom に揃える**:
   他 4 サイトが capture + head -5 + neutralize_ctrl で揃えている state 書き込みに、5 番目だけ
   素通しの形を足した。規約はコメントで明示されていたが、追加時に読まれなかった。

### Severity Distribution
- CRITICAL: 0 / HIGH: 6 / MEDIUM: 7

