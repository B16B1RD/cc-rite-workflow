---
type: reviews
source_ref: "pr-2130"
captured_at: "2026-08-07T02:39:31+00:00"
pr_number: 2130
title: "PR #2130 review results (cycle 3)"
ingested: false
---

## Review Results

- **PR**: #2130 — fix(pr-review): 8.0.4 positive 検査（本 cycle の結果 JSON 実在確認）を足す
- **Type**: review
- **Reviewed at**: 2026-08-07T11:38:18+09:00
- **Reviewers**: code-quality, error-handling, prompt-engineer, security, tech-writer, test

### Finding Patterns

cycle 3（差分スコープ）。cycle 2 の blocking 5 件は全て FIXED と 6 reviewer が独立に実測確認。
新規 blocking 4 件の内訳と、繰り返し観測されたパターン:

- **設計主張の 3 重化と片側更新（Asymmetric Fix Transcription の再発）**: helper の「誤一致防止」の根拠が
  docstring / `references/measured-gate-record.md` / `docs/SPEC.md` の 3 箇所に写されていた。fix の
  推奨対応が前 2 者だけを名指ししたため、名指しされなかった `docs/SPEC.md` だけが「fix が反証した旧根拠」の
  唯一の生存コピーになった。**修正指示が更新先を列挙する形式だと、列挙漏れがそのまま drift になる。**
  2 reviewer（test / code-quality）が独立検出。

- **テストが「守っているつもり」の対象と実際に落とす対象のずれ**: `_scrub` を守ると宣言した T-11 が、
  実際には同一行の `tr` による小文字化で偶発的に緑になっていた。cycle 2 で指摘され、cycle 3 の fix は
  `--commit-sha` 経路（`_degraded`）の arm を足したが、指摘が名指しした走査経路の call site は依然
  無防備のまま。**「関数が変異で落ちる」と「その call site が変異で落ちる」は別物**で、前者だけを
  満たす修正は指摘を PARTIAL のまま残す。3 reviewer（test / code-quality / security）が独立検出。

- **新設した縮退分岐に観測が付かない**: `rite_tempfile_new` 失敗時の WARNING + 空代入は、健全系と
  出力が違うのにどの assertion からも区別できない。同じファイルの 1 つ上の関数には
  `(機械検査: T-06n)` という追跡ポインタが付いており、**同一 PR 内で著者自身が示した追跡基準が
  新規分岐にだけ適用されていない**。sibling には `chmod 500` + `TMPDIR` 差し替えで縮退告知を pin する
  前例が既にあった。

- **非実測（static_trace）指摘は cycle を跨いで残り続ける**: cycle 2 の F-07/F-08/F-09 は 5 reviewer が
  cycle 3 で独立に再掲したが、いずれも measured=false のため 2 cycle 連続で non-blocking のまま。
  修正コストが「既存行への括弧挿入で行数増加 0」であっても、blocking でない限り fix 側に届かない。

### Severity Distribution

- CRITICAL: 0
- HIGH: 1
- MEDIUM: 2
- LOW-MEDIUM: 1
- LOW: 0
- non-blocking (実測なし): 4

