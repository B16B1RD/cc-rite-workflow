---
type: reviews
source_ref: "pr-3032"
captured_at: "2026-09-24T07:09:26+00:00"
pr_number: 3032
title: "pipefail 下で echo の出力を grep -q へパイプしない（cycle 3）（レビュー結果）"
ingested: true
---

# レビュー結果: pipefail 下で echo の出力を grep -q へパイプしない（cycle 3）

## 判定

mergeable（reviewer 1 名、差分スコープ、ブロッキング 0 件）

## 変更の要旨

停滞診断による見直しで、後続 Issue に送った 1 行の置換漏れを PR 内で直した。

## レビューで得た知見

- 受入条件の文言どおりの raw 検索は、検出器の例外に隠れた残件を拾える一方で、誤ヒット（`-eq`、`grep -c`、fixture 文字列、コメント）も多い。raw 検索のヒットは「対象外の理由」を分類して記録し、対象の残件 0 を示す。
- テストが書き出す別プロセスの stub 本文は、親の `set -o pipefail` を継承しないため、同じ字面でも欠陥クラスに当たらない。

