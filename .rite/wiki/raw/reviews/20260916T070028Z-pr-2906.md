---
type: reviews
source_ref: "pr-2906"
captured_at: "2026-09-16T07:00:28+00:00"
pr_number: 2906
title: "修正計画と検証鮮度のレビュー"
ingested: true
---

## Review Results

PR #2906

Reviewers: application-reviewer, test-reviewer, error-handling-reviewer, security-reviewer, prompt-engineer-reviewer, code-quality-reviewer, acceptance-reviewer

Findings: 3

rename 検出が有効な `git diff HEAD --name-only` は移動元を返さないため、Non-Target `protected/secret.py` を計画内 `src/moved.py` へ移動すると対象外ファイルの削除を見逃す。最終 caller は rc=0 を成功扱いし、明示 Non-Target 厳守と計画外変更の停止を保証できない。新規追加の変更パス列挙が原因で、diff-line inspection により revert test PASS。<br>Likelihood-Evidence: entrypoint_connection plugins/rite/skills/fix/SKILL.md:1623 → verify の変更パス検査。<br>Verification: repro python3 /tmp/rite-application-isolated-2906/run-isolated.py => RENAME DIFF: src/moved.py; RENAME RESULT: 0; FIX_VERIFICATION=pass; NON_TARGET_EXISTS: False
`inputs` ディレクトリを `rglob` で走査すると、その配下の directory symlink は `directory` とだけ記録され、リンク先の内容を鮮度キーに含めない。`inputs=['src']`、`src/link → ../shared` を通じて読む `shared/config` を変更しても関連テストを再利用し、AC-5 の「内容変更後の古い成功を拒否」に違反する。新規 fingerprint と再利用分岐が原因で、diff-line inspection により revert test PASS。<br>Likelihood-Evidence: entrypoint_connection plugins/rite/skills/fix/references/fix-plan.md:24 → verify related → fingerprint → reused。<br>Verification: repro python3 /tmp/rite-application-isolated-2906/run-isolated.py => SYMLINK RESULT: 0; FIX_VERIFICATION=reused; CURRENT: after OBSERVED: before RUNS: ['run']
[AC-5] fingerprint() は入力ディレクトリの rglob で得たディレクトリ symlink 自体を directory と記録するだけで、参照先の内容を含めない。inputs=['src']、src/link -> ../deps、検証コマンドが src/link/flag を読む通常の計画で、deps/flag を good から broken に変えても以前の成功を再利用する。現在の検証は失敗するため、古い成功を現状態の証拠にしないという Then を満たさない。<br>Likelihood-Evidence: runtime_observation python3 /tmp/rite-test-review-2906/run-strict.py symlink で依存内容変更後も再利用を観測<br>Verification: repro python3 /tmp/rite-test-review-2906/run-strict.py symlink => helper_rc=0; FIX_VERIFICATION=reused; FIX_VERIFICATION=pass; direct_test_rc=1

