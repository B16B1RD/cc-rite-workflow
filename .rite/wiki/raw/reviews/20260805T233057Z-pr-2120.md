---
type: reviews
source_ref: "pr-2120"
captured_at: "2026-08-05T23:30:57+00:00"
pr_number: 2120
title: "PR #2120 review results"
ingested: true
---

## Review Results

- **PR**: #2120 — feat(hooks): flow-state の phase 遷移を append ログへ記録し工程別時間を自動収集する
- **Type**: review
- **Reviewed at**: 2026-08-06T08:28:46+09:00
- **Reviewers**: application, error-handling, test, tech-writer, security

### Finding Patterns

本 cycle の指摘 7 件はすべて MEDIUM で、2 つのパターンに集約された。

**パターン 1 — 直近コミットで強化された同種経路の「強化前の形」を新規箇所へ転記する (4 件)**。本 PR は `.rite/logs/.gitignore` を生成する 3 番目の書き手を追加したが、guard として 2 コミット前 (#2114) に同一の共有ディレクトリ保護で `[ ! -s ]` (中身検査) + WARNING + `[CONTEXT]` marker へ明示的に強化された形ではなく、`session-start.sh` の強化前の形 (`[ -f ]` + `2>/dev/null || true`) を採用していた。しかも新規コメントが根拠として挙げた「`/rite:setup` の生成 `.gitignore` が covers するのは 2 エントリ」という記述も、同じ #2114 で 3 エントリへ増えていた。**同一 Issue が同一 PR 内で「実装の形」と「その根拠として引用した事実」の両方を stale にする**のが本 cycle の dominant pattern。Wiki の Asymmetric Fix Transcription の変種で、参照先が「別ファイルの対称位置」ではなく「数コミット前の自分自身の系譜」である点が新しい。**転記元を選ぶときは同種経路の最新版を `git log -S` で特定してから写す**のが対策になる。

**パターン 2 — テストのコメントが claim する保護と実 assert の catch 能力の乖離 (3 件)**。追加された 6 TC のうち、(a) 「実際に書き込みが成立した set ごとに 1 行」という順序契約、(b) `issue_number` / `pr_number` の JSON 型 (number)、(c) `ts` の provenance (同一 `date` 呼び出し由来) の 3 つは、実装コメント・SPEC・テストコメントのいずれかが明示的に pin したと述べているのに、実 assert がその差分を検出できなかった。**3 件すべて mutation testing (隔離 detached worktree での sed 改変 → suite 再実行) によって「実装を壊してもテストが PASS したまま」を実測して初めて surface した** — 静的なコメント読解では 3 件とも「pin 済み」に見える。逆に、mutation を当てて実際に FAIL した assert (1 行性 / `from` の値 / worktree 統合 / `{ ...; } 2>/dev/null` グルーピング / `.gitignore` 生成) は識別力が実証された。**新規 TC の識別力評価には mutation が不可欠で、コメントの pin claim を証拠として採用してはならない**。

### 実測必須ゲートの挙動

7 件のうち 4 件が `Verification:` アンカー付き (blocking)、3 件がアンカー無し (non-blocking へ降格)。降格した 3 件の内訳は、テスト欠落 2 件 (実装は正常動作することを実測済みだが「テストが検出しないこと」の実測は添えていない) と、コメント文言と実体の食い違い 1 件 (字面整合クラスでアンカー不適格 — tech-writer が正しく判別)。**「実装が正しく動くことの実測」は `Verification:` アンカーの要件を満たさない** — アンカーが記録すべきは指摘した欠陥そのものの再現であり、テスト欠落の指摘なら mutation を当てて suite が PASS し続けることを観測する必要がある。test-reviewer はこれを実行して 3 件を blocking に載せ、error-handling は実行せず 2 件が降格した。同じ「テスト欠落」でも、mutation の有無で blocking / non-blocking が分かれる。

### 討論フェーズ

同一箇所 (`flow-state.sh:214` の `.gitignore` guard) に対し error-handling が「silent は既存 convention なので fail-loud 化を要求しない」、security が「fail-loud 化必須」と相反。orchestrator が実コードを裏取りしたところ、error-handling の前提とした「既存 convention = silent」は 2 コミット前に更新済みで、より新しい同種経路の rationale コメントが本ケース (0 バイト残骸) を名指ししていた。**前提崩壊により error-handling の立場が維持できなくなり合意成立** (エスカレーション不要)。矛盾の解消に必要だったのは両者の主張の重み付けではなく、片方が前提とした事実の裏取り 1 回だった。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 7 (blocking 4 / non-blocking 3)
- LOW: 0

