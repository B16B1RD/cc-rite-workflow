---
type: reviews
source_ref: "pr-2044"
captured_at: "2026-07-29T03:46:34+00:00"
pr_number: 2044
title: "PR #2044 review results"
ingested: false
---

## Review Results

- **PR**: #2044 — fix(iterate): サーキットブレーカー発火を batch/対話とも機械的停止に統一する
- **Type**: review
- **Reviewed at**: 2026-07-29T12:44:32+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling

### Finding Patterns

散文が実行契約である rite のスキル定義 Markdown を書き換える PR で、4 レビュアーが独立に同型の欠陥を検出した。パターンは「述語を新設したが、その述語と隣接する既存の出力・状態が同期していない」に集約される。

1. **marker 三つ組の自己矛盾（2 レビュアーが同一箇所を独立検出）**: 新設した述語 `REFIRE` は成功分岐でのみ再評価され、失敗分岐では隣の `ITERATE_CYCLE` が無条件に 0 へ書き換わるため `ITERATE_CYCLE=0; RESET=failed-refire; REFIRE=1` という互いに否定し合う marker 行が出る。同一 PR が成功側では「counter は 0 に戻ったのに REFIRE=1 という自己矛盾した marker が出る」ことを理由に再評価を入れており、鏡像である失敗側だけが取り残された。**教訓: 述語を再評価する修正を入れたら、同じ述語が関わる全分岐（特に失敗側）を対で洗うこと。**

2. **注意行と本文テンプレートの矛盾**: エラー経路用の注意行を「追加する」指示だけを書き、同じ通知テンプレート内で矛盾する既存行を「差し替える／省く」指示を書かなかったため、実行時に矛盾する 2 つの再開手順が同一通知に並ぶ。**教訓: LLM 向けの散文で「注意行を追加せよ」と書くときは、追加によって偽になる既存行の扱いも同じ場所で指示する。**

3. **rationale が条件の移管に追随しない drift**: 条件を `RESET` から `REFIRE` へ移した後、分割の理由を説明する直上コメントが旧条件のまま残り、3 レビュアーが「読んだ LLM が `RESET` を条件と解釈しうる」と指摘した。**教訓: 条件式を移管したら、その条件を名指しする散文（コメント・表・前文）を旧語彙で grep して掃く。コミットメッセージ自身がこの手順を警告していても、override は掃けて条件参照は掃け残る。**

4. **hooks の canonical idiom が skills 側の埋め込み bash に届かない**: 診断スニペットの SoT (`control-char-neutralize.sh`) と parity test は `hooks/**/*.sh` のみを sweep するため、`skills/**/SKILL.md` の fenced bash に同型 idiom が入っても検出されない。本 PR が skills 側に最初の該当 site を作った。**教訓: hooks に canonical idiom を置くときは、skills の埋め込み bash が同じ規約の射程に入るかを決めておく。**

5. **user-facing docs のエラー記述が片側に寄る**: 単一の `flow-state.sh set` が counter リセットと handoff クリアの 2 責務を担うのに、失敗時の帰結を counter 側だけ docs に書いたため、直上の絶対表現（"never auto-continued past the limit"）が同 PR の兄弟ファイル記述と正面から矛盾した。**教訓: 1 回の書き込みが複数の状態を担うなら、失敗記述も担う状態の数だけ書く。**

### Process Observations

- 実測必須ゲートが期待どおり機能した。4 件中 3 件は `Verification:` アンカー（再現コマンド + 観測結果）を伴い blocking、1 件は散文の矛盾指摘でアンカーを添付できず non-blocking として記録に残った。散文 PR でも「stub を作って埋め込み bash を実行する」形で実測が成立することが 3 レビュアーで再現された。
- レビュアー間で同一箇所を検出したのは 1 件で、cross-validation により LOW-MEDIUM から MEDIUM へ昇格した。矛盾指摘はゼロで討論フェーズは発火していない。
- 仕様への疑問が 2 件挙がり、ユーザー確認で 1 件は承認、1 件は別 Issue（#2045）へ切り出した。いずれもレビュアー自身が「実装は仕様準拠 / マージはブロックしない」と結論したうえでの提起であり、この分類が機能した。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 2 (blocking) + 1 (non-blocking)
- LOW-MEDIUM: 1 (blocking)
- LOW: 0

