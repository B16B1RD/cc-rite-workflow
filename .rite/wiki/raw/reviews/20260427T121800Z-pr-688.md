---
type: reviews
source_ref: "pr-688"
captured_at: "2026-04-27T12:18:00+00:00"
pr_number: 688
title: "PR #688 cycle 31 review results"
ingested: false
---

## Review Results — cycle 31

- **PR**: #688
- **Type**: review
- **Cycle**: 31 (cycle 30 fix の re-review)
- **HEAD**: 93731ff

### Finding Patterns

1. **rejected(scope-creep) judgment が真の CRITICAL を見逃すアンチパターン**: cycle 30 fix で flow-state-update.sh writer-side legacy fallback を実装した際、cross-session takeover (legacy.session_id != current sid で別 session metadata が silent に保持される) を `rejected(scope-creep)` として承認したが、cycle 31 で Error Handling reviewer が empirical revert test で **CRITICAL silent corruption** と認定。設計判断として「対称化を優先」した tradeoff が、実は data corruption を生む silent regression だった。`rejected` action lines を commit message に書く際、reject した懸念事項自体を reviewer の cross-validation で empirical 検証する gate を持たないと、author の主観判断で CRITICAL 級リスクを silent 通過させる。

2. **AC 解消と同時に新 AC 違反を導入する fix anti-pattern**: cycle 30 fix は AC-4 silent skip を解消したが、同じ修正で cross-session metadata 混合という新規 silent corruption を導入。1 つの finding を fix する diff が別の finding を生む反転パターン (AC trade-off shift)。

3. **Test の dead assertion**: TC-AC-4-WRITER-FALLBACK の 6 sub-assertions のうち `legacy phase updated` は fixture phase と patch phase が同一値で identification power がゼロ。empirical revert test (pre-fix base に対し新 TC 実行) で機械的に検出可能だが、test author が同値設計に気付きにくい failure mode。

4. **Reader/writer 非対称 size handling**: cycle 30 fix が「state-read.sh と完全対称化」を claim したが、reader の `[ ! -s ]` size guard を writer 側に rep していなかった非対称残留。「対称化」claim と実装の strict diff を比較する verification discipline が必要。

5. **canonical example と doctrine の内部矛盾**: bash-trap-patterns.md で canonical 例コメント「return を含めてはならない」と新 doctrine「Form B return 0 必須」が同居する self-introducing drift。doctrine 追加時に既存 canonical example を整合確認する規範。

### Severity Distribution
- CRITICAL: 1 (cross-session metadata 混合)
- HIGH: 1 (size-0 非対称)
- MEDIUM: 4 (canonical 矛盾 / 表記揺れ / dead assertion / E2E TC gap)

