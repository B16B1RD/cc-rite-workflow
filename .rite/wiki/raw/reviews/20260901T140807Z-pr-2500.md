---
type: reviews
source_ref: "pr-2500"
captured_at: "2026-09-01T14:08:07+00:00"
pr_number: 2500
title: "PR #2500 review results (cycle 2)"
ingested: false
---

## Review Results

- **PR**: #2500 — feat(issue-cancel): /rite:issue-cancel を追加し Issue の中止経路を通す
- **Type**: review
- **Reviewed at**: 2026-09-01T23:06:52+09:00
- **Reviewers**: tech-writer, prompt-engineer, code-quality, security, test, application (cycle 2 差分スコープ)

### Finding Patterns

cycle 1 の 9 件は blocking 0 まで解消したが、**cycle 2 の blocking 10 件のうち 7 件は cycle 1 の fix 自身が持ち込んだ**。件数が 9 → 10 でほぼ横ばいなのに収束トレンド判定は `insufficient` を返す。**件数の増減より「新規 blocking のうち何件が直前の fix 由来か」の比率のほうが、ループが収束しているかの信号として鋭い**。

再現した欠陥クラス:

1. **判定表の行に序数で隣接行を参照する形**（5 名が独立検出）。`in_worktree` 行にステップを 1 つ挿入したら、それを「1〜2 をスキップ」と参照していた `in_main` 行の意味が黙って変わり、新設したガードが最も危険な経路から外れた。**ステップ参照は序数ではなく手順名で書く**。さらに根本的には、全経路が通るべきガードを判定表の行に置かず、表の外の無条件前段に置く。
2. **「Bash が展開するのか、Write が literal に消費するのか」を取り違える**。`{reason_file}` を `${TMPDIR:-/tmp}/...` と定義して Write の書き出し先に渡した（展開されない）一方、`git branch -D -- "{branch_name}"` は placeholder の literal substitute なので二重引用符内でも `$(...)` が展開される。**同じ問いを反対方向に間違えた 2 件**。skill に新しくパスや値を書くたび、この 1 問を通す。
3. **pin が「語句の在否」しか見ておらず空振りする**。cycle 1 でこの欠陥を 3 件直したのに、cycle 2 で追加した新規 pin 15 件のうち 4 件が同じ形だった。`BRANCH_CHECK_FAILED` は同一セクションの `REMOTE_BRANCH_CHECK_FAILED` に部分文字列一致し、規則を削除しても反転しても緑。**marker 名の部分文字列関係（`X` ⊂ `REMOTE_X`）は pin の空振り源**。pin は規則の述語まで含めた 1 行 literal にアンカーし、**追加した pin ごとに mutant を作って赤化を確認してから commit する**。
4. **fix が「絞り込めていない」を別の形で再導入する**。`--search "linked:issue:N"`（`:N` が無視される）を退けた代わりに置いた fallback が `--limit 100`（最新 100 件の窓）で、どちらも「絞り込めていないのに成功して見える」同じ defect class。**取得件数が limit と等しいときは「0 件」と結論せず fail-loud で止める**のが、機構を増やさずに済む最小の対処。

### Severity Distribution
- CRITICAL: 1
- HIGH: 5
- MEDIUM: 3
- LOW-MEDIUM: 1

