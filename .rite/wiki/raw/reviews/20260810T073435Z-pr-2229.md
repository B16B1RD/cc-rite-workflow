---
type: reviews
source_ref: "pr-2229"
captured_at: "2026-08-10T07:34:35+00:00"
pr_number: 2229
title: "PR #2229 review results (cycle 2)"
ingested: false
---

## Review Results

- **PR**: #2229 (cycle 2, 差分スコープ)
- **Type**: review
- **Reviewers**: application, error-handling, test, tech-writer, prompt-engineer, security

### Finding Patterns

前 cycle の指摘を直した diff が、**同じ欠陥クラスを別の場所で再生産する**パターンが cycle 2 の指摘 7 件中 4 件を占めた。具体的には (a) 「N 箇所に複製された記述のうち一部だけ直す」— 前 cycle の指摘自体が「4 箇所に複製された誤りを直せ」だったのに、修正で新たに 3 箇所の複製を作り 1 箇所を掃き残した、(b) 「drift を目視で検出する」と宣言した文が、その宣言を追加した commit 自身で drift を発生させた。**複製の同期漏れを指摘されたら、次に問うべきは「同期したか」ではなく「複製を消せるか」** で、今回は値源を単一の定義（実回収集合）へ寄せることで 3 サイトの手続き記述をまとめて消せた。

もう 1 つは **テストの検出力が実装の文字列に暗黙依存する**型（2 件）。`assert_not_grep` による否定 assert は、対応する肯定 assert が同じリテラルを固定していないと、リテラルを書き換えるだけで黙って空振りする。さらに、実装側に「同じ条件式を診断目的で複製する」コードを足すと、ファイル全体 grep の `assert_grep` が診断側の複製で満たされ、強制側を削除しても緑のままになる。**pin は「ファイルのどこかに文字列がある」ではなく「強制している箇所にある」を見るべき**で、区間限定 grep がその手段になる。

散文の事実誤りは cycle 1 に続き cycle 2 でも出た（GraphQL の PR 番号解決）。共通するのは「実装の分岐を 1 段だけ読んで書いた」ことで、3 段ある解決経路の 2 段目までしか辿っていなかった。**防御機構の適用範囲を書くときは、その機構が「適用されない」と書く経路を実際に実行して確認する**のが唯一の防止策だった（今回 3 reviewer が独立に同じ実測で反証した）。

### Severity Distribution
- CRITICAL: 0
- HIGH: 3
- MEDIUM: 4
- LOW: 0

