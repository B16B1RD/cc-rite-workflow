---
type: reviews
source_ref: "pr-2052"
captured_at: "2026-08-02T11:08:23+00:00"
pr_number: 2052
title: "PR #2052 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2052 — docs(wiki-ingest): ステップ 6 の index.md 更新指示を実体の 5 列テーブル形式へ是正する
- **Type**: review
- **Reviewed at**: 2026-08-02T20:07:04+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling, test, security (cycle 2 / fix 後の再レビュー)

### Finding Patterns

cycle 1 の 2 件は解消を実測で確認できた一方、cycle 2 で blocking 3 件が新たに出た。内訳の構造が cycle 1 と異なる。

1. **producer 側の形式変更が consumer 側の silent failure を「普遍化」する**: 本 PR は index.md のカタログ形式を producer 側（テンプレート + ingest 指示）で確定させたが、consumer 側の `/rite:wiki-query` Pass 1 は箇条書き行しか解析せず、候補 0 件時に無診断で `exit 0` する。**2 名の reviewer が独立に同一の欠陥へ到達しながら、処置の scope が `current-pr` と `follow-up` に割れた** — 前者は「診断の追加は読み手対応（別 Issue）とは独立に本 PR で閉じられる」、後者は「Issue が当該ファイルをコメントのみとスコープしている以上リリース順序 gate で対処すべき」。形式変更 PR では「変更した producer」ではなく「変更していない consumer」に欠陥が現れるため、Issue のファイル単位スコープ定義と欠陥の所在がずれる。**scope 軸が割れたときは処置がユーザー判断になる**（rite の `follow-up` は「本 PR では deferred、別 Issue として後続対応」であり `current-pr` と同義ではない）。

2. **fix が導入した防御コードが、その fix と同一 cycle の別 reviewer に指摘される**: cycle 1 以前から存在した `grep -c ... || true` について、code-quality が finding、error-handling と security が推奨事項として**3 名が独立に同じ箇所へ到達**した。理由づけコメント（「rc=1 を吸収するため」）が実挙動と食い違い、実効は rc>=2 の吸収だけで、その経路では空値が同期ゲートを通過して壊れた統計行を書き込む。**「コメントが主張する目的」と「コードの実効」の乖離は複数観点から独立に見つかる** — 逆に言えば、防御コードを足すときはコメントに実効を書くだけで 3 名分のレビュー指摘を予防できる。

3. **実測アンカーの有無が blocking / non-blocking を分け、それが指摘の重要度と一致しない**: cycle 2 の 6 件のうち 3 件が `Verification:` アンカーを持ち blocking、3 件が持たず non-blocking へ降格した。降格した 3 件は散文層の欠陥（placeholder 未束縛 / 手順の走査範囲が未確定）で、runtime 再現手段が構造的に存在しない。**MEDIUM の実装欠陥より HIGH の散文欠陥のほうが merge を止めにくい**という非対称は設計どおりだが、散文が実行契約である rite のようなリポジトリでは記録経路（PR 記録コメント）の実効性が品質を左右する。

### 有効だった検証手法

- **前 cycle の判定を引き継がず再実測する**: cycle 2 の全 reviewer に「前 cycle の判定を無批判に引き継がないこと」を明示した結果、cycle 1 で「解消済み」とされた 4 点はすべて再検証されたうえで、cycle 1 が見落とした非主経路（skip のみのサイクル・孤児ページ更新・重複行の恒久残存）の欠陥が 2 件新たに出た。再レビューを「前回指摘の確認」に縮退させると、この層は永久に見つからない。
- **fix の主張を mutation で追試させる**: cycle 1 の fix がコメントに書いた実測値（`hits 2 -> 1 / skipped 0 -> 1`）を test reviewer が隔離 worktree で 10 変異にわたって再現し、うち M8（テンプレートを develop へ全面 revert）で「既存 assertion は 1 本も落ちず追加した 2 本だけが red」を示した。**fix が塞いだと主張する穴が実在したことと、塞がったことを、同一の実測が同時に証明する**。
- **scope の enum 定義を実ファイルで確認してから処置を決める**: `follow-up` を `current-pr` と同義と読むと、reviewer が「別 Issue へ」と明示した signal が消える。`severity-levels.md` と `_reviewer-base.md` の Scope Assignment を引いて定義を確認したことで、処置の割れが「同じ問題への 2 つの正しい読み」であることが確定し、ユーザー判断へエスカレートする根拠になった。

### Severity Distribution
- CRITICAL: 0
- HIGH: 2
- MEDIUM: 1
- LOW: 0

