---
type: reviews
source_ref: "pr-2130"
captured_at: "2026-08-07T03:22:30+00:00"
pr_number: 2130
title: "PR #2130 review results (cycle 4)"
ingested: true
---

## Review Results

- **PR**: #2130 — fix(pr-review): 8.0.4 positive 検査（本 cycle の結果 JSON 実在確認）を足す
- **Type**: review
- **Reviewed at**: 2026-08-07T12:21:22+09:00
- **Reviewers**: code-quality, error-handling, prompt-engineer, security, tech-writer, test

### Finding Patterns

cycle 4（差分スコープ、fix diff は +43 -15 の 5 ファイル）。cycle 3 の 8 件（blocking 4 + non-blocking 4）は
6 reviewer が独立に検証し、7 件 FIXED / 1 件（F-07）は「行は入ったが内容が誤り」。**新規 blocking 5 件は
すべて cycle 3 の修正そのものが生んだもの**で、以下のパターンが観測された:

- **診断文の「結論は正しいが機構が偽」**: 非収束を告知する 1 行を足したが、原因ディレクトリの
  名指しが実装と食い違っていた（marker は `${TMPDIR}`、結果 JSON の最終保存先は
  `state_root/.rite/review-results`、実際に塞がるのは staging tempfile）。**同じ診断ブロックの 5 行上が
  「探索先」として正しい results dir を自ら印字している**ため、運用者は「この注記は自分に当てはまらない」
  と判断して再実行し、塞ごうとした非収束ループにそのまま入る。3 reviewer が独立に runtime 実測で検出。
  → **文書の 1 行修正であっても、名指しした artifact が実装のどれを指すかは grep で確認する。**

- **新設した assert の錨が、同じ commit が触った構造に打たれる**: `_scrub` を守る T-11f の awk 終端錨を
  fail 診断ブロックの特定行（`^  切り分け:`）に打ったが、**同じ commit がその同じブロックに 1 行足していた** —
  挿入位置が 1 行違えば新 assert 自身が赤になり、しかも失敗ラベルは無関係な原因（制御文字混入）を
  名指しする。前 cycle に「錨は無関係な編集で誤発火しない形に打つ」と自ら指摘して別 pin を直した
  当の commit で、新 arm には同じ原則を適用していなかった。

- **`rc` は「判定が継続した」の代理にならない**: 縮退経路のテストで rc=1 を assert したが、
  分岐を `exit 1` へ退行させても rc は 1 のまま（marker は 1 つも出ない）。rc が同値になる退行が
  存在する以上、pin すべきは rc ではなく**その経路が出すはずの marker そのもの**。

- **除外条項の propagate 先を列挙すると、列挙外が残る**: 前 cycle と同じ 3 重化 drift が
  1 clause ずれて再発（今度は「3 arm すべて」の除外条項が SPEC.md にだけ入らなかった）。
  前 cycle の指摘が propagate 先を 4 箇所と名指しし、fix はその 4 箇所を正確に処理した結果、
  名指しされなかった 5 箇所目が残った。**同一主張の全サイトは grep で数えてから修正する。**

- **コメントが被覆を過大主張する**: 「走査経路 3 leg の `_scrub` を pin する唯一の assert」と書いたが、
  実測で守られているのは 1 leg のみ（残り 2 leg は構造的に LF を運べないため被覆穴ではないが、
  コメントを根拠に残り 2 leg の `_scrub` を外す編集が緑のまま通る）。

### Severity Distribution

- CRITICAL: 0
- HIGH: 1
- MEDIUM: 3
- LOW-MEDIUM: 1
- LOW: 0
- non-blocking (実測なし): 2

