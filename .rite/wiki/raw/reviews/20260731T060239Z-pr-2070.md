---
type: reviews
source_ref: "pr-2070"
captured_at: "2026-07-31T06:02:39+00:00"
pr_number: 2070
title: "PR #2070 review results"
ingested: false
---

## Review Results

- **PR**: #2070 — fix(lint): 説明的番号参照の検出範囲を index.md へ広げ log.md / raw/ の除外を明文化する
- **Type**: review
- **Reviewed at**: 2026-07-31T15:00:58+09:00
- **Reviewers**: application, error-handling, prompt-engineer, test, tech-writer, security

### Finding Patterns

bash helper の走査範囲拡張 PR。6 reviewer 中 4 名が指摘 0 件、2 名 (prompt-engineer / test) が MEDIUM を報告した。

検出された指摘パターン:

1. **兄弟ステップ間の契約記述の非対称** — 同一 SKILL.md 内で並列に並ぶ複数ステップのうち、新たに実行が保証されたステップだけが兄弟の持つ前提条件の一文 (「空 HEREDOC は legitimate」) を欠いた。委譲表現 (「ステップ 4 / 5 / 6.2 と同一」) は、委譲元と委譲先で実際に通る経路が異なる場合に前提を落とす。**経験則: 「他ステップと同一」と書いて済ませる前に、そのステップだけが通る新経路がないかを確認する。**

2. **同一 PR 内で相互に矛盾する 2 つの追加行** — 最上位仕様書 (docs/SPEC.md) に規範文「consumers must accept both forms」を追加しつつ、同じ PR の rationale には「現行形式では consumer が候補を抽出しない」と既知の例外を書いた。読み手は上位文書だけを読んで「全 consumer 対応済み」と誤読する。**経験則: 規範文を追加するときは、同じ PR が別ファイルに書いた例外と突き合わせる。上位文書ほど例外の併記が要る。**

3. **設計判断 (Decision Log) を pin するテストの不在** — D-07「行単位で形式を判別して混在に耐える」に対し、単一形式の fixture は両方あるのに混在 fixture が無かった。判断の存在理由そのもの (移行途中の混在) が未カバーで、行単位判別を弱める変異が全 assert を生き延びる。**経験則: Decision Log に「〜にも耐える」と書いたら、その「〜」を fixture にする。単一ケースの網羅は判断を pin しない。**

4. **実測アンカーの raw pipe による無音降格** — test reviewer は変異注入で実測済みだったが、`Verification:` アンカーの repro に awk regex 由来の `\|` が残り、Anchor detection regex に full match せず non-blocking へ降格した。bash/jq 中心のリポジトリでは repro にパイプが入るのが常態であるため、この降格は頻発しうる。**経験則: repro にパイプを書くときは `¦` (U+00A6) へ置換する。`\|` はエスケープでも検出側の `[^|]` を通らない。**

その他の観察:

- reviewer 6 名中 4 名が独立に「PR 本文の実測値が cycle 途中の値で止まっている」ことを検出した (テスト件数 97 → 実測 144、hits 228 → 実測 230、合計 250 → 実測 252、skipped_rows 1 行 → 実測 0)。**実装を直すたびに PR 本文の実測値が陳腐化する。AC 検証表に数値を書くなら最終 cycle で再実測する。**
- 存在プローブと読出失敗の分離 (`git cat-file -e` / `[ -f ]` を read の前に置く) は、`git show` / `cat` がどちらも非ゼロを返すため rc では区別できないという制約への正しい対処として、error-handling reviewer が実測で検証した。
- partial pollution gate の緩和を「完全一致のみ」に閉じる設計 (prefix へ緩めない) は、security reviewer が近傍 6 変種の実測で迂回不能を確認した。**gate を緩めるときは完全一致に閉じ、`case` のパターンをクォートする。**

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 2
- LOW: 0

