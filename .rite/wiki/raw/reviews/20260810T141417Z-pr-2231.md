---
type: reviews
source_ref: "pr-2231"
captured_at: "2026-08-10T14:14:17+00:00"
pr_number: 2231
title: "PR #2231 review results (cycle 2)"
ingested: true
---

## Review Results

- **PR**: #2231 — fix(pr-review): reviewer 並列起動の直列化を spawn 時刻の機械記録で検出する
- **Type**: review (cycle 2, 差分スコープ)
- **Reviewed at**: 2026-08-10T23:13:11+09:00
- **Reviewers**: application, prompt-engineer, tech-writer

### Finding Patterns

cycle 1 の blocking 2 件は両方 FIXED。だが **cycle 2 の blocking 2 件は、いずれも cycle 1 の fix 自身が導入した欠陥**だった。修正が新しい欠陥を生む比率が 2/2 で、これは「パッチの重ね掛け」が始まる典型的な位置。

型 A: **単純化の主張が例外を持つ**。`jq | awk` を `awk <<< "$var"` へ置き換えて「パイプを外したので total は常に配列の要素数と一致する」と注釈したが、herestring は空文字列にも改行 1 個を供給するため空配列で `total=1` になる。パイプ版は 0 件だった。**修正の rationale として書いた断定が、その修正自身によって falsify される**形。設計選択の根拠を注釈に書くときは、その根拠が成り立たない入力（空・単一要素・境界値）を 1 つは手で当ててから書く。

型 B: **挙動を変えた fix が、自分が変えた挙動の SoT 記述を追随させない**。`elif` を独立 `if` に分離して WARNING が最大 2 行になったのに、docstring の「WARNING 1 行」はそのまま残った。しかも同じ fix が 8 行下に「併記は独立に出す」を追記したため、1 ファイル内で契約記述が自己矛盾した。さらに同 fix が追加したテスト（2 行を pin）と未変更のテスト（1 行を pin）が併存し、ヘッダがどちらを宣言しているか読み手が決定できなくなった。**挙動を変える fix は、その挙動を宣言している箇所を grep で列挙してから着手する**（SoT 指定が別ファイルにあるとき特に落ちやすい — 本件は SKILL.md が「SoT は helper docstring」と宣言していた）。

**レビュー実行そのものの観測**: cycle 1 は reviewer を 1 名ずつ別メッセージで発行して直列化（spawn spread 856s）、cycle 2 は 1 メッセージで並列起動（78s、閾値 120s 内で完全に無言）。同一セッション内で違反と修正の両方が本 PR の検出器で観測された。

### Severity Distribution
- CRITICAL: 0
- HIGH: 0
- MEDIUM: 8
- LOW-MEDIUM: 2

