---
type: reviews
source_ref: "pr-2094"
captured_at: "2026-08-03T02:03:01+00:00"
pr_number: 2094
title: "PR #2094 review results (cycle 4)"
ingested: false
---

## Review Results

- **PR**: #2094
- **Type**: review
- **Reviewed at**: 2026-08-03T11:01:47+09:00
- **Reviewers**: application, error-handling, test, tech-writer, prompt-engineer, security

### Finding Patterns

blocking 5 件。**5 件すべてが前 cycle の fix 由来**で、指摘の生成源が完全に反転した cycle。

**1. 修正ループが自分で指摘を作り始めたら、それは主題の収束を意味する**。PR の主題（carry-forward の正しさ）は 2 cycle 連続で指摘ゼロ — error-handling が疑い所 6 点を全て潰し、security が injection 8+ ベクタで bypass なしを確認、application が `!= "0"` の境界と非退行を確認、prompt-engineer は自らの昇格根拠を反証して撤回。**残っているのは fix ループ自身が持ち込んだ散文・診断・テストラベルの churn** で、コードの振る舞いではない。この区別を報告で潰さないこと。

**2. 「行番号でコメントを書く」は同一コミット内で自己矛盾する**。テストコメントに実装の行番号を書いたが、**同じコミットが header に 8 行・診断コメントに 13 行を足したため、書いた瞬間に参照先がずれていた**。コメントとその参照先を同一コミットで触るときは、行番号が commit 適用後の値である保証がない。semantic anchor（「〜のブロック」「〜の 2 行」）で書く。

**3. テストへの env 追加は kill power を「交換」しうる**。優先順位 1 > 2 を pin するため 2 回目の実行に env override を足したところ、その env がガードを偽にして**既存ファイル値が候補から外れ、優先順位 1 > 3 を守っていた保護が消えた**。1 本のテストで 2 つの優先関係を同時に固定しようとすると、片方の入力がもう片方の前提を壊す。**優先関係ごとに実行を分ける**。

**4. 診断を足したら、その診断を pin するアサーションと出力量の clamp も同時に要る**。`.errors` 添付は (a) 丸ごと外す変異が全 PASS を素通りし、(b) ファイル内容を補間するため 20 万バイトが 1 行で stderr へ流出した。**ファイル由来の文字列を診断へ載せるときは、中和・clamp・アサーションの 3 点セット**。リポジトリに先例（`neutralize_ctrl ¦ head -c 200`）があるなら踏襲する。

**5. 網羅性の断定は撤去した直後でも再入する**。「これが上記 4 ケース以外の唯一の帰結」という文を header に書いたが、これは**同じ PR が前々 cycle で「列挙・断定は後段が 1 つ増えただけで偽になる」として撤去した形式そのもの**だった。しかも「the four」の先行詞が、直前の env 変数リスト（ちょうど 4 個）と数が合って誤解を誘う。**SoT を完結させる作業と、網羅性を断定する作業は別物**。前者は必要、後者は不要。

**6. 収束局面では推奨事項の revert 提案も件数で判断しない**。「診断追加そのものを revert すれば 2 件消える」という選択肢があったが、その診断は他 Issue の値転写が起きたことを示す唯一の信号で、撤去は中立ではなく悪化だった。**件数ではなくコードの是非で選ぶ**。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 3
- LOW-MEDIUM: 1
- LOW: 0

