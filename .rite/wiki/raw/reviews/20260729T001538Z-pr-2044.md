---
type: reviews
source_ref: "pr-2044"
captured_at: "2026-07-29T00:15:38+00:00"
pr_number: 2044
title: "PR #2044 review results (cycle 2)"
ingested: false
---

## Review Results

- **PR**: #2044 — fix(iterate): サーキットブレーカー発火を batch/対話とも機械的停止に統一する
- **Type**: review
- **Reviewed at**: 2026-07-29T09:14:47+09:00
- **Reviewers**: prompt-engineer, tech-writer, code-quality, error-handling
- **Cycle**: 2 (re-review after fix)

### Finding Patterns

cycle 1 の 5 件を修正した後の再レビューで 4 件。うち 1 件は cycle 1 で誰も見つけなかった元コミット由来の機能欠陥で、残り 3 件は cycle 1 の修正が導入した記述・観測性の欠陥だった。

1. **「上限に達している」を「上限に達して停止した」の代用にしない (CRITICAL)**: サーキットブレーカーの発火後再実行 override が `cycle_count >= max` を「発火後」の判定に使っていたが、この状態は**最終 cycle の実行中ずっと成立する通常状態**でもある。ステップ 1 が review を invoke する**前**に counter を書くため、最終 cycle の review/fix 中に Ctrl+C → recover すると、発火していないのに counter がリセットされ上限が無通知で倍増する。**「終端状態」を表す述語は、その終端に到達した事実そのもの (発火時に `max+1` を書く等) で表現し、境界値の一致で代用しない**。境界値は「まさに今その境界を通過中」でも真になる。

2. **同一 PR 内で不変条件を宣言したら、同 PR の他の追加行がそれを破っていないか照合する**: 「両分岐は同構造であり、差は sentinel の消費者だけ」と宣言した同じ PR が、片方の分岐にだけ注意行を足していた。宣言文を書いたら、その PR の diff 全体を宣言に照らして読み直す。

3. **条件付き出力の存在を、後段の指示文が無条件に断定しない**: stderr 退避を追加した際、mktemp 失敗時は診断が出ない縮退経路が残るのに、停止通知は「診断を確認してから再実行」と断定していた。トークン (`RESET=failed-refire`) が診断あり/なしで同一だったため後段が分岐できない。**縮退の有無をトークンに載せる**か、指示文を条件付きにする。

4. **相互排他な 2 規則を隣接段落に並置しない**: 「resume では counter を継続する」と「上限到達時はリセットする」が同じ段落に並び、LLM がどちらを適用するか決定できない状態になっていた。分岐条件を状態語で一意に書く。

補足: cycle 1 の指摘 5 件はすべて正しく修正され、tech-writer は cycle 2 で指摘 0 件 (可) となった。cycle 2 の新規指摘は「修正が正しいか」ではなく「修正が導入した新しい面」に集中しており、re-review をフルレビューで回す (スコープ縮退しない) 価値が実証された形になっている。

### Severity Distribution
- CRITICAL: 1
- HIGH: 0
- MEDIUM: 3
- LOW: 0

