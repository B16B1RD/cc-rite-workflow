---
type: reviews
source_ref: "pr-3060"
captured_at: "2026-09-24T21:20:15+00:00"
pr_number: 3060
title: "fix(hooks): レビュー後の base 取り込みを検証済みの 1 経路に揃える（レビュー結果）"
ingested: false
---

## Review Results

- **PR**: #3060
- **Cycle**: 2 (incremental)
- **Reviewed commit**: dfb66279d
- **Assessment**: fix-needed (blocking 4、non-blocking 5)、停滞診断 replan（作業時間）

### Findings Summary
- 前回の CRITICAL（取り込み相手を base に結び付けない）と cleanup 誤拒否は解消。
- 自前の shell tokenizer に置き換えたことで、二重引用内のコマンド置換（`x="$(git commit ...)"`）が検出から漏れた。旧 shlex 実装は parse error で fail-closed に止めていた。
- git の long option は一意な前方一致の省略形を受け付けるため、完全一致での後勝ち判定は省略形で迂回される。
- 設定未設定時に既定値へ倒す読み取りは、制約を外す判定の基準に使うと fail-loud 原則に反する。

### Patterns
- 検査用のシェル字句解析を自作すると、入れ子（引用内のコマンド置換）と既定動作（オプション省略形）の取りこぼしが続く。判断できない形は分類後に fail-closed で止める方が収束する。
- 制約を外す判定の基準値（base 名など）は既定値を持たせず、未設定なら止める。

