---
type: reviews
source_ref: "pr-750"
captured_at: "2026-04-30T07:42:21+00:00"
pr_number: 750
title: "PR #750 review results"
ingested: false
---

## Review Results

- **PR**: #750 — refactor(hooks): #681 tool 系 2 hooks + session-ownership helper の自 session state 参照対応
- **Type**: review
- **Reviewers**: code-quality / error-handling / security / test

### Finding Patterns

PR review (8 findings, 3 HIGH / 2 MEDIUM / 2 LOW). 主要パターン:

1. **Silent legacy fallback 経路の観測性欠如** (HIGH x2): `_resolve-flow-state-path.sh` 失敗時の `2>/dev/null || FALLBACK="<legacy>"` 形式が deploy regression の silent failure 経路を作る。5 caller 全てが同じ形式で stderr を捨てている。観測性向上の手段 (RITE_DEBUG ログ / workflow-incident-emit.sh 連携) が design の next step。

2. **Schema-2 fast-path API contract enforcement 不在** (HIGH): per-session file 構造による「ファイル存在 = 自 session」を構造的保証として扱うが、code-level で SID 一致 verification を行わない。defense-in-depth として `basename` 抽出 + hook_sid 比較を追加すれば落ちる。

3. **AC-4 integration test 欠落** (HIGH): session_id mismatch hook no-op を直接 verify する TC が tool 系 hook test に欠落。helper unit test のみでは architectural assumption の崩壊検出不能。

### Lessons

- 兄弟 Issue (#680 lifecycle / #681 tool) の対称適用は対称化 doctrine (Wiki: Asymmetric Fix Transcription) の好例。3 ファイルで同形 path resolver 切替を一貫適用できた。
- 一方で「対称適用したから OK」とすると、各 caller の context (Pattern 5 Mode B / post-hook ownership) の差異を見落とすリスク。defense-in-depth の追加は caller ごとに必要。

