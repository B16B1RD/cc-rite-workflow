---
type: reviews
source_ref: "pr-2130"
captured_at: "2026-08-07T05:27:00+00:00"
pr_number: 2130
title: "PR #2130 review results (run2 cycle2)"
ingested: false
---

## Review Results

- **PR**: #2130 — fix(pr-review): 8.0.4 positive 検査（本 cycle の結果 JSON 実在確認）を足す
- **Type**: review
- **Reviewed at**: 2026-08-07T14:25:59+09:00
- **Reviewers**: code-quality（API エラーで途中終了）, prompt-engineer, security, tech-writer, test

### Finding Patterns

- **【最重要】「修正した」と commit body に書いた項目が diff に着地していなかった**（4 reviewer が独立検出）。
  原因は fix 側のスクリプト構成: 独立した 2 件の編集を 1 つの Python スクリプトに入れ、後段の
  `assert old in s` が落ちて例外終了したため、**前段で in-memory に完了していた編集も
  `open(p,'w')` に到達せず破棄された**。fix 側は traceback を見て後段の対象文字列だけを直し、
  前段の再適用を忘れた。教訓は 2 つ:
  1. **独立した編集を 1 スクリプトに束ねない**（片方の失敗が他方を巻き添えにする）
  2. **fix 報告の各項目を `git diff` の hunk と 1:1 で突き合わせる** — レビュー側も
     commit body を信頼せず diff を読む必要がある

- **新設した assert が「掲げた契約」に到達しない**（4 reviewer が独立検出）。`shift; shift` の
  ループ hardening を測るはずの arm が未知オプションを渡しており、`*)` arm で即 `exit 2` して
  `shift` を一度も実行しない。**同ファイルのヘッダ規約自身が「値なしフラグはいずれも required
  value を空にする」と宣言しているのに、追加した arm だけがそれを外れていた。**
  前 cycle の「assert 名と fixture の乖離」と同型で、fix が同じパターンを再生産した。

- **gate の帰結記述が実測と逆、が declared SoT 側に入った**。前 cycle で SKILL.md の同型誤り
  （「値源を取り違えると gate が落ちる」→ 実際は誤 pass）を直した当の fix が、今度は
  `review-result-schema.md`（SKILL.md が「真実の源」と宣言している側）へ
  「7 桁未満は fail 側へ倒れる」と書いた。実測では `--commit-sha` 側は **degraded (exit 0)**。
  **fail / degraded の境界は、書くたびにオペランド別に実測すること。**

- **限定句を足すと、その限定句が別経路と矛盾する**。「`saved=false` で到達するのは HEAD 不変の
  cycle **だけ**」は、positive 層が degraded に降りた cycle を見落としている。同じ節が
  degraded の続行を明記しているため節内で自己矛盾する。

- **carve-out の伝播漏れ**: 前 cycle で差し戻し先の carve-out を Pre-Check 層に入れたが、
  同一節の prose 層 On ERROR には入らなかった。**同一 failure mode に対する指示が節内に
  複数あるときは、grep で全数を数えてから直す。**

### Severity Distribution

- CRITICAL: 0
- HIGH: 0
- MEDIUM: 4
- LOW: 3（うち 2 件は nit-noted）
- non-blocking (実測なし): 1

