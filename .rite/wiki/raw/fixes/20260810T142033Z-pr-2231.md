---
type: fixes
source_ref: "pr-2231"
captured_at: "2026-08-10T14:20:33+00:00"
pr_number: 2231
title: "PR #2231 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #2231
- **Type**: fix (cycle 2)
- **Fixed at**: 2026-08-10T23:20:00+09:00

### Fix Patterns

cycle 2 の blocking 2 件は **どちらも cycle 1 の fix 自身が導入した欠陥**だった。修正が新しい欠陥を生む比率 2/2 は「パッチの重ね掛け」が始まる位置で、ここで追加パッチを既定にすると次 cycle も同じ比率で再生産される。

**この位置で効いた対処は「機構ごと単純化」**。cycle 2 の 2 件（marker が WARNING より先に出る経路 / docstring の「WARNING 1 行」が実挙動と食い違う）は、出力ブロックを個別に patch すると分岐が増えて説明も例外付きになる。代わりに **marker を判定変数へ畳んで最後に 1 本だけ出す**形へ書き換えると、(a) ほぼ同一の emit 行 2 つが 1 つになり、(b) marker が WARNING より先に出る経路が構造的に消え、(c) docstring が例外なしの 1 文で書けるようになった。3 つの指摘が 1 つの単純化で同時に消える。

**再発しやすい 2 型**（cycle 1 の教訓の続き）:

型 A の続き: **単純化の rationale を注釈に書くとき、その根拠が成り立たない入力を 1 つは手で当てる**。`jq | awk` → `awk <<< "$var"` の置換で「パイプを外したので total は常に配列の要素数と一致する」と書いたが、here-string は空文字列にも改行 1 個を供給するため空配列で `total=1` になり、注釈自身が 1 コマンドで falsify された。空・単一要素・境界値を当てる習慣が要る。`printf '%s' "$var" | awk` なら空入力が 0 レコードになり、特例そのものが消える（左辺が jq でないので rc 喪失の再導入にもならない）。

型 B の続き: **挙動を変える fix は、その挙動を宣言している箇所を grep で列挙してから着手する**。SoT 指定が別ファイルにあるとき特に落ちる（本件は SKILL.md が「SoT は helper docstring」と宣言していた）。同じ fix が追加したテストが新挙動を pin し、未変更のテストが旧挙動を pin する状態は、ヘッダがどちらを宣言しているか読み手が決定できなくなる形で表面化する。

**コメントの配置**: marker 値の enum に説明注記を同じインデント列で挟むと、実在しない 4 番目の値として読まれる。列挙の中に散文を入れない。

### Statistics
- Total findings: 10
- Fixed: 2 (+ 隣接する 1 件を同一単純化で解消)
- Replied: 0

