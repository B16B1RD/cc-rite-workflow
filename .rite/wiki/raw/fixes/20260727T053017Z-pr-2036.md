---
type: fixes
source_ref: "pr-2036"
captured_at: "2026-07-27T05:30:17+00:00"
pr_number: 2036
title: "PR #2036 fix results (cycle 2)"
ingested: false
---

## Fix Results

- **PR**: #2036
- **Type**: fix
- **Fixed at**: 2026-07-27 (cycle 2)

### Fix Patterns

cycle 2 で 12 件を修正。中核は **「記録する」と宣言した先が、既定設定と出力最適化の重なりで実際には到達不能だった** というパターン。

- **「破棄しない」を保証するなら、記録先は永続チャネルに置く**: 非実測指摘の記録先を「統合レポートの section」1 本に置いたが、(a) 既定 `post_comment: false` で PR コメントが投稿されない、(b) E2E で統合レポート出力が minimize される、(c) 永続 JSON からは除外契約で落ちる、の 3 つが重なって記録がゼロになった。表示専用チャネルは「記録した」の根拠にならない。**唯一の永続成果物 (ローカル JSON) に additive 配列として載せる**のが最小かつ確実な修正だった。
- **除外契約を書くときは「どこに残るか」を同時に書く**: `findings[]` から除外する契約だけを明記し、代替の記録先を書かなかったことが穴の直接原因。除外と保存はセットで規定する。
- **存在判定に「値」を含めると検出層に穴が空く**: 「マーカーはあるが形式が壊れている」を検出する 2 段判定で、stage 1 に `Verification:` だけでなく種別キーワード (`repro|failing_test`) まで含めたため、**ラベルを取り違えたケースが両段から外れて無音降格**した。検出層の存在判定は bare marker で行い、値の検証は stage 2 に任せる。false-positive WARNING が増えるコストより silent false-negative の方が高い。
- **sandbox 環境では `git status --porcelain` を直接使わない**: write-block bind mount が `??` エントリとして必ず現れるため raw 版は恒に非空になり、「working tree が clean なら skip」型のガードが**一度も発火しない**。プロジェクトに専用 helper (`git-status-filtered.sh`) がある場合、既存の使用箇所 (`post-review-state-verify.sh` 等) を grep して convention を確認してから書く。
- **file:line を key にする map は、同じ位置にある別出自のデータを巻き添えにする**: rite finding 由来の non-blocking 分類が、同一 file:line にある人間レビュアーの thread を無音で「対応済み」に計上していた。key の衝突可能性がある map では、出自の確認を分岐条件に入れるか、判定不能時は安全側 (blocking) に倒す。
- **同一 rationale が doc と script コメントの両方にあるとき、doc だけ直すと script 側が正反対の記述として残る**: 「将来の consumer」「`// false` が正準」という script コメントが、doc 側で新設した禁止規則と正面衝突した。rationale の写しは grep して同期する。

### Statistics
- Total findings: 12
- Fixed: 12
- Replied: 0

