---
type: fixes
source_ref: "pr-2095"
captured_at: "2026-08-03T11:40:17+00:00"
pr_number: 2095
title: "PR #2095 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #2095
- **Type**: fix
- **Fixed at**: 2026-08-03T20:45:00+09:00

### Fix Patterns

cycle 2。前 cycle の fix が導入した記述に blocking 指摘が集中した典型例と、その脱出手順。

1. **helper の多ケース挙動を散文で記述しようとすると必ず誤る**: cycle 1 は「アンカー仕様を引用した指摘がどう分類されるか」を 1 文に潰して 3 ファイルへ複製した。実測すると帰結は 5 通りに分岐し、しかも句読点の位置という文書では表現しづらい要因で割れる。記述で追随するのではなく、**常に成立する 1 つの authoring 規則**（marker 文字列を書かない）へ畳むのが正解だった。多ケースの帰結説明は SoT 1 箇所に委ね、他所では再掲しない。

2. **escalation trigger は実際に効く**: fix.md の simplification-first「前 cycle の fix が導入した箇所への指摘は、機構ごと削除・単純化を先に検討する」に従ったところ、3 ファイルすべてで net-negative（削除 + 短い置換）になり、develop からの net diff は元の +84/-1 に戻った。追加パッチを重ねていれば cycle 3 でその追加物が新たな指摘対象面になっていた。

3. **同一 PR 内で「A を直したが同型の B を直していない」は次 cycle で必ず出る**: cycle 1 で PR #2070 を「実例として不適格」と判定して除去したが、対比のもう一方（PR #2052）に同じ判定基準を適用しなかった。cycle 2 で両 reviewer が独立に検出した。判定基準を 1 箇所に適用したら、同じ基準が当たる箇所を全部 grep すること。

4. **reviewer 間で repro が食い違ったら平均せず解決する**: 同一 fixture 名で reviewer と自分の実測結果が逆になった。原因は句点の位置（marker 直後 / LHS 内側）で、両方が正しかった。食い違いを「どちらかが間違い」と処理せず、差分を特定すると規則の曖昧さそのものが露出する。

### Statistics
- Total findings: 7
- Fixed: 4
- Replied: 0
- Non-blocking (measured gate): 3

