---
type: fixes
source_ref: "pr-2227"
captured_at: "2026-08-10T04:33:20+00:00"
pr_number: 2227
title: "PR #2227 fix results (cycle 2)"
ingested: false
---

## Fix Results

- **PR**: #2227 (cycle 2)
- **Type**: fix
- **Fixed at**: 2026-08-10

### Fix Patterns

診断メッセージの改名で原因を断定してしまう型。「この変数に中身が入るのは X のときだけ」という前提でラベルを書き換えると、その前提が helper 側の実装に依存している場合に壊れる。今回は helper が set -euo pipefail 下で stderr 未捕捉の jq を複数持つため、exec 成功後の runtime abort でも同じチャネルに中身が入った。原因を断定するラベルへ変える前に、そのチャネルへ何が流れ込みうるかを helper 実装まで遡る。遡れないなら中立な表現のままにする方が安全で、情報量も減らない。

指摘を読んで直すときに、指摘本文が使っていた行番号をそのまま成果物のコメントへ転記しない型。fix が数行挿入するだけで同一コミット内で指し先がズレる。対象は構造名（どの echo 文か、どの関数か）で指す。

### Statistics
- Total findings: 5
- Fixed: 2
- Replied: 0

