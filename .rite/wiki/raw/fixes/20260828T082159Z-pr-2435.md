---
type: fixes
source_ref: "pr-2435"
captured_at: "2026-08-28T08:21:59+00:00"
pr_number: 2435
title: "PR #2435 fix results"
ingested: false
---

## Fix Results

- **PR**: #2435
- **Type**: fix
- **Fixed at**: 2026-08-28T08:20:00Z

### Fix Patterns
skip 権威ファイルは setup 生成の root .gitignore に頼らず、書込直前に既存 `_ensure_dir_gitignore` で対象ディレクトリへ sidecar `*` を置く。会話 marker は観測用に留め、skip 判定は永続ファイル存在のみにする。汎用 sentinel 表と専用経路が同じ sentinel を別の次ステップに送るときは、汎用表側に専用経路例外を 1 行足す。契約テストは極性（`if [ -f` 対 `if [ ! -f`）と write-fail 枝の rm を一意 pin し、gitignore は `git check-ignore -q` rc=0 で pin する。

### Statistics
- Total findings: 5
- Fixed: 5
- Replied: 0

