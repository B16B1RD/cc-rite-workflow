---
type: fixes
source_ref: "pr-688"
captured_at: "2026-04-28T11:10:28+00:00"
pr_number: 688
title: "PR #688 fix results (cycle 12)"
ingested: false
---

## Fix Results

- **PR**: #688
- **Type**: fix
- **Fixed at**: 2026-04-28T19:58:00+09:00
- **Cycle**: 12 (累積 14 回目 38+ cycle 中)

### Fix Patterns

cycle 12 で確立された fix patterns (7 findings 全修正):

1. **HIGH F-01 (tech-writer)**: workflow-incident-emit-protocol.md Scope 表で `commands/lint.md` を out-of-scope と誤記載していた事実誤認を 2 行化で訂正。cycle 43 F-05 commit message では grep 対象 path が `commands/wiki/lint.md` のみで `commands/lint.md` (rite lint) を見落としていた。Wiki 経験則「Design doc は現 HEAD の SoT を verify してから書く」の同型違反 (ファイル名衝突パスの取り違え)。本 cycle で `find -name` 全候補列挙→対象選択 doctrine を Revision Log に追記し、再発防止。

2. **MEDIUM F-02 (code-quality)**: flow-state-update.sh L181/L220 の hardcoded 行番号 `line 376 area` (実際は L414-417) を `_rite_flow_state_atomic_cleanup` 関数名 anchor に置換。同 PR cycle 43 F-02 で確立した「semantic name 参照」原則の自己メタドリフト fractal を解消。subshell isolation により実害なしだが、コメントが maintainer の認知モデルを歪める経路を遮断。

3. **MEDIUM F-03 (code-quality)**: resume-active-flag-restore.sh L55-62 の inline `for _helper in ...` ループを `_validate-helpers.sh` 呼び出しに置換。3 caller のうち 2 つが新 helper を使い 1 つが旧 inline pattern を残す migration 取り残しを解消。`_validate-helpers.sh` 導入の核心理由 (drift 防止) を 3 caller 全てで一貫化。

4. **MEDIUM F-04 (code-quality, documentation)**: state-read.sh L62 / flow-state-update.sh L67 のコメント overstate を訂正。「helper を 1 つ追加する際に 2 箇所更新が不要になり」と書いていたが、実際には helper 名 list (両ファイルに 7 entry × 2 箇所ハードコード重複) の同期更新は依然必要。集約されたのは validation logic (4 行 if/echo/exit) のみ。「片肺更新は構造的に防がれた」誤解を解消し、Issue #687 root cause と同型の drift 再発許容経路を文書レベルで予防。真の DRY (DEFAULT_HELPERS 配列内蔵) は将来 Issue で検討。

5. **LOW F-05 (security Hypothetical)**: _resolve-session-id-from-file.sh STATE_ROOT validation を `*..*|*'$'*` から `*..*|*'$'*|*'`'*` に拡張し、backtick (command substitution 誘発) を reject。さらに `tr -d '[:cntrl:]'` で newline / NUL / 0x00-0x1F / 0x7F も独立 check で reject する defense-in-depth。Hypothetical (現 caller chain では到達不能) だが将来の direct caller / untrusted source 経路に備える。

6. **LOW F-06 (security, documentation)**: flow-state-update.sh L431-439 のコメント「mktemp で umask 022 環境では 644 で作成」が GNU coreutils mktemp 実装 (man mktemp: "The file is created with mode 0600") と矛盾する factual error を訂正。実機検証 (umask 022; mktemp /tmp/test → -rw-------) で確認。chmod 600 は OS デフォルト 600 を冗長に強制する形だが、defense-in-depth (将来 mktemp 実装変更 / 非 GNU 環境への移植性 / コードレビュー認知負荷の均一化) として保持の意義を明示化。

7. **LOW F-07 (security Hypothetical)**: _emit-cross-session-incident.sh sanitize() の `tr -d '\n\r'` を `tr -d '[:cntrl:]'` (POSIX class) に強化。tab / backspace / form feed / vertical tab / U+007F (DEL) 等の全制御文字を 1 ステップで除去する superset 動作。新規追加コストは 1 文字で既存挙動を完全包含。Phase 5.4.4.1 grep 検出 break 経路への defense-in-depth。

### Statistics
- Total findings: 7
- Fixed: 7
- Replied: 0
- Skipped (separate Issue): 0

### 教訓 (経験則候補)
- 累積対策 PR の cycle 12 でも「同 PR 内に確立した規範が同 PR 内の他箇所に未浸透」という self-meta drift fractal が発火する。cycle 43 F-02 で確立した「semantic name 参照」原則は同 PR の cycle 12 でようやく `_rite_flow_state_atomic_cleanup` 関数名 anchor に置換された (L181/L220)
- DRY 化助手の効果記述には「何が集約され、何が依然分散しているか」の両方を明示する義務がある (overstate は drift 再発を許容する経路となる)
- ファイル名衝突 (lint.md vs wiki/lint.md など) を含む grep evidence は `find -name` で全候補列挙してから対象選択する手順を caller doctrine / Revision Log に記録する
- security Hypothetical findings は exception category のため LOW でも blocking。defense-in-depth として `[:cntrl:]` POSIX class への拡張は新規 1 文字追加コストで現 / 将来の制御文字 vector を包含する経済的修正
- pre-existing dirty files (本 session で修正していない agent / commands / hooks/scripts ファイル) は commit から除外し、scope を限定する (Wiki 経験則「rejected(scope-creep) judgment は cross-validation + empirical revert test で gate」と整合、scope 限定で gate 通過)

