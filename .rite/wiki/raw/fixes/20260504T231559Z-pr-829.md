---
type: fixes
source_ref: "pr-829"
captured_at: "2026-05-04T23:15:59+00:00"
pr_number: 829
title: "PR #829 fix results (13 findings)"
ingested: false
---

## Fix Results

- **PR**: #829
- **Type**: fix
- **Fixed at**: 2026-05-05T08:30:00+09:00

### Fix Patterns

design doc PR で頻出する 4 修正パターンを実測:

1. **数値主張の grep 実測値再生成 (CRITICAL × 1)**: design doc Section 2.2 の Phase 番号列挙が grep 実測と乖離していたため、再現可能な grep コマンド (`grep -oE '^### (Phase )?[0-9]+\.[0-9]+(\.[0-9]+)?' file | sort -u`) を Section 2.2 冒頭に明記し、実測値で全列挙を書き直した。「38 サブセクション」→「42 サブセクション (本 plan スコープ内 37 件)」に修正。今後の plan PR では先行 PR (PR-E0) で `scripts/measure-*.sh` を入れる option を Section 8.3 で確認する。

2. **存在しないファイル参照の find/ls verify (CRITICAL × 2)**: `regression-history.md` (架空) と `run-all.sh` (実在せず) の 2 件は両 reviewer (tech-writer / code-quality) の Cross-Reference verification + find/ls 検証で双方検出。本 plan の S3 で「design doc 内の実コード reference は find/ls で実在 verify」を暗黙の contract として明記した。

3. **同一 PR 内 sibling 表の Asymmetric drift 解消 (HIGH × 2)**: Phase 0.4.x 統合範囲の 4 sibling 表 (Section 3 row 2 / 4.1 / 6.2 row 1 / 8.1 PR-E3) すべてに Phase 0.4.2 を含めた。Section 3.1 の references 列挙に `sub-skill-handoff-contract.md` を追加し「7 ファイル」claim と一致させた。Wiki 経験則「Asymmetric Fix Transcription」が plan layer (実コード以外) でも有効。

4. **NFR-6 違反の表現格上げ (MEDIUM × 1)**: Section 5.1 / 5.2 で「緊張関係」を「明確な NFR-6 違反」に格上げし、代替案 A/B/C を提示。許容判断は PR-E4 で SoT 化する手順を Section 8.1 検証手順 (d) に追加した。

### Statistics

- Total findings: 13
- Fixed: 13
- Replied: 0
- Skipped (separate Issue): 0

