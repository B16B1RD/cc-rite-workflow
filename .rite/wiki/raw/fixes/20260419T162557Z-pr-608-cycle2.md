---
type: fixes
source_ref: "pr-608-cycle2"
captured_at: "2026-04-19T16:25:57+00:00"
pr_number: 608
title: "PR #608 fix results (cycle 2)"
ingested: false
---

## Fix Results (cycle 2)

- **PR**: #608
- **Type**: fix (re-review cycle 2)
- **Fixed at**: 2026-04-20T00:55:00+09:00

### Fix Patterns

#### 1. Markdown code fence 閉じ忘れの propagation (CRITICAL × 3)
- 前 fix commit 5993a54 で `if ! ... fi` ブロックを既存閉じフェンスの直前に挿入する際、`fi` 行の後に独立した閉じフェンス ` ``` ` が必要であることを 3 箇所すべて見落とした
- 結果として cleanup.md の bash code fence count が 144 (偶数、balanced) → 153 (奇数、UNBALANCED) になり、後続の散文 + Skill 呼び出し指示が bash code として誤解釈される構造的バグになった
- **教訓**: bash block 末尾に新規 statement を追加する際は **commit 前に必ず `awk '/^```/{c++} END{print c}' file.md` で fence balance を検証する**。LLM のレビューが Markdown レンダリング破綻を CRITICAL として検出可能 (本 cycle で実証)

#### 2. 削除済み phase 名への参照残置 (HIGH)
- F3 (cycle 1) で `ingest_completed` を whitelist + stop-guard から YAGNI 削除したが、ingest.md L1026 に「Optional, 単独実行時のみ flow-state を `ingest_completed` に deactivate する」という残置文があった
- L107 / L1028 / whitelist NOTE のすべてが「ingest.md does NOT call flow-state-update.sh at all」と宣言しているのに、Step 1 のみ deactivate 指示が残っており LLM が grep 追跡で「whitelist 登録漏れか?」と誤推論する原因に
- **教訓**: phase 名 / 変数名 / 定数の rename / 削除は code 側 (whitelist + stop-guard) と doc 側 (caller md / step 説明) を grep 全リポジトリで対称的に検証する。命名と説明の drift は HIGH の文書バグに昇格する

#### 3. HINT 文言の section 名不整合 (MEDIUM)
- stop-guard.sh L289 cleanup HINT が「Phase 0 was just activated」と表現するが、cleanup.md 実体は L95 で `## Phase 1.0: Activate Flow State` と命名されている
- LLM が HINT を読んで cleanup.md を grep しても "Phase 0" section が見つからず、継続 step を誤認する経路があった
- **教訓**: stop-guard HINT 内の section 名参照は caller md 実体の section 名と完全一致させる。create lifecycle HINT の `Phase 0.6` / `Mandatory After Delegation Step 2` 等の固有名 parity に揃える

#### 4. test の cross-TC 暗黙依存 (LOW × 2)
- TC-608-A が grep specificity 低 (`Phase 0` で他 phase HINT と prefix 衝突) + `fresh_ts` を TC-475-A 定義に依存
- **修正パターン**: grep を `/rite:pr:cleanup Phase 1.0` (cleanup-specific) に変更 + `fresh_ts` を defensive 再代入で TC isolation 確保
- **教訓**: 新規 TC 追加時、既存 TC の helper 変数や曖昧な grep target に依存しないこと。set -u 環境で TC 削除/移動が連鎖破壊しない isolation を保つ

#### 5. session-end fallback の glob 統一 (LOW)
- create_* は `[[ "$_state_phase" == create_* ]] && [ "$_state_phase" != "create_completed" ]` の glob pattern、cleanup_* は `[ "$_state_phase" = "cleanup" ] || ...` の explicit 列挙で非対称
- 新規 cleanup_* phase 追加時に fallback path が silent に古い list で動作する経路を `cleanup*` glob 統一で解消

### Statistics
- Total findings: 10 (CRITICAL 3 + HIGH 1 + MEDIUM 1 + LOW 5)
- Fixed: 9
- Replied: 0
- Skipped: 1 (TC-608-D 補強、TC-475-D parity の意図的最小設計)
- Confidence override: 0
- Markdown structure verification: cleanup.md fence count 153 (UNBALANCED) → 156 (balanced)
- Test result: stop-guard.test.sh 29 passed (回帰ゼロ)

