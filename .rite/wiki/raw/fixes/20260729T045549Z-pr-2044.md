---
type: fixes
source_ref: "pr-2044"
captured_at: "2026-07-29T04:55:49+00:00"
pr_number: 2044
title: "PR #2044 fix results (cycle 3)"
ingested: false
---

## Fix Results

- **PR**: #2044
- **Type**: fix (cycle 3)
- **Fixed at**: 2026-07-29T14:00:00+09:00

### Fix Patterns

cycle 3 は**コード変更ゼロ、散文のみ 24 行**の修正だった。3 件の blocking 指摘はいずれも「cycle 2 の修正が何をしたか」の記述精度の問題で、パターンは 2 つ。

1. **縮小を消滅として書いた**: 中断窓を「review invoke + ステップ 6 全体」から「共有前段 → sentinel 出力」へ狭める修正をしたのに、散文 5 箇所で「そういう経路を持たない」と無条件に断定した。**教訓: 窓を狭める修正をしたら、残った窓の大きさを書く。「閉じた」と書けるのは残存窓がゼロだと示せるときだけ。** 縮小と消滅を書き分けないと、後続の実装者が「ここはもう安全」と読んで別の変更で窓を広げる。あわせて「完全な閉塞には何が必要で、なぜ今やらないか」（今回は Non-Target の hook 改修が要る）まで書くと、別 Issue 化の判断材料が残る。

2. **同一 API の default 挙動を、呼び出しごとに書き下さなかった**: `flow-state.sh set` は `--handoff` を伴わないと handoff を default-clear する。fire 分岐についてはこれを正しくモデル化していたのに、同じ commit で新設した共有前段の set については「再セットしない」（受動）としか書かず「クリアする」（能動）に踏み込まなかった。結果、停止通知が「counter reset に失敗＝handoff クリアにも失敗」という成立しない連言を人間に断定していた。**教訓: default 挙動を持つ API を複数箇所で呼ぶなら、各呼び出しについて default が何をするかを個別に書き下す。1 箇所で正しく書けたことは他の箇所の保証にならない。** 修正の副産物として、consumer を持たなかった marker (`HANDOFF_CLEAR`) に条件分岐という consumer が付いた — **未消費 marker は「まだ書かれていない条件がある」ことの兆候として使える。**

### Verification Approach

実 helper を隔離 `RITE_STATE_ROOT` 上で走らせ、「fire 分岐の set を skip して共有前段の set だけ実行する」という**片方の失敗を模擬した最小手順**で handoff キーの消滅を確認した。stub を書かずに実 helper を隔離環境で回すほうが、default 挙動の検証としては証拠力が強い（stub は自分の理解をコピーしたものなので、理解が誤っていれば stub も誤る）。**API の default 挙動を主張の根拠にするときは、stub ではなく実装を隔離環境で走らせる。**

### Convergence Signal

3 cycle を通じて指摘の性質が段階的に浅くなった: cycle 1 は marker の自己矛盾（実装の観測性）、cycle 2 は安全網の実挙動退行（実装の正しさ）、cycle 3 は散文の過大主張のみ（記述の精度、コード指摘ゼロ）。**収束の兆候は「指摘件数」ではなく「指摘がコード層から散文層へ移ったか」で読むほうが早い。** 件数は cycle 2 の 2 件から cycle 3 の 3 件へ増えているが、層は明確に上がっている。

### Statistics
- Total findings: 6
- Fixed: 5 (blocking 3 + non-blocking 2 — うち 1 件は未修正だと blocking 修正が無効化されるため同時対応)
- Non-blocking (記録のみ): 1
- Replied: 0

