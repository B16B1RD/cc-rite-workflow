---
type: fixes
source_ref: "pr-3148"
captured_at: "2026-09-26T11:16:23+00:00"
pr_number: 3148
title: "fix(review): PR が持ち込んだ実測済みの誤動作を重要度 MEDIUM 以下でも同じ PR で直す（修正結果）"
ingested: false
---

## Fix Results

- **PR**: #3148 — fix(review): PR が持ち込んだ実測済みの誤動作を重要度 MEDIUM 以下でも同じ PR で直す
- **Type**: fix (cycle 2, stagnation replan)

### Fix Strategies
- 既存 helper を別工程から呼ぶ block は、helper の「該当なし」だけを正規の分岐とし、それ以外（marker なし・rc 2 / 127・判定不能）は helper の出力を表示して停止する形にした。正常経路では元工程向けの marker を再出力しない。変異（停止の削除、marker の再出力）でテストが落ちることを確認した。
- 停滞診断の replan は保存済み全指摘（non-blocking を含む）の再検討を要求する。現行 triage が non-fatal として移送した指摘でも、review-close が保存済み receipt の current-pr finding を拒否するため、移送では run を閉じられないことを rejection_reason に書いた。
- 修正 commit の body 必須段落を入れ忘れた場合、レビュー済み HEAD に証跡が束縛されているため amend は拒否される。未 push なら `git reset --soft <reviewed HEAD>` で戻して commit し直す。

