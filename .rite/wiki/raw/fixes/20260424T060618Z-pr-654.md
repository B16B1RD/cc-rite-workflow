---
type: fixes
source_ref: "pr-654"
captured_at: "2026-04-24T06:06:18+00:00"
pr_number: 654
title: "PR #654 fix results"
ingested: true
---

## Fix Results

- **PR**: #654
- **Type**: fix
- **Fixed at**: 2026-04-24T15:10:00+09:00

### Fix Patterns

PR #654 review で発見された 1 CRITICAL finding (caller HTML コメント内 bash literal の `; then continue with Phase 0.6 ...` 構文エラー) は、累積 9 件目の declarative 対策 PR が **新たに導入した bash literal の構文有効性を test で検証していなかった** ために merge 直前まで検出されなかった。declarative 文書追加 PR の品質保証ポイント:

1. **literal として LLM に渡すコードは構文有効性を test で検証すること** — `bash -n` 相当の static check が困難な場合は invalid pattern を含まないかの grep で代替可能 (本 PR では `--preserve-error-count[[:space:]]*;[[:space:]]*then[[:space:]]+continue` を NOT-contain で grep)
2. **literal 文字列を散文と混在させる場合、構文区切り (backtick / 括弧) で明示的に分離する** — `bash ... ; then continue` のような shell 文法トークン (`; then`) を散文と隣接配置すると LLM が if 構文の一部と誤解釈する経路がある
3. **DRIFT-CHECK ANCHOR は対称化対象の全 site で同一文言で記載する** — 本 PR では create-interview.md に新規 4-site anchor を追加したが、対称位置の create.md / stop-guard.sh の既存 3-site anchor は更新されておらず drift detector が機能しなかった (review F-03 で指摘)
4. **2-site 内 duplication (同一ファイル内 N 箇所) には grep -cF で count check を入れる** — 1 箇所のみの match で pass する grep は片肺欠落 silent regression を許容する
5. **escalation path の test (error_count=1+) も初回 entry path と同等の sentinel 4 句 grep で覆うこと** — TC-651-A2 で initial entry のみ verify していた問題を補完

### Statistics
- Total findings: 10
- Fixed: 10
- Replied: 0
- Skipped (separate Issue): 0

### 累積系列への寄与

本 PR は累積 9 件目の対策で「declarative 規約強化が新たな bug (構文エラー) を生む」という self-exemplar を実測した cycle になった。対策追加自体が新たな攻撃面を作るリスクを示唆。

