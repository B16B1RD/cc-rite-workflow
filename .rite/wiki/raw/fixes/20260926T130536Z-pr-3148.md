---
type: fixes
source_ref: "pr-3148"
captured_at: "2026-09-26T13:05:36+00:00"
pr_number: 3148
title: "fix(review): PR が持ち込んだ実測済みの誤動作を重要度 MEDIUM 以下でも同じ PR で直す（修正結果）"
ingested: true
---

## Fix Results

- **PR**: #3148 — fix(review): PR が持ち込んだ実測済みの誤動作を重要度 MEDIUM 以下でも同じ PR で直す
- **Type**: fix (cycle 3)

### Fix Patterns
- 複数の入力経路を 1 つの複写処理に合流させるときは、経路ごとの出所の識別子（レビュー対象 commit）と複写処理が探す対象（作業ツリー HEAD）の一致を、全経路共通の前提条件として 1 か所に置く。経路ごとの分岐を足さない。
- 停止条件を AND で書いたら、片側を外す変異でテストが落ちるかを確かめる。常に同時に成立する条件（marker と終了コードなど）は等価変異になるので、重複条件として削除する。

