---
type: fixes
source_ref: "pr-2489"
captured_at: "2026-08-30T14:05:38+00:00"
pr_number: 2489
title: "PR #2489 fix results"
ingested: false
---

## Fix Results

- **PR**: #2489
- **Type**: fix
- **Fixed at**: 2026-08-30T22:57:00+09:00

### Fix Patterns

指摘 8 件のうち 6 件が「同一の主張・同一の集合が複数箇所に literal で書かれていて、片方だけが実装追随から漏れる」という単一の失敗モードに収束した。修正で得られた実行可能な知見は次の 3 点。

- **集合を数詞で要約した散文は、集合を列挙した散文とは別サイトとして pin する**。判定式のループ対象を 5 → 4 変数に縮めたとき、同一ファイル内の「`n_*` 5 種」という数詞要約 2 箇所が更新から漏れた。追加した静的 pin は「散文の列挙 / bash の if / gate ループ」の 3 サイトを相互突合していたが、数詞要約はそのどれにも該当せず機械検知されなかった。3 レビュアーが独立に同一箇所を指摘しており、人間には見えるが pin には見えない典型。
- **1 行ポインタで rationale を退避する構造は、振る舞いを変えたとき本体とアンカー先の両方を直す必要がある**。本体の「informational 指標として完了レポートの内訳にのみ表示する」を実態へ直しても、その `rationale:` 行が指す先に同じ主張が残っていた。ポインタ方式は記述の重複を消すのではなく 1 レベル下へ移すだけなので、修正時の追随対象も 1 レベル下まで及ぶ。
- **集合一致を測るテストで抽出を固定 whitelist の alternation にすると、測っているのは「whitelist ∩ 各サイト の一致」でしかない**。「3 箇所が同一の変数集合を指す」という受入基準に対し、whitelist 外の変数名を 1 箇所にだけ足す変異が全 assert を素通りした。しかも当該変数は residue gate のループ変数リストにも無いため runtime backstop も効かず、整数比較の rc=2 が論理和に飲まれて clean 判定が silent emit される経路が残っていた。抽出を開いた regex (`n_[a-z_]+` 相当) に変え、混入する別項は capture 範囲を絞って落とす形にしたところ、if 条件のみ / 加算式のみ / suffix 改名の 3 変異がいずれも検出されるようになった (修正前は 3 種とも FAIL=0)。

### 補足: 静的 pin だけでは fenced bash の構文健全性を測れない

判定ロジックが継続行を含む if-chain の場合、`; then` を落とす変異は文字列 pin をすべて素通りする。SKILL.md の bash block を抽出して構文検査し実行する型 (本リポジトリでは `remote-branch-delete-guard.test.sh` が先行) を適用すると捕捉できる。抽出テストを書くときは「抽出の破損」と「契約の消失」で診断文面を分けること — 後者を「アンカーが変更された可能性」と報じると、検出すべき退行そのものをテスト側の問題へ誤帰属させる。

### 補足: 両立不可能な修正提案は accept で決着させる

reviewer が提示した修正案 (gate のループ対象に変数を戻す) が、同じ PR で追加した否定 pin と正面衝突するケースがあった。counterfactual を実測して「提案を適用すると既存 assert が 7 件落ちる」ことを確認したうえで、Issue の決定事項と既存の受容済みパターン (同種の変数が以前から gate 対象外かつ literal 展開される) を根拠に accept とした。両立不可能性は議論ではなく実測で示すと、次サイクルでの再訴訟を防げる。

### Statistics
- Total findings: 8
- Fixed: 7
- Replied: 0
- Acknowledged (accept): 1

