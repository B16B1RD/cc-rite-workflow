---
type: fixes
source_ref: "pr-688"
captured_at: "2026-04-26T23:23:16+00:00"
pr_number: 688
title: "PR #688 fix results"
ingested: true
---

## Fix Results

- **PR**: #688
- **Type**: fix
- **Fixed at**: 2026-04-27T08:30:00+09:00

### Fix Patterns

- **set -euo pipefail 下の writer/reader 非対称 pipefail bug**: 同型コードでも `local v=$(...)` (function 内 command substitution) と top-level 直接代入 `v=$(...)` で `set -e` 伝播が異なる。後者は pipeline exit 1 で helper 全体を silent kill する。`|| v=""` での吸収が defensive な canonical pattern。本 PR の writer (`flow-state-update.sh:_resolve_schema_version`) は前者のため偶然 mask されていた。
- **degenerate config の境界ケース**: `flow_state:` セクションは存在するが特定キー (`schema_version:`) が欠落する config は `set -euo pipefail` + grep no-match (exit 1) の組み合わせで silent failure を引き起こす。test では `enabled: true` のみの config で再現。
- **fix scope の限定**: 本 PR の AC-4 完全達成のためには writer 側の defensive 修正も必要だが、scope が膨張するため別 Issue 推奨 (security reviewer の調査推奨に記録)。reader 側だけを explicit guard で対称化する fix が AC-4 scope に最適。

### Statistics
- Total findings: 1
- Fixed: 1
- Replied: 0
- Skipped (separate Issue): 0

