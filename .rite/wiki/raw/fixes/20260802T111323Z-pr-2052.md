---
type: fixes
source_ref: "pr-2052"
captured_at: "2026-08-02T11:13:23+00:00"
pr_number: 2052
title: "PR #2052 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #2052
- **Type**: fix
- **Fixed at**: 2026-08-02T20:20:00+09:00

### Fix Patterns

cycle 2 の 3 件は「修正 1 / accept 2」に分かれた。accept が出たこと自体がこの cycle の主題。

1. **同一欠陥に対し reviewer の scope が割れたら、それはユーザー判断であって統合ではない**: `/rite:wiki-query` Pass 1 の無診断 0 件について、error-handling は `current-pr`（本 PR で fail-loud ガードを入れる）、tech-writer は `follow-up`（Issue にリリース順序 gate を追記し WARNING は別 Issue）と処置を分けた。rite の scope enum では `follow-up` は「本 PR では deferred、別 Issue として後続対応」であり `current-pr` と同義ではない — **両者を「どちらも blocking だから本 PR で直す」と読むと、reviewer が deferred と明示した signal が消える**。Issue #2047 が当該ファイルを「コメントのみ追随」とスコープしていたため、スコープを越えるかどうかはユーザーの領域だった。判断は「#2053 側で対応 + リリース gate」。

2. **accept は「無視」ではなく「処置を伴う deferred」**: 2 件の accept に対し、(a) Issue #2047 へ AC-5（リリース順序 gate: 本 Issue を含むリリースは #2053 マージ後に出す）を追記、(b) fail-loud ガードの follow-up Issue #2086 を起票、という reviewer 自身が推奨した処置を実行した。fingerprint を永続化して次 cycle の再報告を抑止するのは、**処置を取ったうえで**のこと。処置なしの accept は指摘の握り潰しになる。

3. **理由づけコメントが実挙動と乖離した防御コードは複数観点から独立に見つかる**: `grep -c ... || true` について 3 名（code-quality が finding、error-handling と security が推奨事項）が独立に同じ箇所へ到達した。コメントは「rc=1 を吸収するため」と書いていたが、rc=1 でも grep は stdout に "0" を出すので値は壊れず、実効は rc>=2 の吸収だけ。その経路では出力が空になり、直後の同期ゲート（「値を出力した場合のみ同期」）を素通りして壊れた行が書き込まれる。

### 有効だった検証手法

- **2 つの穴を 1 変更で閉じる形を選ぶ**: 指摘は code-quality が「rc を capture して rc>=2 なら skip」、error-handling / security が「BRE の grep を literal 一致へ」と別々の remedy を出した。awk の `index($0, root) == 1` へ置き換えると **rc>=2 の穴と正規表現メタ文字の穴が同時に閉じ、`|| true` 自体が不要になる**（0 件でも `n+0` が 0 を出す）。Simplification-First の観点でも分岐が減る側。
- **happy path 不変を実データで先に確認してから置換する**: 実 wiki worktree（342 ページ）で grep 版と awk 版の内訳が完全一致（90 / 137 / 115）することを確認したうえで edit した。カウント述語の置換は「壊れたケースが直る」だけでなく「正常ケースが変わらない」ことの実測が要る。
- **指摘の repro を修正後に再実行して差分を見せる**: メタ文字を含むパス `a[b/pages/` で、grep 版は空文字、awk 版は正しく 1 を返すことを並べて確認した。「直った」の主張は、指摘が添えた再現条件そのものに対して示す。

### Statistics
- Total findings: 3
- Fixed: 1
- Accepted (認知のみ、処置あり): 2
- Replied: 0

