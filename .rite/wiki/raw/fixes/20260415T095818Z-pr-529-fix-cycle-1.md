---
type: fixes
source_ref: "pr-529-fix-cycle-1"
captured_at: "2026-04-15T09:58:18+00:00"
pr_number: 529
title: "PR #529 fix cycle 1 results"
ingested: false
---

## Fix Results

- **PR**: #529
- **Type**: fix
- **Fixed at**: 2026-04-15T18:55+09:00

### Fix Patterns
- **12 findings 全件を 1 commit で対応**: HIGH 4 / MEDIUM 4 / LOW 4 を 5 ファイルに対する同時 Edit で完了。3 ファイル対称修正 (review.md / fix.md / close.md Phase X.X.W.2) は同一パターンを 3 箇所で write する契約として扱い、1 finding を 3 箇所で適用する方式で drift を避けた。
- **cleanup trap rollback-safety の 2 軸修正**: (1) checkout-back 失敗時の stash pop 無条件実行 → checked_out_wiki=false ガード、(2) signal trap の exit code loss → signal 別 handler で POSIX exit code (130/143/129) を明示渡しし cleanup_body 関数が rc を引数として受け取る設計。両者は同じ cleanup block を触るため衝突回避のために cleanup 関数を `cleanup_body` に rename して明示化した。
- **fallback_sentinel の対称性回復**: trigger Step 3 側の `hook_abnormal_exit` fallback を commit Step 4 側にも対称に追加。Phase 6.5.W.2 / 4.6.W.2 / 4.4.W.2 の 3 箇所すべて適用。同一 fallback pattern を 3 箇所 literal copy で維持する契約。
- **exit 2 semantic preservation**: shell script 側で `exit 2 = legitimate skip` と定義しているのに caller 側が全 non-zero を failure 扱いしていた semantic mismatch を case 文で routing 分離。`WIKI_INGEST_SKIPPED` sentinel を emit することで Phase 5.4.4.1 の false-positive incident を防ぐ。
- **git stderr tempfile 退避**: `2>/dev/null` から `2>"${git_err:-/dev/null}"` + `dump_git_err` helper に置換。副作用として informational message (e.g. `Switched to branch 'wiki'`) も surface されるようになり、これは次 cycle review で LOW 指摘として再検出される可能性がある (悪化しない regression、観測性の側の改善)。

### Statistics
- Total findings: 12
- Fixed: 12
- Replied: 0
- Skipped (separate Issue): 0

### Key learnings (project-specific)
- **shell script の rollback-safety は happy path dogfooding では検証不能**: 本 PR の AC-1/2/3 は happy path 検証だが、HIGH #1 (checkout-back 失敗後の stash pop) と HIGH #2 (signal trap rc loss) は失敗経路 / signal 中断経路でしか発火しない時限爆弾で、empirical テストでは catch 不能。reviewer の静的解析で 2 人が独立検出して初めて可視化された。cleanup trap を書くときは signal 経路の rc を明示的に捕捉すること。
- **trigger 経路と commit 経路の対称性を契約として維持**: 同一ファイル内に trigger と commit の 2 経路がある場合、sentinel emit / trap 登録 / error handling pattern を **対称に書く** ことを契約として明示する。片方だけに `fallback_sentinel` や `commit_err trap` を書くのは silent failure の兆候として reviewer lint 対象にできる。
- **exit code の semantic mismatch は shell script と caller の両側で spec を明示**: shell script header の `Exit codes:` セクションで `2 = legitimate skip` と書いていても、caller が case 文を書かなければ semantic が伝達されず false-positive incident を生む。spec 伝達は片方向では不十分。

