---
type: fixes
source_ref: "pr-636"
captured_at: "2026-04-21T02:56:21+00:00"
pr_number: 636
title: "PR #636 fix results (13 findings resolved)"
ingested: false
---

## Fix Results

- **PR**: #636 — fix(issue-create): create-interview return 後の implicit stop を多層防御で防ぐ (#634)
- **Type**: fix
- **Fixed at**: 2026-04-21T11:55:00+09:00
- **Review cycle**: 1 (self-review via /rite:pr:review 5 reviewers)

### Fix Patterns
13 findings (HIGH 4 / MEDIUM 6 / LOW 3) に対する修正パターン:

1. **Spec drift 修正 (HIGH x2)**: 新 Step 追加時に preamble の範囲指定と行数記述を sync すべし。literal grep 検証では捕捉不能な semantic drift は review で必ず検出する。
2. **Silent failure 経路 close (HIGH x1, MEDIUM x1)**: bash `--if-exists` の silent skip と真の失敗を区別するため `if ! ...; then ... retained flag emit`。silent fallthrough 経路は必ず exit code check で覆う。
3. **Dead marker 削除 (MEDIUM x1、2 reviewer 合意)**: emit されるが consumer 0 件の marker は残さない。INTERVIEW_DONE=1 と MANDATORY_AFTER_INTERVIEW_STEP_0=1 のどちらが必要か cross-validation で収束。
4. **Test defensive pattern drift 修正 (MEDIUM x2)**: cross-TC 独立性のため `fresh_ts="${fresh_ts:-$(date ...)}"` を新規 test でも必ず追加。state 実値検証 (HINT ではなく state file) で silent write failure を catch。
5. **Doc stale reference 解消 (HIGH x1, MEDIUM x2)**: 存在しないスクリプト参照 → Section の inline snippet を one source of truth に。hooks.json 登録経路を plugin.json 経由として正確記述。

### 経験則として残すべき知見
- **累積対策 N 回目 PR ほど drift が多い**: 8 回目の #634 で 13 findings。前対策で残った spec mismatch / dead marker を review で必ず洗い出す
- **Step 追加時の preamble sync は lint 対象**: "Step X-Y" / "N-line block" 等の数値記述は Step 追加時に手動更新が必要で、自動検証の対象外。review で catch するしかない
- **MANDATORY_AFTER_INTERVIEW_STEP_0 のような marker 追加時は 3 点セット**: (1) emit site、(2) consume site (grep 先)、(3) test assertion。同一 PR で揃えない marker は dead
- **Cross-TC 独立性 convention**: `fresh_ts="${fresh_ts:-...}"` defensive fallback は新規 test で drift しやすい。TC テンプレートに含めるのが望ましい

### Statistics
- Total findings: 13
- Fixed: 13
- Replied: 0
- Skipped (separate Issue): 0
- Files changed: 5 (+47 / -19)
- Commit: 7a8ffb9

