---
type: fixes
source_ref: "pr-2094"
captured_at: "2026-08-03T05:26:47+00:00"
pr_number: 2094
title: "PR #2094 fix results (cycle 3)"
ingested: false
---

## Fix Results

- **PR**: #2094
- **Type**: fix
- **Fixed at**: 2026-08-03T14:30:00+09:00
- **Cycle**: 3 (指摘 6 件、うち 4 件が前 cycle の fix で書いたコメント由来)

### Fix Patterns

**本 cycle の主題は「修正に添えたコメントが次 cycle の指摘源になる」問題への対処**。6 件中 4 件が cycle 2 で自分が書いたコメント由来だったため、個別に文言を足す追加パッチではなく、コメントを圧縮して**反証可能な断定そのものを減らす**方向を選んだ。

1. **コメントは機構を語りすぎると次 cycle の検証対象面になる**: C0 全域化の根拠として 2 経路を挙げたところ、うち 1 つ (LF が herestring を切る) が実測で成立せず、さらに文字クラス名 (「C0 全域」) が実射程より狭いと 2 件の指摘になった。4 行を 2 行へ圧縮し、クラス名を書かず観測事実だけを残して根拠をテストへ委ねた。**教訓**: 「なぜこの防御が必要か」を機構レベルで説明しようとすると、検証可能な主張が増えた分だけ指摘も増える。**根拠はコメントではなくテストに置く** (「T-17 / T-18 が固定」と書けば、主張の正しさはテストの Red/Green が保証する)。

2. **行番号アンカーは「導入コミット内で自壊する」ことがある**: テストコメントが hook の行番号を裸で参照していたが、同じコミットが参照先の上に 1 行足したため、コミット時点で既にずれていた。しかも検出機構がファイル名接頭辞を要求する regex だったため、同一文脈の裸 `:NNN` は素通りしていた。**教訓**: 他ファイルを指すときは一意な文字列 (定数名 / メッセージの一部) をアンカーにする。同一コミットで両方のファイルを触る修正では特に危険。

3. **「揃える」で終わらせると複製構造が残り、また割れる**: 前 cycle は片肺の非対称を指摘され literal を揃えたが、複製そのものは残したため同じ drift が再発しうる状態が続いた。今回 local 変数へ 1 度だけ束縛した。**教訓**: 文言の不一致を指摘されたら、揃えるのではなく複製を生む構造を消すところまで行く。

4. **floor guard は「同じ式を共有」しないと変異を殺せない**: sweep 正規表現から腕を落とす回帰を検出する guard を追加したが、初版は正規表現とは独立に site を数えていたため変異を殺せなかった (実測で判明)。正規表現を変数へ一本化して guard が同じ式を見る形へ作り直した。**教訓**: 「X が壊れたことを検出する guard」を書いたら、必ず X を壊す変異で guard が落ちることを実測する。書いただけでは検出しているとは限らない。

### 副産物: pipefail × grep -q の flaky sweep (別 Issue 相当)

parity テストの caller 列挙が `set -o pipefail` 下で `grep -v ... | grep -q ...` を使っており、下流の `grep -q` が早期終了すると上流が SIGPIPE (rc=141) を受けて pipeline が失敗扱いになり、**ファイルが無言で skip される**。200 回中 1 回発生し、スイートの PASS 数が 33-38 で揺れていた。floor guard (`checked >= 24`) が実数 (29-35) より十分低いため masking されていた。**教訓**: `set -o pipefail` 下で `... | grep -q` を書くと、早期終了が SIGPIPE 経由で偽の失敗になる。存在検査は `grep -q` 単体か、パイプなら `|| true` でガードする。floor guard の閾値は実数に近い値にしないと masking する。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1
- MEDIUM: 2
- LOW-MEDIUM: 3
- LOW: 0

