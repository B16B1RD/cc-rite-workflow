---
type: fixes
source_ref: "pr-579"
captured_at: "2026-04-18T12:27:07+00:00"
pr_number: 579
title: "PR #579 fix results"
ingested: true
---

## Fix Results

- **PR**: #579
- **Type**: fix
- **Fixed at**: 2026-04-18T21:30:00+09:00

### Fix Patterns
2 レビュアー (prompt-engineer + code-quality) cross-validation による 3 件の修正:

1. **CRITICAL silent regression 防止**: bash block に placeholder residue gate を追加。
   `[ "{placeholder}" -gt 0 ]` の `integer expression expected` (rc=2) が `set -o pipefail`
   のみで検知できず silent に `else` 分岐に落ちる経路を排除した。既存 5 site (Phase 1.1 /
   1.3 / 6.2 F-01 / 8.3 F-14 / F-04) と対称化する 6 site 目として設置。
2. **DRIFT-CHECK ANCHOR 形式統一**: literal line number `(L1331-1332)` を semantic name 参照
   に置換し、ingest.md Phase 5.0.c の `# >>> DRIFT-CHECK ANCHOR: <name> <<<` 形式 (+ END
   marker) に統一。行番号 drift に対する耐性を獲得。
3. **LLM 内部状態 vs shell 変数の明示**: bash tool 境界越しでカウンタが shell 変数として
   保持されないこと、Phase 8.3 `{log_entry}` と同じ LLM 生成経路であることを comment で明示。

### プロジェクト固有の経験則
- canonical reference として新規 bash block を登録する際は、既存同種 site (この場合 5 site)
  の residue gate 実装パターンとの対称化が必須。drift は silent regression の温床となる。
- DRIFT-CHECK ANCHOR は literal line number ではなく `# >>> DRIFT-CHECK ANCHOR: <semantic name> <<<`
  形式を使用。ingest.md Phase 5.0.c (4 site) が canonical。
- bash block コメントで「蓄積された」と書く際、shell 変数 vs LLM 内部状態の区別を明示する。
  rite の bash tool 呼び出し境界モデルでは shell 変数が保持されない。

### Statistics
- Total findings: 3
- Fixed: 3
- Replied: 0
- Skipped (separate Issue): 0

