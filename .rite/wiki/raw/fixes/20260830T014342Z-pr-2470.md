---
type: fixes
source_ref: "pr-2470"
captured_at: "2026-08-30T01:43:42+00:00"
pr_number: 2470
title: "PR #2470 fix results (cycle 2)"
ingested: false
---

## Fix Results

- **PR**: #2470 (cycle 2)
- **Type**: fix
- **Fixed at**: 2026-08-30T10:45:00+09:00

### Fix Patterns

- **「削除で直す」が既存ガードに抵触したとき、ガードを緩めるより指摘の第 2 案を採る**: 到達不能な `case` 分岐の削除を求められたが、実行すると `skill-rail-diff-check` の subsequence 保存ガード（base の機械レール行は消せない）が落ちた。ガードに除外行を足せば削除は通るが、それは「機構の追加」であり、除外リストは累積する。指摘元の reviewer が併記していた第 2 案（散文を緩めて分岐を保険と位置づける）は散文 1 文の書き換えで済み、ガードに触れない。**削除が原則でも、削除のコストがガードの改変なら、同じ矛盾を解く別の最小手を探す**。

- **fix が新しい分岐・新しい出力を足したら、その分岐に入る fixture を同じ commit で足す**: cycle 1 の修正（marker 正規化行、新設 WARNING 2 本、引数エラー腕の既定値報告）はいずれも削除しても全テストが緑のまま通る状態で残り、cycle 2 で 3 件の指摘として戻ってきた。テストは「正常系の verdict」だけを回しており、fix が足した経路には一度も入っていなかった。**「テストは通った」と「fix が守られている」は別**で、後者は mutation を 1 回かければ即座に分かる。

- **値域を書く箇所がファイル内に 2 つあると、実装に近い方だけが更新される**: sentinel を 1 つ増やしたとき、実装直上の jq コメントは 3 種を正しく列挙したのに、ファイル冒頭の Output 節（marker 契約そのもの）は 2 種のまま残った。4 名の reviewer が独立に検出した。**どちらが SoT かを決め、片方から片方を導出する形にしないと、毎回近い方だけが直る**。

- **同一コミット内の simplification は片側にだけ適用されやすい**: tempfile lib へ移行して 4 つの変数すべてで guard が不要になったが、削除されたのは目に入った 2 つだけだった。**同じ理屈が当たる箇所は grep で洗い出してから消す**。

### Statistics
- Total findings: 6
- Fixed: 6
- Replied: 0 (rite 由来のため reply skip)

