---
type: fixes
source_ref: "pr-2111"
captured_at: "2026-08-04T18:32:50+00:00"
pr_number: 2111
title: "PR #2111 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #2111
- **Type**: fix
- **Fixed at**: 2026-08-05T03:35:00+09:00

### Fix Patterns

cycle 2 の blocking 7 件 + non-blocking 6 件を 1 commit で解消 (a2685d7b)。cycle 1 の fix が導入した欠陥が 2 件 (F-01 injection 面の非対称・F-02 中和の過剰適用) 含まれており、いずれも「防御の粒度が攻撃面とずれる」形だった。

- **防御は攻撃面と同じ粒度で張る**: cycle 1 の詐称対策は title 内の全 `]` を中和したが、詐称は `](pages/…)` の literal 連なりでしか成立しない。過剰な中和は `[CONTEXT]` / `.errors[]` / コードスパン内 `[[:cntrl:]]` を含む正当 title のリンク範囲を壊し、実 Wiki の 3 ページで実害が出ていた (reviewer が `gh api /markdown` で実測)。条件を `](` 限定に絞ると攻撃は閉じたまま回帰が消える。**過剰防御は「安全側」ではなく別の実害**である。
- **委譲で shell 語へ literal 置換する値が増えたら防御を全値へ転記する**: title/description だけ quoted heredoc、他 4 値は double-quote 内へ直接置換という非対称が任意コマンド実行を成立させた。さらに注入コマンド列の rc が複合コマンドの rc になるため **rc=0 + marker 0 行**という「marker 表のどの行にも当たらない状態」が silent success として素通りする — 分岐表には「exit 0 だが marker が出ない」行が要る。
- **散文を畳むと入力正規化ルールが落ちる**: 旧手順の「title / updated / confidence は YAML の引用符を外す」から title だけが脱落し、かつ description の出典が LLM 散文から frontmatter へ移ったため引用符が登録行に固着する経路が開いた。**縮退時は削除文の文単位突合でしか検出できない**。
- **ガードの識別力は「そのガード単独で発火する形状」でしか担保されない**: rc=1 と総称 `grep -q 'ERROR'` だけの assert は兄弟ガード (`-f` と `-r`) に masking され、削除しても全緑で通る。ガード固有の診断文言 (`not found` / `--pages-root was not given`) を assert して初めて識別力が立つ。cycle 1 で TC-13b のコメントに rationale を書きながら同型の TC-13 へ転記しなかったのが再発源。
- **共有 runner の会計契約はローカル summary 再定義で壊れる**: `_test-helpers.sh` の `skip()` を使いながら summary に `, N skipped` 節を持たないと run-tests.sh の skip 会計 cross-check が run 全体を exit 1 にする。3 reviewer が独立検出した。
- **private fork した helper は SoT の進化から silent に drift する**: `_has_c0_del` は共有 `contains_ctrl` のコピーで、共有側が「日本語 TITLE が必要になったら UTF-8 セーフモードを別途追加する」と拡張先を明記していたにもかかわらず fork された。共有側に `--c0-only` モードを足して範囲定義を 1 箇所へ戻すのが正しい解。
- **fix 前 mutation 検証を全修正に適用**: 7 種 (中和条件の全 `]` 化 / 中和削除 / -f ガード / --pages-root 分岐 / placeholder 直渡し / branch_strategy literal / wiki_root 値 / --c0-only range) を隔離 worktree で実行し、各対応 TC が落ちることを commit 前に確認。python heredoc 内の置換が失敗して「生存」に見えた 2 件を sed で再実行して切り分けた — **mutation が適用されたことを置換件数で確認しないと偽の生存判定になる**。

### Statistics
- Total findings: 16
- Fixed: 13
- Replied: 0

