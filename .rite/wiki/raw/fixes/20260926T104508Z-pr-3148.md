---
type: fixes
source_ref: "pr-3148"
captured_at: "2026-09-26T10:45:08+00:00"
pr_number: 3148
title: "fix(review): PR が持ち込んだ実測済みの誤動作を重要度 MEDIUM 以下でも同じ PR で直す（修正結果）"
ingested: true
---

## Fix Results

- **PR**: #3148 — fix(review): PR が持ち込んだ実測済みの誤動作を重要度 MEDIUM 以下でも同じ PR で直す
- **Type**: fix
- **Fixed at**: 2026-09-26

### Fix Strategies
- consumer 側に新しい判定入力（consequence_class）を要求したとき、表示用に最小化された表から JSON を組み立て直す経路（会話・legacy Markdown）ではその入力が必ず失われる。値を既定で補うのではなく、producer が保存した正本（レビュー対象 commit の保存済み JSON）を複写して consumer に渡す形で直した。
- 既存 helper（保存済み JSON の特定）を再利用する際、その helper の診断文が別工程向けの ACTION を含む場合は marker 行だけを残し、誤った復旧指示を利用者に見せない。
- helper が cwd の実 HEAD に照合を固定する仕様だったため、回帰テストは一時 git repo の HEAD を使って組み、別 commit の JSON を借用しないことも固定した。

