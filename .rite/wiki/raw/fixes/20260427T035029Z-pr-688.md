---
type: fixes
source_ref: "pr-688"
captured_at: "2026-04-27T03:50:29+00:00"
pr_number: 688
title: "PR #688 cycle 8 fix results"
ingested: false
---

## Fix Results

- **PR**: #688
- **Type**: fix
- **Cycle**: 8

### Fix Patterns

cycle 7 review で 3-reviewer 独立検出された cross-validated CRITICAL に対する fix。
cycle 6 で「fix-introduced regression を解消」と謳った修正自体が、`if ! bash ... 2>&1 | head -3; then`
の bash pipeline 構文で新たな silent regression を別経路で再導入していた。これは典型的な fix-introduced
bug pattern: 「stderr の trim を目的とした `2>&1 | head -N` は exit code masking と両立しない」。
canonical pattern は「stderr を tmpfile に退避してから head する」(stderr 退避 → if ! cmd 2>"$err" → head 退避)。

加えて、test ファイル間の signal trap drift (state-read.test.sh が flow-state-update.test.sh の cycle 5
cleanup pattern と非対称) を MEDIUM として fix。同 PR 内の同類ファイルは pattern を揃えるべき (drift 排除)。

教訓:
1. bash pipeline + pipefail 未設定下では pipeline 終端の exit code が支配的になる (silent failure)
2. fix の正当性検証では実機 reproduction (今回の review reviewer がやった `bash -c 'if ! (exit 1) ...'`) が決定打
3. cross-validation (3 reviewer 独立検出) は high-confidence finding の最強シグナル — debate 不要で boost

### Statistics
- Total findings: 2
- Fixed: 2
- Replied: 0
- Skipped (separate Issue): 0

