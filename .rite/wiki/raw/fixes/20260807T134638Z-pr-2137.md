---
type: fixes
source_ref: "pr-2137"
captured_at: "2026-08-07T13:46:38+00:00"
pr_number: 2137
title: "PR #2137 fix results"
ingested: false
---

## Fix Results

- **PR**: #2137
- **Type**: fix
- **Fixed at**: 2026-08-07T23:00:00+09:00

### Fix Patterns

**新規 lib を既存 lib 群へ追加するとき、兄弟が採用済みの防御パターンと、それを強制する静的検査への登録が引き継がれない**。本 cycle で修正した F-01 は `hooks/scripts/lib/context-marker.sh` の引数パーサが `shift 2` を使い値なしフラグ末尾で無限ループするもの (実測 rc=124)。同ディレクトリの `worktree-git.sh` は `shift 2 || shift` を、`review-save-json-verify.sh` 等 10 本は `shift; shift` を既に採用しており、専用テスト `shift2-loop-hardening.test.sh` の TC-7 が後者を静的に強制していた。にもかかわらず新規ファイルだけが後退したのは、**TC-7 のリストがファイル名の明示列挙**で、新規ファイルが構造的に死角へ入るため。教訓: 既存の hardening test がファイル名列挙型なら、新規ファイル追加時にリストへの登録要否を必ず確認する。列挙を sweep 型 (`lib/*.sh` 等) にできないかも検討する余地がある。

**入力検証を持つ helper では、拒否経路の診断出力自体が検証対象から漏れる**。F-02 は `marker_emit` が改行入り KEY を rc 1 で拒否しながら、ERROR 文がその KEY を無加工でエコーするため、拒否した出力の中に桁 0 の `[CONTEXT]` 行が現れるもの。`review-save-json-verify.sh` は同じ失敗に対し `_scrub()` を**検査ブロックより前に**定義する規約を持っていたが、本 lib はそこから漏れていた。教訓: 検証を通らなかった値こそが「壊れた値」であり、それを引用する診断は検証と同じ強度で守る。`_scrub` の定義位置が検査ブロックより後だと拒否経路を覆えない点も先例のコメントが明記している。

**修正の identification power は変異で実測する**。本 cycle では 2 種の変異 (引数ガード無効化 + `shift 2` 復元 / `_marker_scrub` 素通し) を入れ、新規 assertion がそれぞれ rc=124 と行数 1 で落ちることを確認した。「テストを足した」だけでは、それが何を守るのか分からない。

**fix 対象は blocking のみ、non-blocking は fix しない**。本 cycle のレビューは 10 指摘のうち blocking 2 件 (実測アンカー付き) / non-blocking 7 件 (実測なし) / nit-noted 1 件。実測必須ゲートにより fix ループへ入るのは 2 件だけで、残りは永続 JSON と PR 記録コメントに残して人間レビューへ委ねた。fix 側が「全部直す」に倒れると、ゲートが分離した意味が消える。

### Statistics
- Total findings: 10
- Fixed: 2
- Replied: 0
- Non-blocking (実測なし、fix 対象外): 7
- nit-noted (scope による受け流し): 1

