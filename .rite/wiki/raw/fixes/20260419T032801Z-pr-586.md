---
type: fixes
source_ref: "pr-586"
captured_at: "2026-04-19T03:28:01+00:00"
pr_number: 586
title: "PR #586 fix results (cycle 4)"
ingested: false
---

## Fix Results

- **PR**: #586
- **Type**: fix
- **Fixed at**: 2026-04-19T12:35:00+09:00

### Fix Patterns

**パターン 1: 自己開発 repo 専用 anchor の consumer distribution 経路漏れ → 発動条件で early skip + 手動追記案内**

- 修正前: Phase 1.3.1 の発動条件は `.rite/wiki/` 行の有無のみ check し anchor 存在を verify しない → consumer project (anchor 不在) で Phase 1.3.3 Edit が `old_string not found` で hard fail
- 修正後: 発動条件 #4 として `grep -qF '# <<< gitignore-wiki-section-end' .gitignore` を追加し、anchor 不在時は `state="skip"; reason="anchor_absent"` で early skip + LLM 分岐テーブルで手動追記案内を表示
- **教訓**: command 指示書が Edit ツールの `old_string` に「自 repo 固有のコメント / anchor」を hardcode するときは、**発動条件側で anchor 存在 check を必ず追加** する。「本 repo で動くから OK」は consumer project 視点で hard fail を生む典型的な dogfooding bias

**パターン 2: 同一ファイル内の確立された規約違反 → canonical 参照実装と一字一句揃える**

- 修正前: Phase 1.3.4 verification で `git add --dry-run ... 2>&1` を採用 (同 init.md L555 の「2>&1 は付けない」規約違反 + canonical script `gitignore-health-check.sh` L270-277 の stderr 退避パターンと drift)
- 修正後: `dry_run_err=$(mktemp ...)` + `2>"${dry_run_err:-/dev/null}"` + 失敗時 head -3 で stderr 別途表示 (canonical script と一字一句揃える)
- **教訓**: 新規 bash block 追加前に同一ファイル内の「類似操作の canonical 例」と「避けるべきパターンの明示コメント」を grep で確認する。canonical reference を明示参照する PR では、参照実装の pattern と drift しない実装にする (経験則「canonical reference 文書のサンプルコードは canonical 実装と一字一句同期する」と同型)

**メタパターン: cycle 1-3 で見落とされた latent design issue が cycle 4 で初検出**

- 1-3 サイクル経過しても reviewer の verification scope に「(a) anchor の consumer-project distribution 経路 check」「(b) 同一ファイル内の既存 project convention check」が含まれず、cycle 4 で初検出
- **教訓**: re-review (verification mode 有効化時も含む) でも初回レビューと同等の網羅性を確保すべき (Anti-Degradation Guardrail 原則と整合)

### Statistics
- Total findings: 2
- Fixed: 2
- Replied: 0
- Skipped (separate Issue): 0

