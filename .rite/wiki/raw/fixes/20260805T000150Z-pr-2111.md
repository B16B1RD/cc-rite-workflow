---
type: fixes
source_ref: "pr-2111"
captured_at: "2026-08-05T00:01:50+00:00"
pr_number: 2111
title: "PR #2111 fix results (cycle 4)"
ingested: false
---

## Fix Results

- **PR**: #2111
- **Type**: fix
- **Fixed at**: 2026-08-05T09:05:00+09:00

### Fix Patterns

cycle 3 の blocking 8 件 + non-blocking 4 件を 1 commit で解消 (24892df6)。`/rite:recover` 経由でレビュー結果を永続化してから再開した cycle。

- **防御の「次の層」は同じ機構の中に残る**: cycle 2 で quoted heredoc の `"` 経路を塞いだが、終端子行 (`WIU_EOF` だけの行) という脱出口が残っていた。6 heredoc が同一終端子を共有するためクォート均衡が不要で、脱出後も helper 呼び出しが正常に走るため **rc=0 + 3 marker 揃いの silent success** になる。**シェル層で閉じられない防御は、値を substitute する側（LLM）のゲートとして書くしかない** — block 内のシェルは既に parse 済みで手遅れ。
- **「rationale をコメントに書いた」は転記の完了ではない**: cycle 2 で TC-13/13b に施した「総称 `grep -q 'ERROR'` → ガード固有文言」の是正が、同一クラスの兄弟 TC (TC-14 / TC-22 / TC-22b) と必須引数ガード 3 本へ転記されず、いずれの退行も 40/40 green で生存した。**是正の対象は「その TC」ではなく「同じ assert 形を持つ TC 群」**。
- **消費者を足したら生産者・条件・引用元を同時に足す**: cycle 2 で完了レポートに `dedup_removed` 合計の消費者・index 更新失敗の受け皿を足したが、(a) カウンタの生産者（ステップ 2.1 表）、(b) `{ERROR 1 行目}` の引用元が存在しないケース（signal 中断は出力ゼロ、127 は非 ERROR 行）、(c) skip 決定 Raw Source での呼び出し条件、をいずれも足していなかった。**受け皿だけ作ると次 cycle で「埋められない欄」として返ってくる**。
- **退化値の fixture は契約を固定しない**: `dedup_removed` が 0/1 でしか観測されない 2 行重複 fixture では、「最初の 1 本だけ回収」「カウンタ定数化」の退行を殺せない。docstring が複数形（「後発行群」）で規定する契約は n≥2 の fixture が要る。
- **判別子は marker 値だけでなく WARNING 本文にも閉じる**: `stats_sync=synced` + 「stderr に WARNING」という判別子は、同じ呼び出しで出る重複中止 WARNING を統計の部分未同期と誤判定し、存在しない行名の捏造か二重計上に倒れる。**同一チャネルに複数の発行元がある場合、条件は literal で閉じる**。
- **fix 側 mutation は「修正したガードに対応する TC があるか」まで見る**: 7 種の mutation のうち 1 種（診断中和の無効化）が最初の実行で生存し、F-07 の実装修正に対応する TC がないことが判明した。cycle 3 のレビューが指摘したのと同じ「ガードに殺せる TC がない」問題を自分で再生産する直前で捕まえられた。**fix commit 前の mutation は修正の妥当性検証だけでなく、その修正が testable かの検証でもある**。

### Statistics
- Total findings: 12
- Fixed: 12
- Replied: 0

