---
type: fixes
source_ref: "pr-2126"
captured_at: "2026-08-06T15:30:14+00:00"
pr_number: 2126
title: "PR #2126 fix results"
ingested: false
---

## Fix Results

- **PR**: #2126
- **Type**: fix
- **Fixed at**: 2026-08-07

### Fix Patterns

cycle 1 の blocking 8 件を 1 コミットで解消。指摘は「新規 helper + それを消費する散文契約 + テスト」の 3 層構成に対するもので、修正も 3 層すべてに及んだ。

1. **既存永続データを新規 consumer が読むときは、集合の意味を書込側の定義から引く**（最大の欠陥、4 reviewer が独立検出）。`findings[]` を名前から「blocking 集合」と推測したが、実測必須ゲートの書込側コメントは「scope=nit-noted はゲート対象外のため非実測でも findings[] に残る」と明記しており、実体は blocking と nit-noted の和だった。修正は jq に `select(.scope == "current-pr" or .scope == "follow-up")` を足すだけだが、**記述側 5 箇所（helper docstring / SKILL.md 2 / cycle-scope.md / docs 2）を同時に直さないと jq だけ直して記述が腐る**。教訓: 名前が意味を語っていると思った時点で、書込側のコメントを SoT として引用する。

2. **横展開 hardening のテストが「対象リスト方式」のとき、新規ファイルは自動で保護されない**。`shift 2` の無限ループは既存 8 script へ hardening 済みで専用スイートが機械検査していたが、リストに登録されない新規 helper は素通りした。修正では実装と**リスト登録の両方**を行い、さらに helper 自身のテストにも no-hang の回帰 pin を置いた（二重化）。教訓: リスト方式の lint を持つリポジトリでは、新規ファイル追加時のリスト登録漏れが構造的な死角になる。実装だけ直してリスト登録を忘れると、次の新規ファイルで同じ regression が再発する。

3. **fail-safe fallback で「安全側に倒す」と「失敗理由を可視化する」を混同しない**。`prev_finders=$(jq ...) || prev_finders=""` は jq 失敗を握り潰し、出力される空文字列が正常系（blocking 0 件）と**バイト単位で同一**になっていた。区別する手段が無いため、保証していたはずの不変条件（前サイクル finder の無条件再起動）が無音で破れる。修正は新しい reason を足さず既存の loud な経路へ合流させるだけで済んだ。教訓: fallback を書く前に「この fallback 後の出力は正常系と区別できるか」を問う。区別できないなら fallback ではなく既存の fail-safe へ倒す。

4. **1 ファイル内で診断を出す経路と捨てる経路を同居させない**。探索段は tempfile + head + sed で stderr を surface する canonical な形を実装済みだったのに、その直後の jq/git 経路は `2>/dev/null` で捨てていた。同一ファイル内の非対称は、後から読む人に「捨ててよい経路」と誤読させる。修正では対象（壊れたファイルのパス、解決できなかった sha）と原因（stderr 先頭 3 行）の両方を出す形へ統一した。

5. **rationale を reference へ退避する設計では、同じ語が SoT 宣言・理由説明・注入本文の 3 箇所に現れる**。static contract test が単語 1 個を pin していたため、reviewer prompt へ注入される本文から mandate を削除しても残り 2 箇所で pin が満たされ green のままだった。テスト自身の header が「単語 1 個の pin は素通りする」と警告していたのに、4 つの mandate のうち 1 つだけがその規約に違反していた。教訓: 退避設計を採るなら、pin は**注入本文の行形**（`^1\. \*\*...\*\*`）でアンカーする。

6. **helper のテストは production の呼び出し形を最低 1 本含める**。全 14 テストが `--results-dir` を明示していたが、実呼び出しは同オプションを渡さない。既定解決（`state-path-resolve.sh` 経由）を壊しても全件 green だった。修正では multi_session sandbox を持つ既存スイートに TC を追加し、worktree cwd から main checkout 側を拾うことと、解決失敗時の loud fallback の両方を pin した。教訓: オプションを常に明示するテストは、既定値解決という最も壊れやすい経路を丸ごと素通りさせる。

7. **部分文字列 needle は「空であること」を検証できない**。`assert_contains "prev_finders="` は marker 行に常に出現するため、既定値を代入する変異でも PASS した。marker の値を切り出して等値比較する helper を足して解消。教訓: `key=value` marker の検証は値を切り出してから等値で見る。

### Statistics
- Total findings: 17
- Fixed: 8
- Replied: 0
- Non-blocking (measured gate): 9

