---
type: fixes
source_ref: "pr-2044"
captured_at: "2026-07-29T05:19:56+00:00"
pr_number: 2044
title: "PR #2044 fix results (cycle 4)"
ingested: false
---

## Fix Results

- **PR**: #2044
- **Type**: fix (cycle 4)
- **Fixed at**: 2026-07-29T14:20:00+09:00

### Fix Patterns

1. **agent が人間に渡す復旧コマンドを、agent の実行コンテキストでしか検証していなかった（HIGH）**: 停止通知に埋め込んだ手動リセットコマンドが `--session` を持たず、`flow-state.sh` の session 解決順に委ねていた。agent の Bash tool は env var 経路、人間の端末は env 不在で `.rite-session-id` 経路になり、しかも `session-start.sh` は env がある間 `.rite-session-id` を書かないため、**両者の不一致は drift ではなく設計上の定常状態**。結果、人間が実行すると rc=0 で成功しながら別 sid の state を新規作成し、当の counter は手つかずで残る。**教訓: agent が人間に渡す復旧コマンドは、agent の実行コンテキストではなく人間の実行コンテキストで正しいかを検証する。rc=0 は「正しい対象に効いた」ことを意味しない。**

2. **同じ条件を 2 箇所で別々に書き下し、3 cycle 連続で壊した**: `docs/CONFIGURATION.md` の handoff 残存条件は cycle 1（片側だけの失敗記述）→ cycle 3（"one cycle" の量化）→ cycle 4（先行詞の崩壊）と 3 回連続で defect を出した。毎回 fresh な散文で条件を書き直し、そのたびに新しい欠陥を持ち込んでいる。**教訓: 同じ条件を 2 箇所で独立に書き下すのをやめ、片方は観測可能な帰結だけを述べて条件は SoT へ委譲する。「同じ箇所が N cycle 連続で壊れた」は、その箇所の記述精度ではなく記述の重複そのものを疑う signal。**

3. **helper を導入したブロック内で、適用先が一部の分岐に留まった**: 前 cycle が診断 helper をステップ 1 ブロックへ読み込ませたが、適用したのは fire 分岐だけで、毎 cycle 通る increment 分岐は素通しのまま残った。cycle 1 の「同じ if の反対分岐を取り残す」と同型で、**本 PR で 4 回目の再発**（marker の再評価 → invariant の転記 → 条件記述の追随 → helper の適用範囲）。**教訓: 「何かをブロックへ導入したら、そのブロック内の全分岐に適用したかを最後に確認する」を機械的な締めの手順にする。**

### Scope Decision

rationale コメントの `references/` 退避（bash fence 内コメント比率 22%→42% で outlier 化）は、実測付き blocking 指摘だったが **follow-up として見送った**。`skills/iterate/references/` の新設は Issue の Target Files 外の構造変更であり、残り検証 cycle が 1 回の時点で入れるとリスクが見合わない。**教訓: サーキットブレーカーの残 cycle は、修正の大きさを選ぶ制約条件として使える。「直すべきか」だけでなく「今の残 cycle で検証しきれるか」で判断する。** 見送りは非実測記録コメントに残して追跡可能にした。

### Statistics
- Total findings: 5
- Fixed: 4
- Deferred (follow-up): 1

