---
type: fixes
source_ref: "pr-661-cycle-1"
captured_at: "2026-04-25T15:45:17+00:00"
pr_number: 661
title: "PR #661 fix results (cycle 1)"
ingested: true
---

## Fix Results

- **PR**: #661
- **Type**: fix
- **Cycle**: 1
- **Fixed at**: 2026-04-26T00:50:00+09:00

### Fix Patterns

cycle 1 で 4 reviewer (prompt-engineer / test / code-quality / error-handling) から 7 findings を受領し全件対応:

1. **DRIFT-CHECK ANCHOR comment 4-arg 拡張** (3 件 / HIGH 2 + MEDIUM 1): create-interview.md:605 / stop-guard.sh:379 / cleanup.md:1674 の ANCHOR comment が 3-arg のままで、隣接 literal は本 PR で `--active true` を含む 4-arg に更新済み。文言と literal の drift を残すと将来 PR で逆方向 drift を invite する fractal pattern が再発する。本修正で 4-arg comment に統一し pair sync を達成。

2. **AC-1 test infrastructure 永続化 (TC-660-D)** (HIGH): PR description で言及された「block-level Python verification」が test infrastructure に永続化されていなかった (Wiki 経験則「emit/consume/test 3 点セット契約」の test 点欠落)。block-aware awk scan による全 commands/**/*.md 走査を test に追加し、CI で permanent 検証可能にした。

3. **Inverse TC (TC-660-E)** (MEDIUM): TC-660-C は `--active true` 明示時の flip のみ assert していたが、本 PR の root cause は flow-state-update.sh:254 の `if [[ -n "$ACTIVE" ]]` preserve-existing semantics に依存していた。双方向 (active=false / active=true) の preserve を assert する inverse TC を追加。

4. **TC 命名 convention 統一 + dead variable 除去** (MEDIUM 2 件): `TC-active-omit-{A,B,C}` を `TC-660-{A,B,C}` に rename し既存 `TC-<issue#>-<letter>` convention に揃えた。同時に TC-660-A の `output_aoa=$(...)` dead variable を `>/dev/null` に変更。

### Statistics
- Total findings: 7
- Fixed: 7
- Replied: 0
- Skipped (separate Issue): 0
- Files changed: 4 (+205 / -62)
- Tests: 69 PASS, TC-013 pre-existing FAIL (scope 外)

### Lessons (raw)

- **Pair-sync invariant for 4-site DRIFT-CHECK ANCHOR**: ANCHOR comment と literal は同期 invariant を持つ。片方を更新する PR は対称先の comment も同時更新しないと、後続 PR で comment と literal の drift を見て「3-arg が正」と誤判断するリスクが残る。Issue #660 の cycle 1 review で 3 箇所同時に発覚したのは、本 PR が `--active true` を literal に追加する際に隣接 ANCHOR comment の 3-arg → 4-arg 拡張を見落としたため。
- **TC infrastructure persistence**: 「block-level Python verification を実行した」と PR description に書いただけでは CI で再現可能性がない。test ファイルに block-aware awk / python here-doc を埋め込んで、merge 後も regression catch できる test infrastructure として永続化する責務がある (本 PR の TC-660-D は累積対策 PR 11 回目で初めて確立した invariant)。
- **Terminal-equivalent phase の exclusion 設計**: AC-1 の literal grep は `create_completed/cleanup_completed/ingest_completed` のみを exclude するが、`/rite:issue:start` の Workflow Termination 経路は `phase5_post_parent_completion` (前段の意図的 deactivate) + `completed` (terminal final state) も terminal-equivalent な意図的 deactivate を持つ。AC 仕様書では「terminal-equivalent も exclude list に含める」と整理する必要がある (prompt-engineer R-03 推奨事項を test に取り込み済み)。


