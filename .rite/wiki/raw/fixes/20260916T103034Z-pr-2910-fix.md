---
type: fixes
source_ref: "pr-2910-fix"
captured_at: "2026-09-16T10:30:34+00:00"
pr_number: 2910
title: "docs(projects): Status の仕様を role 基準に書き換え、ボード形態別の通し回帰テストを追加する（修正結果）"
ingested: false
---

## Fix Results

- **PR**: #2910 — docs(projects): Status の仕様を role 基準に書き換え、ボード形態別の通し回帰テストを追加する
- **Type**: fix
- **Fixed at**: 2026-09-16
- **Findings addressed**: 4 (CRITICAL 2 / HIGH 2)、全件 fix、commit b426009a0

### Fix Patterns
- 実装を role 基準に書き換える文書 PR で、SoT に「全 consumer は未マップ列を触らない」という全称保証を新規に書いたところ、write 側（Status 更新 helper と drift-check の `--reconcile`）で偽だった。修正は新しい注記を足すのではなく、全称文を consumer 別（read 側は放置 + WARNING / write 側は destination role へ書く）の列挙に置き換えた。文書の主張を広げる修正案は、適用前に主張が名指しする集合を実装で列挙して一致を確認する。
- 状態保持 gh shim を使う回帰テストで、書き込み subcommand の不在を denylist regex で pin していたが、複数行 GraphQL 引数（`-f query='` 直後の改行）が行単位 grep をすり抜けた。shim のログ行を `${*//$'\n'/ }` で 1 行に潰し、許可した読取 subcommand 以外の gh 行が 1 つでもあれば fail する allowlist へ反転し、collapsed mutation・未列挙 write verb・shim の 1 行化を positive control で固定した。
- 静的文法走査の positive control は「総 hit 数 ≥ arm 数」ではなく arm ごとに fixture 到達を assert する。修正で消した誤文言は grammar arm に追加して再発を pin する。

