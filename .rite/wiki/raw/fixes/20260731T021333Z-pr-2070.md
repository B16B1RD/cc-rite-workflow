---
type: fixes
source_ref: "pr-2070"
captured_at: "2026-07-31T02:13:33+00:00"
pr_number: 2070
title: "PR #2070 fix results (cycle 3)"
ingested: false
---

## Fix Results

- **PR**: #2070
- **Type**: fix

### Fix Patterns

欠損を露出する仕組みを作っても、その仕組みに到達しない経路が残っていれば意味がない。今回の 3 経路はいずれも「ガードの外側で件数が減る」形だった: 除外フィルタが判定より前段にあって落とした行が計数にも欠損報告にも現れない、ヘッダー判定がデータ行を飲み込む、母数を作る多段パイプが無検査。ガードを足したら「そのガードに到達しない入口が残っていないか」を別途たどる必要がある。

除外規則の適用位置は意味を変える。TODO/FIXME 除外を行単位の前段で行うと、コードスパン内に引用されただけの TODO でも行ごと落ちる。コードスパンのマスク（引用は主張ではない）より後に判定すると引用が救われる。前段の除外は「安い」が、後段のマスクと組み合わせたときの順序が結果を決める。

多段パイプは実行失敗と「結果 0 件」を同じ空文字列に潰す。`printf | grep | awk` を awk 1 本に畳むと rc を 1 箇所で捕捉でき、プロセスも減る。フィルタ段を足すたびに silent-0 の入口が増える。

診断メッセージに外部入力を埋め込む箇所は、同一ファイル内の他の診断と同じ中和規約に揃える。1 行だけ規約から外れると、静的チェックが `head -N | ... >&2` の形しか sweep しない場合に構造的にすり抜ける。

anti-pin（「この文字列が出ないこと」）は、pin する文字列が実装に実在するかを確かめないと恒久的に緑になる。同じ WARNING を pin する肯定側のテストと同一 literal を使えば、文言変更時に両方が同時に落ちる。

上流ガードと同一の regex を下流で再判定する分岐は到達不能になる。`match()` を副作用（RSTART 設定）目的で呼ぶ場合は、rc 判定を残さず呼び出しだけにする。

### Statistics
- Total findings: 12
- Fixed: 12
- Replied: 0

