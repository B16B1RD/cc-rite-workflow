---
type: fixes
source_ref: "pr-2483"
captured_at: "2026-08-30T09:32:45+00:00"
pr_number: 2483
title: "PR #2483 fix results"
ingested: false
---

## Fix Results

- **PR**: #2483
- **Type**: fix

### Fix Patterns

コメントの事実誤りへの指摘 1 件。修正は文言置換のみで機構は不変（simplification-first: 追加パッチではなく記述の訂正で閉じた）。

根本原因として一般化できる経験則: **「この値を書くのは 1 箇所だけ」という単一 writer の主張を判定ロジックの正当化根拠に使うとき、`grep -rn -- "--flag value"` のような CLI フラグ検索だけでは producer を数え切れない**。値を `echo` で生む正規化 / migration 関数（本件では `flow-state.sh` の `_phase_migrate` が legacy phase を `init` へ縮退させる経路）が母集団から漏れる。単一 writer を根拠にするなら、フラグ検索に加えて「その値を生成して state に書き込むすべての経路」を数える必要がある。

判定を単一値に絞ること自体は変えず、コメントを実態（2 経路）へ直したうえで、漏れていた経路が抑止されても次の phase 変化で別の短絡が通知するため恒久沈黙にならないことを併記した。「主張を実態に合わせる」か「判定を広げる」かは、漏れた経路の帰結が回復可能かで決まる。

### Statistics
- Total findings: 1
- Fixed: 1
- Replied: 0

