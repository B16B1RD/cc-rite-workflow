---
type: fixes
source_ref: "pr-813-cycle2"
captured_at: "2026-05-04T11:02:07+00:00"
pr_number: 813
title: "PR #813 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #813 — refactor(create-decompose): #806 create-decompose.md 本体スリム化 (#773 follow-up)
- **Type**: fix (cycle 2)
- **Reviewers Addressed**: prompt-engineer-reviewer, tech-writer-reviewer, code-quality-reviewer

### Fix Patterns

cycle 2 で対応した 10 finding は以下のパターンに分類される:

1. **Broken cross-reference (HIGH x 1, 3 reviewer 一致)**: refactor で削除した heading 名を caller 側 prose が文字列参照し続ける silent dangling reference。修正 scope に caller 側更新を含めなかった refactor の見落とし。検出は `grep -F` で機械化可能、3 reviewer 独立検出で high-confidence。

2. **Asymmetric Fix Transcription 自己再現 (HIGH x 1, Wiki 経験則 high confidence)**: refactor で「重複削除」と判断した DRIFT 警告 inline comment が実は対称性契約 (parent-routing.md / sub-issue-link-handler.md と 3 site sync 義務) の片肺だった見落とし。Wiki 経験則「Asymmetric Fix Transcription」が累積 20+ 回観測されており、本 PR が累積 21 回目の自己再現事例として実測。

3. **Moved content の inline pointer 残存 (MEDIUM x 2, 2 reviewer 一致)**: bash inline comment の `(see "structure defined below")` が文書下方を指すが structure を別ファイルに Moved した。圧縮時にコメント文を Moved 通知と同期させる check が必要。

4. **位置づけ table SoT 列 stale (MEDIUM)**: 新節追加した references が既存 SoT mapping table の行を更新し忘れる drift。新 SoT 宣言時の pre-existing duplicate 顕在化と同型 (PR #801 累積)。

5. **Design document の goal-setting back-propagation 漏れ (MEDIUM)**: Umbrella design doc の当初目標と Sub-Issue 実着地の乖離。AC 緩和を design doc 側にも反映する運用が必要 (#805 / #806 で確立)。

6. **Hardcoded 行番号 cross-reference の二重 stale (MEDIUM)**: refactor で対象ファイルが縮んで stale 化、同時に参照先 inline comment 自体を削除して二重 stale。「DRIFT-CHECK ANCHOR は semantic name 参照」原則を references/regression-history への参照にも拡張。

### 経験則候補

- **Refactor で heading / inline comment を削除する際の caller-side propagation grep**: pre-commit gate で `grep -F '"removed-caption-text"' ...` を機械検証
- **対称性契約の自動検出**: 4-site test だけでなく「DRIFT 警告対称性」を含む grep test を追加
- **Hardcoded line reference の lint**: regression-history.md / 他 ref docs における `Lxxx` 参照の禁止 / 検出 (`/rite:lint` Phase 3.11 hardcoded-line-number-check の対象を references/ 配下にも拡張)
- **AC 段階的緩和の design doc back-propagation**: Sub-Issue series の最初の緩和発生時に design doc 側の goal-setting を更新する rule

### Statistics
- Total findings: 10
- Fixed: 10 (HIGH 2, MEDIUM 4, LOW 4)
- Replied: 0
- Skipped (separate Issue): 0
- AC-1 段階的緩和: ≤300 → ≤500 (cycle 1) → ≤510 (cycle 2、+17 行)

