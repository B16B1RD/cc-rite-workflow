---
type: fixes
source_ref: "pr-2036"
captured_at: "2026-07-27T04:23:11+00:00"
pr_number: 2036
title: "PR #2036 fix results"
ingested: false
---

## Fix Results

- **PR**: #2036
- **Type**: fix
- **Fixed at**: 2026-07-27

### Fix Patterns

12 件すべてを修正で解消 (返信のみ / スキップ 0 件)。修正戦略として最も効いたのは **「同期するのではなく revert する」判断**だった。

- **同一 invariant の複数コピー問題では、片側だけの変更を revert するほうが 3 箇所同期より安全なことがある**: cross-field invariant #2 の述語を read 経路 3 コピーのうち 1 つだけ変更していた指摘に対し、「3 箇所 + SoT を同期する」ではなく「変更した 1 箇所を revert する」を選んだ。理由は (a) 残り 2 コピーと SoT が別 Issue の所有ファイルでスコープ外、(b) write 側が当該フィールドを出力しない現状では述語が恒に偽で機能しない、(c) revert なら非対称そのものが消える。同期は「両側が実際に動く」段階まで待つほうがコストも危険も小さい。
- **「N 箇所で同期が必要」と指摘されたとき、まず N を減らせないか検討する**: 上と同根。同期対象を増やす方向 (SoT も実装も直す) は将来の drift 点を増やす。
- **判定媒体と保存媒体の取り違えは、定義文の語彙で潰す**: `total_findings` を JSON フィールド参照 (`verification.measured == true`) で定義したが、その判定が走る時点では JSON が未生成だった。修正は「集合演算での定義 + 判定媒体の注記」で、フィールド名は「同一述語の JSON エンコーディング」として別文脈に降格させた。
- **同一 prompt に注入される複数文書は 1 セットとして整合を取る**: reviewer prompt は複数ファイルから抽出・結合されるため、片方だけ更新すると reviewer が矛盾指示を受け取る。抽出元ファイルの一覧 (`shared_reviewer_principles` の抽出範囲) を変更影響の grep 対象に含める。
- **相対リンクの深さは近接行と比較すると誤りが見つかる**: 同一リストの 17 行上が `../../` なのに新規行だけ `../../../` だった。`realpath -m` での機械検証が確実。

### Statistics
- Total findings: 12
- Fixed: 12
- Replied: 0

