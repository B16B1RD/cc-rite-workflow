---
type: fixes
source_ref: "pr-2917"
captured_at: "2026-09-16T14:05:42+00:00"
pr_number: 2917
title: "fix(projects): Status role の文書を実装に同期し、未検証経路のテストを追加する（修正結果）"
ingested: false
---

## Fix Results

- **PR**: #2917 — fix(projects): Status role の文書を実装に同期し、未検証経路のテストを追加する
- **Type**: fix
- **Fixed at**: 2026-09-16T23:05:00+09:00
- **Findings addressed**: 1 (F-01 HIGH)

### Fix Patterns
- テスト内で gh mock の挙動を後から `sed -i` で書き換える構造は、GNU/BSD sed 差と no-match 無音の 2 つの穴を同時に持つ。marker の設置は mock 定義そのものに書き、board を実際に読む TC に marker 存在の positive control を置くと、absence assert が空振りでないことを同 suite 内で固定できる。
- simplification-first: パッチを重ねず、事後パッチ行を削除して mock 定義側へ移すことで sed 依存と pattern 依存がまとめて消えた。

### Verification
- related: `bash plugins/rite/hooks/tests/post-compact.test.sh` 81/81
- full: `bash plugins/rite/hooks/tests/run-tests.sh` 全 pass（scope verify --kind all）

