---
type: fixes
source_ref: "pr-2099"
captured_at: "2026-08-04T06:08:34+00:00"
pr_number: 2099
title: "PR #2099 fix results"
ingested: true
---

## Fix Results

- **PR**: #2099
- **Type**: fix

### Fix Patterns
cycle 5 (最終)。blocking 5 件を修正。中核は **simplification-first の実適用**。

cycle 4 は「pin 書込失敗経路」に `rm -f` + WARNING + marker を足して縮退を塞いだが、cycle 5 で
(a) その `rm -f` 自体の失敗サブ経路で WARNING が矛盾、(b) 構造的に同型の「counter reset 失敗 →
pin 更新ゲートを通らない」経路が未処理、の 2 件が出た。ここで同じ機構を reset 失敗分岐へ複製する
（= 3 つ目のパッチ）代わりに、**ゲートの述語そのものを疑った**。

`cur_cc == 0` は「新しい run か」の **proxy** で、reset 失敗時にその proxy が壊れるのが根本原因。
実際の述語 `fresh || cur_cc == 0`（選言）へ置き換えると、経路 (b) は機構を 1 つも足さずに消える。
経路 (a) は分岐数を増やさず rm の rc で WARNING と marker を分けるだけで解消。

**教訓: 累積対策 PR で「同じ機構への N 回目のパッチ」を書こうとしたら、その機構が依拠している
述語が proxy になっていないかを先に疑う。proxy を実際の述語へ替えると、パッチが必要だった経路群が
まとめて消えることがある。** cycle 4 の「1 経路だけ塞いで同型の他経路を残す」失敗も、proxy を
疑っていれば cycle 4 の時点で全経路が閉じていた。

副次: 強いテストスイート (124 assertion / 実 fixture 回帰つき) でも未 pin の契約は残る。判定式の
「窓の範囲」項・環境要因 guard・判定成立側の reason 2 値がそれで、いずれも **mutation で初めて発覚**
した。assertion 数もカバー範囲の見た目も識別力の代理指標にならない。

### Statistics
- Total findings: 16
- Fixed: 5
- Replied: 0

