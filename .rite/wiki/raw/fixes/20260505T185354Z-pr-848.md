---
type: fixes
source_ref: "pr-848"
captured_at: "2026-05-05T18:53:54+00:00"
pr_number: 848
title: "PR #848 fix results"
ingested: true
---

## Fix Results

- **PR**: #848
- **Type**: fix
- **Fixed at**: 2026-05-06T03:55:00+09:00

### Fix Patterns

主要 fix pattern: **WARNING 文言短縮型 refactor の test pin asymmetric drift**。
docstring SoT 統一 refactor で WARNING 文言を短縮した結果、tests/state-read.test.sh の
grep pin (`grep -q "boolean リテラル値"`) と乖離 → CI red 確実発火。

修正戦略の選択:
1. **採用**: WARNING に「リテラル値」を復元 (test pin 側は変更しない)。理由は
   (a) 自然な日本語表現を保てる、(b) test 側 false-positive guard 文字列の同時更新が不要、
   (c) 既存 mutation kill power を維持
2. **non-採用**: test pin を「boolean リテラル」に短縮。test 側の false-positive
   guard 文字列も同時更新する必要があり、変更スコープが広がる
3. **non-採用**: TAG 文字列を定数化して test と本文を decouple。設計改善だが本 PR の
   スコープ外 (Issue #842 が要求しているのは SoT 統一のみ)

cross-validation 効果: code-quality (CRITICAL) + error-handling (HIGH) の 2 reviewer
独立検出により、CI 失敗が確定した状態でマージ承認される silent regression を防げた。

教訓: docstring SoT 統一型の refactor では「caller pattern guidance を WARNING text に
二重記載しない」だけでなく「WARNING 文言改訂時に test pin と同期する責務」も
docstring に明記する必要がある。今回 docstring に同義務を追記したため将来の同種
regression を構造的に防ぐ。

### Statistics
- Total findings: 6
- Fixed: 4 (CRITICAL test regression / HIGH deprecated note redundancy / MEDIUM × 2 用語統一 + test 同期 note 追加)
- Replied: 0
- Skipped: 2 (LOW × 2 — caller list 表現は「等」緩和で吸収、PR description 補足は scope 外)

