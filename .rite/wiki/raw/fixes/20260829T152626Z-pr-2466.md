---
type: fixes
source_ref: "pr-2466"
captured_at: "2026-08-29T15:26:26+00:00"
pr_number: 2466
title: "PR #2466 fix results"
ingested: true
---

## Fix Results

- **PR**: #2466
- **Type**: fix
- **Fixed at**: 2026-08-30T00:35:00+09:00

### Fix Patterns

散文駆動スキル（SKILL.md）の契約変更に対する static-contract テストの穴を埋めた 1 cycle。

1. **追記箇所と pin の 1 対 1 対応を数え合わせる**: 前 commit は SKILL.md の 3 箇所を追記したのにアサーションは 2 箇所分しかなく、3 つ目（Placeholder Legend + Note の substitution 契約）だけを base 版へ戻すとスイートが green のまま AC が壊れる状態だった。**「契約を N 箇所追記したら pin も N 箇所あるか」を機械的に数える**のが有効な検査で、レビュー側は mutation（該当箇所だけ base に戻して suite が落ちるか）で検出できた。

2. **section 限定の positive + ファイル全体の negative の組み合わせ**: Legend 節に限定した `assert_grep_in_section` 2 件（Legend 行 / Note の例外記述）で新しい契約を固定し、base 版 Note の 4 placeholder 禁止列に対する `assert_not_grep` で再混入を塞いだ。positive だけでは「新記述の削除」しか検出できず、negative だけでは「新記述が別文言に変わる」を通してしまう。両方が要る。

3. **negative アサーションの pattern は base 版の literal を使う**: `assert_not_grep` は file-wide なので、pattern が広すぎると無関係な行を掴む。base 版の禁止列（`{project_number}` / `{parent_issue_number}` は本コマンド body で substitute しない）のように **base にしか存在しない連続literal** を使うと、file-wide でも誤検出しない。

4. **空振り検証は「該当箇所だけ base に戻す」mutation で行う**: 新規アサーション追加時は編集前実行の FAIL 確認だけでなく、**pin 対象の記述だけを差し戻して 3 件とも落ちる**ことを確認するのが確実。今回は Legend 行削除 + Note 差し戻しで 3/3 FAIL を実測した。

### Statistics
- Total findings: 2
- Fixed: 1
- Replied: 0
- Non-blocking (class B 降格): 1

