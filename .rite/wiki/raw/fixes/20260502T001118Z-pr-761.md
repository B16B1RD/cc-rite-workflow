---
type: fixes
source_ref: "pr-761"
captured_at: "2026-05-02T00:11:18+00:00"
pr_number: 761
title: "PR #761 cycle 1 fix results"
ingested: false
---

## Fix Results

- **PR**: #761 (cycle 1 fix)
- **Type**: fix
- **Result**: 5 findings addressed (HIGH 1 + MEDIUM 2 + LOW 2)

### Fix Patterns

- **PR description fact-check 不足 (HIGH)**: AC-3 解消の核心 statement「`36/36 passed` の `36` は assertion 数」が事実誤認だった。`run-tests.sh:13` の実装 (`for test_file in *.test.sh; TOTAL=$((TOTAL + 1))`) を確認せず数値解釈を書いたため、AC 解消対象だった conflate を別形 (file 数 vs assertion 数) で再現していた。
  - **教訓**: AC 解消の核心 statement に数値解釈が含まれる場合、必ず実装 (`grep -n` で関連 loop / counter を確認) で裏取りする。本 PR では 3 階層 (ファイル / TC / assertion) を明示分離する記述に変更し、各階層の取得コマンド (`ls *.test.sh | wc -l`、`grep -c '^echo "TC-'`、`grep -c 'pass\|_assert\|fail'`) を併記して再発を防ぐ構造にした。
- **race-window probe で identification power と仕様の乖離 (MEDIUM)**: TC-19 で `mid+post >= 1` を assert しつつ「mid_or_post 観測経路を確保」と cover letter に記載していたが、production sequence (50ms) より長い sleep (50ms) では大半が S_post に landing して M_mid 観測は environment-dependent な best-effort になる。
  - **教訓**: outcome classification + race-window assert は「sleep 短縮による dead code 化を防ぐ post-condition」として再定義し、True mid-state observation は別 test (将来 Issue) で補強する旨を明記する。assert 文言も `race-window-hit` から `atomic-completion-observed` に変更して意図を明確化。
- **collected error pattern の partial 適用 (MEDIUM)**: TC-19 で byte_equal_violations は集計形式だったが orphan reap は最終 iter のみ assert で、同一 test 内で異なる pattern が混在していた。中盤 iter の zombie 化が検出不能。
  - **教訓**: invariant 検証は loop 内 collected error counter (e.g. `unexpected_wait_rc_count`) で集計し、ループ外で全 iter assert する pattern に統一する。`byte_equal_violations` と同型に揃えることで test code の一貫性が向上し identification power が均等化される。

### Severity Distribution
- CRITICAL: 0
- HIGH: 1 (PR description fact-check failure)
- MEDIUM: 2 (race-window 仕様 / orphan reap collected error)
- LOW: 2 (DRY 違反コメント / sed コメント正確化)

