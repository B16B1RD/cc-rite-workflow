---
type: fixes
source_ref: "pr-2052"
captured_at: "2026-08-02T07:07:15+00:00"
pr_number: 2052
title: "PR #2052 fix results (cycle 3)"
ingested: true
---

## Fix Results

- **PR**: #2052 (cycle 3)
- **Type**: fix
- **Fixed at**: 2026-08-02T16:10:00+09:00

### Fix Patterns

1. **散文を書き換える前に、その文字列を pin しているテストを grep する**: `{ingest_outstanding_line}` の説明段落を marker 2 本対応へ書き直した際、Issue #1946 の設計契約を literal で pin していた `outstanding-items-contract.test.sh` の assert 語句を巻き込んで削除し、CI が赤になった。**手順書の散文は実行契約であると同時にテストの検査対象でもある**。段落単位で書き直すときは、当該段落中の特徴的な文字列で `hooks/tests/` を grep してから着手する。なお pin されていた不変条件（新しい永続記録先を持たない）は新実装でも成立していたので、直すべきはテストではなく文言側だった — **テスト側の assert を消して緑にするのは、その Issue の charter が無い限り禁じ手**。

2. **変換を新しい入力へ広げるときは冪等性を検査する**: エスケープ規約の適用対象に「既存行から保持した値」を足したが、保持値は前サイクルで既に変換済みの文字列だった。規約に冪等性の限定が無いため、更新サイクルごとにエスケープ文字が 1 つずつ増え、元の状態を復元できないまま蓄積する。**「値に X が含まれるなら Y にする」型の規約は、Y を含む値を再入力したときの挙動を明示しないと非冪等になる**。

3. **カウンタに加算元を足したら、その総数を分解している等式と内訳表示も同時に直す**: 新しい中止経路で `n_warnings += 1` を配線したが、完了レポートの内訳（6 項）と等式は据え置きだった。総数 1 に対し内訳合計 0 という自己矛盾した出力になる。同種の先行例（`n_lint_anomaly` 導入時）は等式・内訳の両方へ項を足しており、**加算元の追加は「カウンタ表 / 内訳表示 / 等式」の 3 点セットで行う**のが既存の慣習だった。

4. **散文で「中止せよ」と書く経路は、実行ブロック内で機械的に止める**: 中止 5 経路のうち 4 つはブロック内で marker を emit して `exit 0` していたのに、1 つだけ「この値が出たら中止せよ」という LLM 向けの散文だった。しかもその経路はブロックが破壊的な値そのものを正常系と同じ形状で出力してから止める構造で、5 経路のうち最も破壊的な失敗を最も弱い手段で守っていた。**同種の経路が複数あるとき、最も危険な 1 本が最も弱い強制になっていないかを確認する**。

5. **`grep -c` の 0 件マッチ (rc=1) は正常系**: 空ドメインは恒常的に存在するため `|| true` の付与は防御ではなく要件。ブロックが将来 strict mode 化されると、中止 marker を出さないまま abort する。

6. **同一の literal を 2 箇所に書かない**: 完了レポート行の literal を手順側と表側の両方に書いていたため、片方だけ直すと drift する。表を SoT にして手順側はポインタにする（本リポジトリの rationale 退避方針と同じ）。

### Statistics
- Total findings: 14
- Fixed: 14
- Replied: 0

