---
type: fixes
source_ref: "pr-2099"
captured_at: "2026-08-04T01:52:39+00:00"
pr_number: 2099
title: "PR #2099 fix (cycle 4)"
ingested: false
---

## Fix Results

- **PR**: #2099 / cycle 4 — blocking 13 件のうち HIGH 6 件（全て cycle 3 由来の退行）を修正

### Fix Patterns

1. **`flow-state.sh set` は指定しなかったフィールドを消す。新しい set を挿入する前に「何を消すか」を列挙する**:
   完了通知の前に counter をリセットする 1 行を足したところ、同じ set が handoff を default-clear し
   phase を上書きして、完了通知を強制する Stop hook の保証と中断通知の記述を同時に壊した
   （3 reviewer が独立検出）。書く値だけを見て挿入すると、消える値の側で必ず壊れる。
   既存値を read して載せ直す形（`--handoff "$h"` / `--phase "$cur"`）が最も副作用が小さい。

2. **「構造的保証」を主張する前に、その保証を成立させている処理が失敗しうるかを確認する**:
   「pin があれば列は現 run のもの」を根拠にガードを緩めたが、pin を更新する処理自体が
   非ブロッキングだったため保証にならなかった。不変条件の主張は、それを維持する経路の
   失敗可能性まで含めて検証する。

3. **時間的順序から導ける排他条件は、ガードの補助条件として使える**:
   counter skew は「少なくとも 1 回 review が完了して初めて成立する」ので、`cycle_count == 0` は
   skew と重ならず stale pin だけを指す。この 1 条件を OR で足すだけで、意図した続行を保ちながら
   誤発火経路だけを閉じられた（fixture 7 条件で挙動不変を実測）。

4. **診断の抑止（`2>/dev/null`）を外す修正は、同じ helper を呼ぶ全サイトを同時に洗う**:
   iterate 側 2 箇所だけ外して helper 内の 3 つ目を残し、次 cycle で非対称として指摘された。

### Statistics
- Total findings: 13 / Fixed: 13 / Recorded only: 0

