---
type: fixes
source_ref: "pr-2051-c2"
captured_at: "2026-07-29T15:15:17+00:00"
pr_number: 2051
title: "PR #2051 fix results (cycle 2)"
ingested: true
---

## Fix Results

- **PR**: #2051 (cycle 2)
- **Type**: fix
- **Fixed at**: 2026-07-30

### Fix Patterns

cycle 1 (18 件) → cycle 2 (9 件) と収束したが、cycle 2 で新たに判明したのは「cycle 1 の修正自体が持っていた検証の穴」だった。テストを直したことで、そのテストの弱さが次のレビューで露見する構造になっている。

1. **アサーションの存在と、そのアサーションが対象コードを通ることは別**: cycle 1 で恒真アサーションを差分アサーションに直したが、cycle 2 の mutation で別の 2 件が空振りと判明した。(a) セクション境界ガードを名指しするアサーションが、fixture で対象セクションを後ろに置いたため in_section がそもそも立たず、ガードを削除しても緑のまま。(b) rc=2 に至る 3 経路のうち awk fatal の arm だけテストが無く、そのカウンタを削除しても全 assertion が緑。**検証の強さは「該当行を壊して赤くなるか」を実測しないと分からない**。恒真かどうかの目視判断では届かない。

2. **`$( )` でコマンド置換したヘルパーの `exit` は呼び出し元を止めない**: sandbox 生成ヘルパーは hard-fail として `exit 1` を書いているが、`SANDBOX="$(make_plain_sandbox)"` では subshell だけが終了し、呼び出し元には空文字が返る。`cd ""` は rc=0 の no-op なので `|| exit 1` ガードも発火せず、fixture 書き込みと `rm -f` が呼び出し元の CWD（リポジトリルート）に落ちる。しかもテストは PASS を報告する。**ヘルパー側の fail-closed 意図は、呼び出し規約によって無効化されうる**。値が生まれる位置（代入の直後）で検査するのが唯一確実。

3. **集約診断は「最も出力が長いケース」で消えやすい**: 走査不能ファイル数の ERROR を findings 判定の後ろに置いたため、両方が起きた run でだけ集約行が出なくなっていた。診断の価値が最も高い状況で診断が消える。**早期 exit の前に集約を出す**のが原則で、exit code の優先順位（findings が rc を取る）とは独立に決める。

4. **exit code を 1 つ増やす変更は、その値を読む全レイヤに波及する**: rc=2 を新設した 2 スクリプトについて、per-check rationale / 姉妹 script の header・usage / 呼び出し側 skill の DoD 注記 の計 4 箇所が rc=0 時代の記述のまま残った。1 箇所直すと残りが「更新漏れ」として相対的に確定するため、レビューで必ず露見する。

5. **skill 本文でコードと契約文が矛盾すると fail-fast が握り潰される**: 新しい exit 1 経路を追加しながら、同ファイルの「原則 exit 0、事前チェック失敗は非ブロッキング」という契約文 2 箇所を更新していなかった。skill 本文は LLM が実行する仕様書なので、この矛盾は Conflicting Instructions として実行時に解決され、契約文の側に倒れうる。

6. **契約が複数サイトに分裂したら静的 parity テストで固定する**: 4 スキルの helper 分岐は 2 つの契約（fail-fast / skip reason）に意図的に分裂しており、片方は reason 文字列と表示テーブル行の一致が前提だった。typo しても全 109 テストが緑のままだったため、静的 assertion を追加した。リポジトリに既に 27 本ある同型の parity テストに倣えば実装コストは低い。

### Statistics
- Total findings: 14 (blocking 9 + nit-noted 1 + non-blocking 4)
- Fixed: 10
- Replied: 0

