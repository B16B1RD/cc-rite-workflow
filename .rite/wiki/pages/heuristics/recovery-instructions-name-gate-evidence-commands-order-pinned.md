---
type: "heuristics"
title: "再開手順は後段ゲートが要求する証跡を作るコマンドを名指しし、静的検査で順序も固定する"
domain: "heuristics"
description: "停止後の再開手順が一般的な案内文だけで、後段のゲートが要求する証跡（特定コマンドの実行結果）を作るコマンドを名指ししないと、利用者はゲートを満たせず同じ停止を繰り返す。手順内のコマンド順序も静的検査で固定すると、順序 drift による停止の再発を防げる。"
created: "2026-09-26T07:12:24+00:00"
generated: { by: "rite-wiki-ingest/claude-opus-5-5", at: "2026-09-26T08:23:48Z" }
verified:
  - { by: "rite-wiki-ingest/claude-opus-5-5", at: "2026-09-26T08:23:48Z" }
sources:
  - type: "fixes"
    resource: "raw/fixes/20260926T071224Z-pr-3120.md"
  - type: "reviews"
    resource: "raw/reviews/20260926T075636Z-pr-3120.md"
tags: []
confidence: medium
---

# 再開手順は後段ゲートが要求する証跡を作るコマンドを名指しし、静的検査で順序も固定する

## 概要

停止通知やエラーメッセージが示す「再開手順」は、後段のゲートが検査する証跡（例: スコープ検証 helper の `check` と `verify --kind all` の実行結果）を実際に作るコマンドを名指しして並べる必要がある。手順が一般的な案内（「検証してから進めてください」等）に留まると、利用者は正しい順序で必要な証跡を作れず、同じゲートで再び止まる。

## 詳細

再試行権を発行した run が iterate から再試行の review に進めなかった不具合の修正では、再開手順を「後段のゲートが要求する証跡を作るコマンドを名指しして並べる」形に直した。単に「検証してください」と書くだけでは、利用者はどのコマンドがどの証跡を作るかを推測する必要があり、順序を誤ると証跡が欠けたままゲートに再度弾かれる。

さらに、この名指し自体が drift しないよう、静的検査で手順内のコマンド順序まで固定した。手順書の自由記述に任せると、後からの編集でコマンドの順序が入れ替わり、ゲートの前提（例: `check` の後に `verify --kind all` を実行する）が崩れても検出されない。順序を静的検査の対象にすることで、手順書の編集がゲートの前提を暗黙に破壊する経路を塞ぐ。

同じ手順を複数の文書に書く場合は、実行者が直接読む箇所（停止通知の再開行など）だけにフルコマンドを置き、契約の SoT との一致をテストの静的検査で固定する。フルコマンドを各文書へ複製すると、次に手順を変えたとき一部だけが古いまま残る。静的検査があれば、その同期漏れを次の変更で検出できる。

手順に「検証」のような一般語が残っていないかは、レビューで実際に手順どおり動かすと見つけやすい。使い捨ての worktree で文書どおりにコマンドを実行すると、一般語が後段ゲートの要求（検証済み修正の記録など）と食い違っていることを実測で示せる。

## 関連ページ

- [agent が人間に渡す復旧コマンドは、人間の実行コンテキストで正しいかを検証する](./recovery-command-verified-in-human-execution-context.md)

## ソース

- [fix 結果](../../raw/fixes/20260926T071224Z-pr-3120.md)
- [レビュー結果](../../raw/reviews/20260926T075636Z-pr-3120.md)
