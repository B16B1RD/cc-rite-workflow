---
type: "heuristics"
title: "修正に添えるコメントは機構を語るほど次サイクルの検証対象面を広げる — 根拠はテストに置く"
domain: "heuristics"
description: "「なぜこの防御が必要か」を機構レベルで説明すると、反証可能な断定が増えた分だけ次 cycle の指摘も増える。根拠はコメントではなくテストに置き、コメントには観測事実だけを残す。"
created: "2026-08-03T07:46:56Z"
updated: "2026-08-03T07:46:56Z"
sources:
  - type: "reviews"
    ref: "raw/reviews/20260803T051521Z-pr-2094.md"
  - type: "fixes"
    ref: "raw/fixes/20260803T052647Z-pr-2094.md"
  - type: "fixes"
    ref: "raw/fixes/20260802T164206Z-pr-2094.md"
  - type: "reviews"
    ref: "raw/reviews/20260802T163111Z-pr-2094.md"
tags: []
confidence: high
---

# 修正に添えるコメントは機構を語るほど次サイクルの検証対象面を広げる — 根拠はテストに置く

## 概要

コードの修正そのものより、その修正に添えたコメントのほうが次のレビューサイクルで指摘を生みやすい。PR #2094 のある cycle では指摘 6 件のうち 4 件が、前 cycle の fix で自分が書いたコメント由来だった。**「なぜこの防御が必要か」を機構レベルで説明しようとすると、検証可能な主張が増えた分だけ指摘も増える。**

## 詳細

### 観測された内訳

前 cycle の fix で追加・変更したコメント由来の 4 件は次のように分かれた。

| 種別 | 内容 |
|---|---|
| 行番号参照 | 他ファイルの行番号ベタ参照が、**同一コミット内で既に陳腐化**（そのコミット自身が参照先の上に行を足したため） |
| 根拠の誤り | 根拠として挙げた 2 経路のうち 1 つが実測で成立しない |
| 射程の誤り | 文字クラスの射程を実挙動より狭く記述 |
| 複製 | 46 文字の条件節を literal で二重化（前 cycle に 1 度 drift 済み） |

### 対処: 個別パッチではなく圧縮

個別に文言を足す追加パッチではなく、**コメントを圧縮して反証可能な断定そのものを減らす**方向を選ぶ。実例では、C0 制御文字全域化の根拠として 2 経路を挙げていた 4 行を 2 行へ圧縮し、文字クラス名を書かず観測事実だけを残して、根拠をテストへ委ねた。

```
✗ 4 行かけて「なぜ C0 全域なのか」を 2 つの機構で説明する
   → 1 経路が実測で成立せず、クラス名が実射程より狭いと 2 件の指摘
✓ 観測事実だけを 2 行で書き、「T-17 / T-18 が固定」と添える
   → 主張の正しさはテストの Red/Green が保証する
```

**根拠はコメントではなくテストに置く。** テスト ID を書けば、その主張が正しいかどうかは走行結果が答える。散文で機構を説明すると、説明そのものが検証対象になる。

### load-bearing なコメントは mutation で固定する

逆方向の失敗もある。コメントが 8 行かけて「A ではなく B と書く理由」を説明しているのに、A へ書き換えても全テストが通る状態は珍しくない。**コメントで守っている不変条件は mutation テストで生存を確認しないと、リファクタで silent に失われる。** 隔離コピー（worktree でなく `cp -a` でも足りる）に mutation を当ててスイートを走らせ、Red になる assertion が実在することを実測してから完了とする。

つまり load-bearing なコメントには 2 つの帰結しかない。

- テストで固定できる → コメントは短く保ち、テスト ID を指す
- テストで固定できない → その説明は本当に必要か疑う（多くは撤去できる）

### 複製構造は「揃える」で終わらせない

「2 箇所の文言を揃える」という指摘に対し literal を揃えただけでは、複製構造そのものが残るため同じ drift が再発する。**揃えるのではなく、複製を生む構造を消すところまで行く**（local 変数へ 1 度だけ束縛する等）。ただし共有変数への抽出が、2 文の構造（自動詞/他動詞）の違いで片方の文を作り替える必要を生むなら、それは「重複排除に見せた機構追加」になる — 文言を揃えるだけの最小編集で足りるなら、そちらを選ぶ。

### 行番号アンカーは導入コミット内で自壊する

他ファイルを指すときは行番号ではなく**一意な文字列**（シンボル名 / 定数名 / メッセージの一部）をアンカーにする。同一コミットで両方のファイルを触る修正では特に危険で、参照を導入したコミット自体が参照先に行を足すと、コミット時点で既にずれている。検出機構がファイル名接頭辞を要求する regex だと、同一文脈内の裸の `:NNN` 形式は機械検出を素通りする。

## 関連ページ

- [Fix 修正コメント自身が canonical convention を破る self-drift](../anti-patterns/fix-comment-self-drift.md)
- [列挙・全称主張を持つ記述は書き直しでは収束しない — 撤去だけが指摘面を消す](../anti-patterns/enumeration-claim-rewrite-never-converges.md)
- [アサーションの検証強度は「該当行を壊して赤くなるか」でしか測れない](./mutation-testing-measures-assertion-strength.md)

## ソース

- [PR #2094 review results (cycle 3)](../../raw/reviews/20260803T051521Z-pr-2094.md)
