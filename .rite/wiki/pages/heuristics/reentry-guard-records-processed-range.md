---
type: "heuristics"
title: "再入ガードは「実行したか」ではなく「何を対象に実行したか」を記録する"
description: "完了済みを示すだけの再入ガードは、同じ run の途中で処理対象が増えたときに古い完了記録で新しい対象を覆い隠し、未処理のまま素通りさせる。ガードには処理本体が読む対象と同じ単位で範囲を記録し、最新の対象と一致するときだけ skip する。"
domain: "heuristics"
created: "2026-09-24T05:30:00Z"
generated: { by: "rite-wiki-ingest/claude-opus-5-5[1m]", at: "2026-09-24T05:30:00Z" }
sources:
  - type: "reviews"
    resource: "raw/reviews/20260924T044346Z-pr-3017.md"
tags: ["reentry-guard", "idempotency", "state-file", "test-fixture"]
confidence: high
promote: rite-plugin
---

# 再入ガードは「実行したか」ではなく「何を対象に実行したか」を記録する

## 概要

完了済みを示すだけの再入ガードは、同じ run の途中で処理対象が増えたときに古い完了記録で新しい対象を覆い隠し、未処理のまま素通りさせる。ガードには処理本体が読む対象と同じ単位で範囲を記録し、最新の対象と一致するときだけ skip する。

## 詳細

**存在だけのガードは、対象が増える経路で取りこぼす。** 一度だけ実行したい後処理を「完了ファイルがあれば skip」で守ると、同じ run を再開して後続のサイクルが新しい入力を生んだとき、その入力は一度も処理されないまま skip される。ガードを消す経路が「新しい run を始めたとき」にしかなければ、run の途中で増えた対象は構造的に拾えない。ガードが守るべきなのは「同じ対象を二度処理しない」ことであって、「一度でも実行したら二度と実行しない」ことではない。

**範囲の単位は処理本体が読む単位にそろえる。** ガードに記録する範囲は、処理本体が実際に入力として選ぶものと同じ単位にする（例えば「処理したレビュー結果ファイルの名前」）。cycle 番号や HEAD の SHA のような別の単位で記録すると、読み手が「処理済みか」を判定する基準と処理本体が「何を処理するか」を選ぶ基準がずれ、両者の間に取りこぼしや二重処理の隙間が生まれる。範囲を持たない旧形式の記録は「処理済み」と見なさず、もう一度処理する側に倒す。

**最新の対象を選ぶ規則は、字句順と更新時刻が食い違う fixture でテストする。** 「最新」を選ぶ規則（名前の字句順の末尾か、更新時刻の最大か）が読み手・書き手・処理本体に複製されていると、そのうち 1 箇所だけ規則を取り違える変異が起こりうる。字句順と更新時刻の順が一致する fixture では、どちらの規則でもテストが通ってしまう。字句順の末尾と更新時刻の最大を意図的に別のファイルにしておくと、規則の取り違えを検出できる。処理本体をテスト内の stub で再実装している場合、その stub と実物の規則がずれても検出できないため、実物を使うか、実物の規則を直接固定するテストを別に置く。

**仕組みを変えたら、根拠として挙げた実測を見直す。** ガードを入れた理由として過去の実測を挙げていても、述語を変えた後はその状況をガードがもう防いでいないことがある。その経路を実際に閉じているのが別の規則なら、rationale の根拠をその分担に書き直す。

## 関連ページ

- [テスト fixture の変異は各不変量・guard を単独で kill する配置で設計する](./fixture-mutation-isolates-invariants.md)
- [mutation は述語軸だけでなく配置・routing・副作用・到達の各軸に当てる](./mutation-axes-beyond-predicate.md)

## ソース

- [レビュー結果](../../raw/reviews/20260924T044346Z-pr-3017.md)
