---
type: "heuristics"
title: "終了コードの契約は、その形を作る経路をすべて数え上げてから書く"
description: "スクリプト冒頭の終了コード契約を「exit 1 は引数エラー」のように特定の値と原因で書くと、埋め込みインタプリタの例外、インタプリタの欠落（127）、pipefail 経由の外部コマンドの終了コード（2）など、同じ出力の形を作る別の経路が漏れる。経路を実装から数え上げ、入力を壊して一つずつ実行してから、呼び出し元の扱い（非 0 はすべて拒否など）に合わせた粒度で書く。"
domain: "heuristics"
created: "2026-09-24T09:40:00Z"
generated: { by: "rite-wiki-ingest/claude-opus-5-5[1m]", at: "2026-09-24T09:40:00Z" }
sources:
  - type: "reviews"
    resource: "raw/reviews/20260924T091612Z-pr-3042.md"
tags: ["exit-code", "contract", "documentation", "pipefail", "set-e"]
confidence: medium
promote: rite-plugin
---

# 終了コードの契約は、その形を作る経路をすべて数え上げてから書く

## 概要

スクリプト冒頭の終了コード契約を「exit 1 は引数エラー」のように特定の値と原因で書くと、埋め込みインタプリタの例外、インタプリタの欠落（127）、pipefail 経由の外部コマンドの終了コード（2）など、同じ出力の形を作る別の経路が漏れる。経路を実装から数え上げ、入力を壊して一つずつ実行してから、呼び出し元の扱い（非 0 はすべて拒否など）に合わせた粒度で書く。

## 詳細

**`set -e` の下では、失敗したコマンドの終了コードがそのまま出る。** `reason=$(python3 ...)` のようなコマンド置換が失敗すると、スクリプトはその場で終わり、終了コードは失敗したコマンドのもの（インタプリタの例外なら 1、インタプリタが無ければ 127）になる。`pipefail` があればパイプ内の外部コマンド（awk の 2 など）の終了コードも出る。スクリプトが自分で出す判定行は出ず、stderr もコマンド次第で空になりうる。

**入力を壊して一つずつ実行する。** 引数の欠落・未知値、読めない記録（文字コード・JSON の不正）、状態ファイルの形の崩れ、読み取り権限の欠如、依存コマンドの欠落を一つずつ作り、終了コード・stdout・stderr を表にする。表に出てきた形をすべて含むように契約を書く。

**粒度は呼び出し元の扱いに合わせる。** 呼び出し元がすべて「非 0 なら拒否」で扱うなら、契約は「非 0（判定行なし）」と書き、具体的な値は例として挙げる。特定の値を契約にすると、実装の変更で値が変わったときに契約だけが古くなる。

## 関連ページ

- [分岐を足したら、後ろのアームの出力がまだ使われるかを確かめる](./new-branch-leaves-later-arm-output-discarded.md)

## ソース

- [レビュー結果](../../raw/reviews/20260924T091612Z-pr-3042.md)
